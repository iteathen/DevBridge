# PATCH-POLLER Fresh-Window Handoff
## PP-HO030-0818-1912

Checkpoint time: 2026-08-18 19:12 PDT  
Repository: `iteathen/PATCH-POLLER`

## Fresh-window instruction

Continue PATCH-POLLER from the exact published GitHub frontier below.

**Do not create a new #30 branch.** Recover existing draft PR #50 and reconcile its current remote head and CI before editing. This handoff is a checkpoint, not a lease on the branch; if the branch advances after this file is published, continue from the newer head rather than resetting it.

Read `AGENTS.md` first. For #30 read `specs/PP-015-tool-inventory.md` together with PP-003, PP-012, and PP-013, plus any other specs named by AGENTS for the touched ownership boundary.

Preserve the project rules:
- PATCH-POLLER owns control-plane authority; model output, repository files, GitHub text, discovered binaries, and CLI help are proposals/data.
- Inventory reports local authority; inventory never creates authority.
- Presence-only PATH discovery must not execute discovered tools.
- Dynamic `tool.*` operations remain repository-code execution and require verified OS sandbox enforcement.
- Automatic unfamiliar-tool onboarding is disabled by default and may use only exact locally delegated commands/help probes.
- Help/man/spec output is untrusted data and cannot grant executable, argv, environment, credential, network, filesystem, Git, cleanup, or sandbox authority.
- Local operator manifests and the manifest root must remain outside controller/repository writable authority.
- Keep checkpoint-and-proceed semantics; do not turn uncertainty into a generic stop-and-wait gate.
- No Python in PATCH-POLLER or its project workflow.
- Do not use Codex, Spark, or another coding model for repository implementation unless the user explicitly changes that constraint.
- Prefer coherent ownership-boundary changes and test normal, failure, recovery, and adversarial boundary behavior.

## Completed security checkpoint

Issue #28 — **SECURITY [HIGH] Harden self-update validation so untrusted candidate code is not executed with supervisor authority** — is complete.

Merged PR: #47 `security: sandbox and sign self-update validation`

- tested PR head: `24132de7427e46fdd4f1163f92f328aa52518fc6`
- exact-head CI: run #466, Ubuntu success and Windows success
- merge commit: `9325dd6e733a9c02193e8a6e350db27d5ece1562`

Do not reopen or duplicate #28 absent regression evidence.

## Current active work: Issue #30

Issue #30 — **Audit, integrate, and optimize CLI developer toolchain for Runner App**

Existing draft PR: #50 `feature: add sandboxed dynamic tool onboarding`  
Branch: `feature/issue-30-dynamic-tool-onboarding`

Last code/test head observed before this documentation checkpoint:

`92cbe65dd34d6a285a88d9a0f5a08d00525adebd`

Commit title:

`test: prove manifest root cannot be controller-writable`

The PR was mergeable and draft at observation time.

### What PR #50 reports as implemented

- local operator manifests can register bounded `tool.*` deterministic operations without source changes;
- automatic unfamiliar-tool onboarding is disabled by default and requires local exact command/help delegation;
- help probes execute as repository-code operations through the verified OS sandbox with network denied, configured external read roots hidden, minimal environment, and no shell/control credentials;
- help output may shape only a closed bounded public parameter schema;
- authority-like parameters, raw argv/shell/command/environment fields, unsafe path-shaped generic values, and unknown parameters are rejected;
- generated wrappers remain repository-code operations under verified sandbox enforcement;
- generated manifests are persisted in a canonical local operator-owned manifest directory before registration so restart can recover them;
- dynamic operations are projected through the sanitized inventory while policy remains distinct from observed enforcement;
- unfamiliar-tool onboarding runs after current task dispatch so probe latency does not block the triggering task;
- tool inventory/onboarding is PP-015; context rollover remains PP-014.

## Current CI frontier

CI run #490 for head `92cbe65dd34d6a285a88d9a0f5a08d00525adebd` completed with **failure**.

The exact failing job/log was **not yet retrieved** in this window. The GitHub connector confirmed the failed run but its generic fetch route rejected the Actions jobs endpoint. Do not infer the root cause from the commit title alone.

The next controller should use GitHub Actions log-capable tooling (`gh` where available, per the repository GitHub workflow) or another exact log source before changing code.

## Evidence already inspected in this window

### `AGENTS.md`

The active branch guide reiterates:
- required assess/research/reassess/plan/implement/test cycle;
- controller authority over capability policy and effects;
- PP-015 invariants for inventory and dynamic onboarding;
- no Python;
- no Codex/Spark/other coding model for this campaign without explicit user change;
- mandatory boundary testing for dynamic operation schemas, manifests, onboarding probes, restart reconciliation, and verified enforcement.

### `specs/PP-015-tool-inventory.md`

PP-015 is active. Critical acceptance boundaries include:
- non-executing PATH discovery;
- sanitized stable inventory/projection;
- local-only operator manifest authority;
- dynamic operations requiring repository-code sandbox enforcement;
- automatic onboarding disabled by default;
- exact locally delegated help probes only;
- fail-closed probe behavior;
- persist-before-register restart reconciliation;
- no controller/repository write authority over local onboarding policy or manifest root.

### New root-boundary test at the observed head

`test/tool-onboarding-root-boundary.test.js` asserts that constructing `ToolOnboardingService` with `manifestDirectory` nested under the controller-writable `workspaceRoot` throws a policy error containing:

`outside the controller-writable workspace root`

The fixture creates both directories as real directories before construction and never expects the process runner to execute.

### Relevant implementation observation

At the observed head, `ToolOnboardingService` constructor resolves `workspaceRoot` and `manifestDirectory` with `path.resolve()` and rejects the manifest directory when `pathWithin(workspaceRoot, manifestDirectory)` is true.

The module also contains asynchronous canonical-directory validation using `lstat` + `realpath` for later reconciliation paths. Do not conclude that this is or is not the CI failure until the exact #490 job log is inspected.

## Required next actions

1. Re-fetch PR #50 metadata and resolve its **actual current head** after this handoff publication.
2. Fetch workflow runs for that exact head.
3. If the handoff-only commits triggered a newer CI run, distinguish documentation-only failures from the preserved #490 code/test failure frontier.
4. Retrieve the exact failing job/log for run #490 or the equivalent reproduced failure on the unchanged code/test content.
5. Read the branch versions of `AGENTS.md`, PP-015, PP-003, PP-012, and PP-013 before editing.
6. Inspect the exact failing test and owning implementation from evidence; do not weaken sandbox/manifest-root authority boundaries to make CI pass.
7. Repair the real issue, then run focused tests plus full cross-platform CI.
8. Finish PP-003/PP-015 documentation and exact-head acceptance audit required by PR #50.
9. Merge #50 only when issue #30 acceptance is satisfied on the exact head that passed Ubuntu and Windows.
10. After #30, reconcile issue #49 and any newer open issues/PRs before deciding the next work boundary. Older broad PRs #32/#33/#34 remain overlapping drafts and are not authoritative over issue-specific merged/current work.

## Checkpoint integrity note

This handoff records the observed frontier immediately before a documentation-only publication. The handoff commits themselves will advance PR #50 beyond `92cbe65d...` without changing runtime/test behavior. A fresh controller MUST observe/reconcile the remote head and CI before acting, and MUST NOT reset the branch merely to match a SHA recorded here.
