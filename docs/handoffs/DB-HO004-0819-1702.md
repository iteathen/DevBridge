# DevBridge handoff — DB-HO004 — 2026-08-19 17:02 PDT

## Purpose

This handoff freezes the current DevBridge Windows sandbox work after the user explicitly requested: **publish existing work and generate a handoff file**.

Stop implementation at this checkpoint. The next context should resume from the exact published branch state, reconcile CI, and continue the Windows security qualification without weakening the existing fail-closed contract.

The active work is the Windows secure repository-code execution slice of issue #49, implemented through PR #106.

## Repository checkpoint

- Repository: `iteathen/DevBridge`
- Default branch: `main`
- PR: `#106` — `Add verified Windows ProcessContainer sandbox`
- PR state at handoff: **open, draft, mergeable, unmerged**
- Working branch: `agent/windows-sandbox-mxc`
- PR base SHA: `8476370db7831cfa11b36e748f21d115011635cf`
- Exact implementation checkpoint immediately before this metadata-only handoff: `88f8f4c40a944c61ef874d70d2a2fb8ecfac856b`
- Implementation checkpoint commit message: `fix: let Windows boundary probe finish DACL cleanup`
- Current CI run for that implementation checkpoint: Actions run `#661`, run id `32313650853`
- Broader roadmap issue: `#49`

Do not infer that PR #106 is merge-ready. The Windows live boundary remains red.

## Concurrent branch reconciliation during handoff

The branch advanced while this handoff was being prepared. Those newer commits were detected and preserved; no rollback or force-push was performed.

The implementation checkpoint includes these commits after the earlier `24c720a100600c3bb395d6b3f846cd34b5dc7b9c` checkpoint:

1. `e0e0fea7d7abe84428fd36d6e8283e6004b0eaff` — `fix: close Windows job before draining redirected output`
2. `57eceb35b57be92b532e51cbeb874d645c5f85d9` — `fix: preassign MXC to a compiled Windows kill job`
3. `88f8f4c40a944c61ef874d70d2a2fb8ecfac856b` — `fix: let Windows boundary probe finish DACL cleanup`

The second commit replaces the race-prone post-start Job Object assignment approach with a compiled C# launcher that creates the child suspended, assigns it to a kill-on-close Job Object, then resumes it. Bootstrap compiles and manages that helper using the standard Windows .NET Framework C# compiler.

## Governing documents to reread before continuing

Before changing code, read the current versions of:

- `AGENTS.md`
- `docs/design-principles.md`
- `specs/DB-003-security.md`
- issue #49 and its current discussion
- PR #106 and its current diff/CI
- the latest handoff in `docs/handoffs/`

Treat current repository content as authoritative if any older handoff wording conflicts with it.

Core invariant: **DevBridge owns authority. Repository code and model workers are subordinate proposal/execution engines. A sandbox declaration or dependency presence is never proof of enforcement.**

## Current implementation state

### Windows provider

The branch implements `windows-processcontainer` behind the existing sandbox-provider / `prepareExecution()` control-plane boundary. Windows `auto` selects this provider; Linux retains Bubblewrap.

The provider uses Microsoft MXC ProcessContainer and remains fail-closed until DevBridge's own live adversarial probe passes.

### LPAC / Node startup conclusion

Do not reopen the earlier Winsock capability investigation without new evidence.

The minimum live LPAC capability pair found for Node startup is:

- `internetClient`
- `registryRead`

The deny-network configuration still uses MXC default `block`, enforcement mode `both`, and disallows local network. Live ARM64 qualification continues to report:

- `networkEgress:false`

So network egress denial is working under the current profile. Do not add `lpacCom`, private-network capability, or broader network authority merely to chase the remaining failures.

### Git administrative protection

The branch added a bounded recursive deny projection for existing `.git` administrative descendants rather than relying only on a parent `.git` deny entry.

This is **not yet sufficient on the live Windows 11 ARM64 AppContainer+DACL host**. Current live qualification still reports:

- `gitWrite:true`

The next context must determine why the exact deny projection is ineffective under the actual MXC AppContainer+DACL execution path. Do not weaken the Git-admin invariant to make the canary green.

Also strengthen/retain adversarial coverage for create/replace/rename/delete of Git administrative objects; merely protecting already-existing bytes is not enough if the worker can create new authority-bearing objects.

### Descendant process containment

The branch now includes a compiled Windows kill-on-close Job Object launcher intended to eliminate the start-before-assignment race by using `CREATE_SUSPENDED`, assigning the child to the job, then resuming it.

Ordinary Windows x64 regression coverage is green, but the live prepared Windows 11 ARM64 boundary probe still reports:

- `descendantStarted:true`
- `descendantContained:false`

Therefore the real ProcessContainer/MXC path still does not satisfy DevBridge's process-tree invariant. Do not treat the unit/regression success as qualification evidence.

The next context should trace the exact live launcher/process nesting and prove whether MXC or a descendant is escaping/breaking away from the outer job, or whether the canary/cleanup ordering is observing the wrong lifetime. Preserve the create-suspended preassignment principle unless evidence disproves it.

### CMake/CTest managed scratch

CMake/CTest operations now explicitly project controller-owned managed scratch into repository-code sandbox execution. The earlier stale fixture was corrected, and current ordinary Windows x64 tests show this area green.

### Real workload acceptance

`test/windows-workload-acceptance.test.js` and a CI `Windows workload acceptance` step exist. The step runs only after Windows sandbox qualification succeeds.

It is currently skipped because the boundary qualification fails first.

Once the boundary turns green, this acceptance must prove real CMake/CTest plus the end-to-end task path through the verified Windows provider before PR #106 is considered ready.

## Exact current CI state — implementation checkpoint `88f8f4c...`

Actions run `#661` / `32313650853` completed **failure**.

### Green jobs

- `smoke (ubuntu-latest)` — success
- `smoke (windows-latest)` — success
- `test (windows-latest)` — success

The current Windows x64 ordinary regression suite is green, including the compiled Job Object launcher coverage.

### Linux full-suite failure

`test (ubuntu-latest)` fails exactly one regression:

- test: `Windows ProcessContainer wrapper probe deadline covers DACL fallback cleanup`
- file: `test/sandbox-probe-timeout.test.js`
- observed: `40000 !== 90000`

Current helper `effectiveSandboxProbeTimeoutMs(executable, timeoutMs, platform)` receives a Windows path and `platform === 'win32'`, but uses host `path.basename()`. On Linux, POSIX path parsing does not split Windows backslashes, so the helper fails to recognize `powershell.exe`.

Likely minimal correction: use Windows path parsing when the supplied platform is Windows, e.g. `path.win32.basename(...)`, while retaining normal host/POSIX parsing elsewhere. Re-run Linux and Windows ordinary suites after the correction.

This is a cross-platform regression in the timeout helper/test, not evidence that the Windows security boundary is working.

### Live Windows 11 ARM64 qualification failure

Prepared host:

- Windows 11 Enterprise `10.0.26200`
- ARM64
- Node `22.16.0` ARM64
- MXC reported tier: `appcontainer-dacl`
- `needsDaclAugmentation: true`
- BFS not compiled in

Host preparation and the MXC prerequisite probe succeed.

The adversarial boundary test still fails. The bounded failure tail reports:

- `gitWrite:true`
- `networkEgress:false`
- `descendantStarted:true`
- `descendantContained:false`

This is the primary blocker. Workload acceptance and `doctor` verification remain skipped as a consequence.

## Security / architecture conclusions that must survive continuation

1. **Keep fail-closed semantics.** `repositoryCodeExecution:true` must not be reported until the live adversarial probe passes every required boundary.
2. **Do not weaken `.git` isolation.** Current ARM evidence says the worker can still mutate Git administrative state.
3. **Do not weaken network policy.** Egress is already denied with the minimal LPAC startup capabilities.
4. **Do not accept unit-only process-tree evidence.** Live ARM says detached descendant containment still fails.
5. **MXC remains preview technology.** Package presence, `--probe`, or config shape alone is insufficient evidence.
6. **AppContainer+DACL is the actual qualified tier on the current ARM runner.** Any proposed fix must be tested against that live path, not just an ideal BaseContainer/BFS path.
7. **Control-owned worker IPC remains authoritative.** Windows uses non-authoritative staging for worker results followed by control-plane import/validation; do not expose the authoritative result object broadly to the worker.
8. **CMake/CTest build state belongs in managed scratch, not the Git worktree.**
9. PR #106 is only the Windows sandbox/execution-blocker slice. **Do not close issue #49 solely because PR #106 eventually passes.** Recheck the issue's remaining model/worker-adapter and end-to-end acceptance requirements.

## Recommended continuation order

1. Confirm `agent/windows-sandbox-mxc` still descends from this handoff checkpoint. If the branch advanced, reconcile; never blindly reset or force-push newer work.
2. Reread `AGENTS.md`, design principles, DB-003, issue #49, PR #106, and this handoff.
3. Fix the isolated Linux Windows-path parsing regression in `effectiveSandboxProbeTimeoutMs` and restore ordinary Linux + Windows suites to green.
4. Trace the actual ARM64 ProcessContainer launch hierarchy with the compiled job launcher. Determine why the live detached child still survives despite create-suspended preassignment.
5. Trace MXC DACL application/order for project read-write plus exact Git-admin denies. Determine why `.git/config` remains writable on the live host.
6. Strengthen the live canary if necessary so Git-admin create/replace/rename/delete and descendant escape are all explicitly observed.
7. Re-run the expensive Windows 11 ARM64 qualification only after cheap/local and ordinary CI gates are green.
8. When the boundary is green, run the existing Windows workload acceptance and `doctor` verification through the same verified provider.
9. Reassess PR #106 against DB-003 and issue #49 before moving it out of draft or merging.
10. After this slice is accepted, return to issue #49's remaining broader acceptance items rather than declaring the roadmap issue complete prematurely.

## What not to do

- Do not revert the reconciled commits listed above merely because an older handoff stops at `24c720a...`.
- Do not broaden LPAC/network capabilities to fix `.git` or descendant containment.
- Do not interpret MXC `--probe` success as sandbox verification.
- Do not skip the live ARM64 adversarial canary in favor of Windows x64 unit tests.
- Do not move workload acceptance ahead of the boundary gate.
- Do not force-push over concurrent branch movement.
- Do not merge PR #106 while the live Windows boundary is red.

## Handoff completion rule

This handoff commit must contain **only** this Markdown file and its SHA-256 sidecar. The implementation checkpoint remains the parent commit recorded above.
