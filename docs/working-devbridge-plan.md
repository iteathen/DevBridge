# Working DevBridge implementation plan

Status: active dependency-ordered plan derived from `working-devbridge-assessment-2026-08-27.md` and reconciled through `docs/testing/DB-HO088-goal-reconciliation-and-operational-frontier.md`. Phases 0–2 and the no-elevation Stage 8 software continuation are complete. The exact installed candidate is staged and fail-closed; the next non-GPU product effect is one administrator-authorized protected-service refresh, followed by the Linux C canary/restart repeat and the independently gated Windows media/profile canary.

## Governing method

Each implementation slice follows:

`assess -> primary-source research -> reassess -> document -> plan -> implement -> test -> document implementation`

Every slice stays inside one coherent owner. A child LEGO uses only local neutral inputs/outputs; it does not name siblings, current callers, repositories, providers outside a provider adapter, or temporary topology. The parent composition owns wiring. No legacy compatibility path is added unless a live specification requires a bounded migration, and no migration path remains as a second production authority.

## Phase 0 — establish one truthful integration baseline

Owner: repository integration and documentation, not runtime behavior.

1. Preserve the dirty retired fast-track checkout untouched.
2. Use the isolated core-recovery branch rooted at PR #300 head.
3. Review and integrate PR #300 into its current integration target after exact-head checks remain green.
4. Merge the three unique current-main commits into the core-recovery branch.
5. Resolve documentation conflicts by current specification authority; preserve public entry-point tests and the accepted guest-runtime/chat-exchange designs.
6. Run preflight, architecture gates, focused changed-owner tests, full Windows tests, and doctor.
7. Publish the isolated branch and open/update an integration PR; do not update `main` directly.

Exit evidence:

- one exact candidate contains the recovery lineage plus all current-main changes;
- no GPU/CUDA/ROCm implementation is introduced;
- exact changed paths, tests, and remaining platform gates are documented.

## Phase 1 — close the Windows protected-authority gate

Owners: protected-authority reconciler and Windows platform adapter.

1. Run the latest exact candidate's ordinary, non-construction `devbridge setup` path.
2. Allow at most the one bounded platform elevation transaction already defined by #292 when reconciliation is required.
3. Require automatic return to the original ordinary process.
4. Prove service/runtime generation, service SID/token, read/mutation/acceptance pipe policy, ordinary mutation denial, direct protected-state denial, exact positive lifecycle canary, and exact fixture cleanup.
5. If candidate health fails, preserve rollback evidence and repair the smallest owning Windows adapter/reconciler seam; do not add another setup command or direct service workaround.
6. Record the exact physical evidence on #292/#177 and in repository testing documentation.

Exit evidence:

- one ordinary setup invocation reaches the read-only construction gate;
- ordinary identity lacks protected mutation/storage authority;
- exact protected lifecycle positive canary succeeds and cleans its owned fixture;
- no unprotected local-provider fallback exists.

## Phase 2 — reconcile the stalled Ubuntu production image construction

Owners: canonical image canary, Hyper-V image-construction adapter, and Ubuntu media/preparation adapters.

1. Re-observe the exact durable construction subject, provider identity, VM identity, attached media, disk growth, liveness, and console evidence.
2. Use the supported `setup --construct` re-entry only after Phase 1's ordinary setup gate passes.
3. Repair the provider-local thumbnail normalization defect before making any VM recovery decision:
   - preserve the neutral `captureInstallConsole()` result contract;
   - accept the documented exact RGB565 byte count and the physically observed exact four-terminal-byte variant only;
   - retain the first `width * height * 2` bytes for the observed variant and reject other counts;
   - do not interpret image pixels as width/height metadata;
   - add executable normal, exact-variant, malformed-count, and output-boundary tests rather than regex-only source assertions.
4. Re-run non-construction setup on the exact candidate, then make one supported construction re-entry and require a valid bounded console artifact without changing VM state.
5. Treat steps 3–4 as complete at exact merge `4d5dc5633d978773a3adf02414acbc4234076ca6`; the artifact proves the existing guest failed in the pinned apt late command.
6. Reproduce the exact snapshot transaction in a disposable Ubuntu 26.04 environment outside the production construction frontier (complete):
   - first run snapshot update;
   - then simulate, download, and install the exact six pinned packages;
   - preserve complete bounded apt output and exact base/snapshot identity;
   - remove the temporary diagnostic workflow after evidence is captured.
7. Treat the v5 source-wide `APT::Snapshot` design as rejected physical evidence. Its exact replacement subject failed in Curtin curthooks while installing UEFI boot prerequisites, before Subiquity's update or DevBridge's late-command phases.
8. Preserve Curtin's native prerequisite APT behavior. Use the installer APT pass-through only for a narrow all-package unattended-upgrades blacklist, which is ignored by ordinary apt operations and removed by Subiquity's existing restore step before late commands.
9. Keep the explicit DevBridge-owned snapshot update/install transaction after restore. Make every snapshot metadata refresh fail on any source error; do not treat stale or live-archive indexes as snapshot evidence.
10. Preserve the required security baseline deliberately under the same accepted snapshot. Resolve final top-level candidates across every enabled release/update/security pocket using Debian's specified version ordering, run a no-removal snapshot-bound upgrade before the exact package installation, and require final exact-version qualification.
11. Prove the comparator against dpkg on Ubuntu CI, prove pocket conflicts and missing/oversized index failures locally, and prove that the emitted command order is blacklist -> restore boundary -> exact update -> bounded upgrade -> exact install.
12. Do not add a CA bootstrap stage merely because the minimal diagnostic container lacked it: the physical server target already completed its HTTPS snapshot update. Reassess only if the next exact construction subject produces contrary target evidence.
13. Keep executable/package paths and solver mechanics local and preserve neutral authority/seed contracts. Add bounded installer failure telemetry only through the seed owner's static contract; never expose host secrets or accept caller-supplied commands/paths.
14. Advance the recipe/package generation for every desired guest-byte or install-behavior change, deriving a new exact construction subject rather than reusing either failed media subject.
15. Treat the v6 physical construction as rejected access evidence: it reached an operating installed boot with healthy provider integration and DHCP, while its exact seed disabled SSH installation and its package authority omitted `openssh-server`.
16. In the Ubuntu image owner, enable key-only target SSH installation, resolve and qualify `openssh-server` through the existing exact snapshot package contract, and advance recipe, package, and output generations. Do not repair the guest or add a provider/network fallback.
17. Add focused tests for seed SSH policy, exact package selection, qualification projection, and immutable generation changes. Run preflight, full Windows tests, and all Ubuntu/Windows CI jobs before another protected-runtime install.
18. Follow with a neutral bounded readiness-observation slice: consume elapsed time and a local clock, classify two minutes as the expected frontier and ten minutes as the hard deadline, schedule non-terminal observations at no more than 30 seconds, and block after expiry without repair. Do not expose transport/provider identity through that contract.
19. Reconcile the existing failed effect, create the replacement through the canonical image/lifecycle authority, and retain old subjects until the replacement is qualified.
20. Treat the exact v7 physical subject as rejected installer evidence: the running VM advanced its VHDX, then hit a kernel fault in `ovl_iterate_merged` during Curtin `stage-extract`; the existing twenty-minute no-progress gate captured console evidence and stopped without repair.
21. Select Canonical's supported `ubuntu-server-minimal` source in the Ubuntu seed owner. This exact source is a single `fsimage`, while the default server source is `fsimage-layered`; preserve every other package/network/access/payload/qualification boundary and do not add kernel flags or retry the failed guest.
22. Advance recipe and output generations so the source-selection change derives a new subject. Prove emitted source selection, authority subject change, topology rejection, preflight, full local tests, and all four hosted jobs before another physical construction.
23. Treat the v8 access result as rejected host-key-binding evidence: installation and installed boot succeeded, but strict verification proved cloud-init replaced the subject-owned expected key. Preserve the failed subject and never learn the replacement key into host trust.
24. In the Ubuntu seed owner, project the exact Ed25519 pair through target `user-data.ssh_keys` with deletion of inherited image keys retained, and remove ordinary host-key `write_files` entries. Keep the access/probe/provider contracts unchanged.
25. Advance recipe/output generations, prove the exact SSH-module projection and immutable subject change, and repeat the complete software/CI/install gates before a fresh physical subject.
26. Treat the exact v9 result as rejected bridge-state-root evidence: installation, installed boot, strict subject-owned host-key verification, and bridge entry all succeeded, but the unprivileged guest identity correctly received `EACCES` when the bridge agent tried to create `/var/lib/devbridge`.
27. Move the bridge agent's Linux default into its own persistent per-user XDG state root, preserving its explicit override and Windows default. Reject relative roots, keep module import side-effect-free, and do not add sudo, broader access identity, image-seed directory ownership, provider inputs, or legacy-path migration.
28. Update whole-image sanitization to remove the neutral user-state root, advance recipe/output generations, and prove root selection, override, platform behavior, import isolation, sanitizer projection, immutable subject change, and existing bridge boundaries before full local/hosted verification.
29. Install only the exact accepted runtime and construct one fresh immutable subject through the public gate. Preserve the v9 VM/disk/journal as physical failure evidence until replacement qualification is terminal.
30. Finalize, sanitize, qualify, publish to the local immutable image library, and retire only exact superseded construction artifacts after the new image is verified.

Software checkpoint: steps 16–18 are implemented. Fifty-one focused tests, repository preflight with 39 targeted tests, and the complete 1,223-test Windows suite passed with zero failures. PR #319 passed all four Ubuntu/Windows smoke and full jobs in CI run `33124150724` and merged at exact recovery commit `9925905622d31caa985c27a47c18ebf817748feb`. Exact v7 physical construction then produced the bounded OverlayFS failure evidence recorded in `docs/testing/DB-HO005-issue-197-physical-qualification.md`. Steps 21–22 are complete: 41 focused tests, preflight with 50 targeted tests, the complete 1,330-pass local suite, and all four hosted jobs in CI run `33144784604` passed with zero failures; PR #354 merged at exact accepted commit `041ebd4cb364c8141cfedc1b84c1902c90bd0423`, and the zero-state bootstrap installed that exact component. Fresh v8 subject `subject-a527ba4de198188473c3f22c7f4778af` completed installation and reached installed SSH, then blocked after the exact ten-minute window because cloud-init replaced the subject-owned host key. Steps 24–25 are complete: the same local gates and all four hosted jobs in CI run `33146383449` passed, PR #355 merged at exact accepted commit `452565fa931d7a0e04849b3e32a7f6c60e003483`, and the zero-state bootstrap installed that exact component. Fresh v9 subject `subject-1a3e4a19173f0f6c75fd0758e287bcaf` physically accepted the host key, then failed closed in guest qualification because the bridge agent's Linux default required privilege to create `/var/lib/devbridge`. Steps 27–29 and the local qualification/publication portion of step 30 are complete: 58 focused tests, preflight with 50 targeted tests, the complete 1,332-pass suite, all four hosted jobs in CI run `33148218419`, exact accepted-runtime installation, and physical v10 subject `subject-8a7a9afe109534b2c128f272ab586bcf` all passed. The terminal revision-12 journal binds a qualified parentless `ubuntu-2604-production-v5` VHDX in the local immutable image library. Exact-subject cleanup of older failed construction artifacts remains pending behind a supported lifecycle surface; no manual deletion is authorized.

Exit evidence:

- a qualified Ubuntu base-image generation exists locally;
- construction is restartable and reaches terminal journal state;
- signed source cache and exact recipe/package/payload identities are preserved;
- no stale running VM or orphaned exact-owned construction artifact remains after safe terminal cleanup.

## Phase 3 — finish Linux protected lifecycle authority

Owners: Linux authority plan/inspection/reconciliation/protection adapters.

1. Rebase the preserved #293 Linux slices onto the shared protected-authority reconciler from the synchronized core baseline.
2. Preserve the qualified neutral identity and provider-free service-entry contracts, but correct the primary-source discrepancies recorded in `docs/testing/DB-HO010-issue-293-linux-authority-reassessment.md`: use `Type=exec`, make the read socket parent service-writable/group-traversable (`0750`), use NSS-aware account/group evidence plus actual process groups, and prove the complete content-addressed runtime generation.
3. Add bounded reconciliation for exact system account/group, root-owned immutable runtime, service-owned mutable state, systemd unit, split socket parents, and service lifecycle.
4. Detect modular `virtqemud` versus legacy/proxy socket topology and locally supported authorization. The first supported `service-group` adapter must prove every effective endpoint and fail closed on wide/polkit topology until exact policy evidence is implemented; it must not select the first socket path that exists.
5. Prefer fine-grained service-only polkit/object policy where supported; otherwise require an explicitly proven service-only socket/group policy. Never add the ordinary operator/model identity to provider RW authority.
6. Preserve QEMU DAC and mandatory-access-control layers and exact qcow2/backing-chain ownership.
7. Qualify interruption/re-entry and arbitrary path/command/domain/XML/provider-object rejection in hosted tests.
8. Run real Linux negative and positive qcow2/domain canaries on a capable host before declaring Linux ready.

Hosted checkpoint: the corrected plan/service entry, read-only NSS/systemd/process/runtime inspection, and shared refresh adapter are implemented and locally qualified on `security/293-linux-authority-recovery`; see `docs/testing/DB-HO010-issue-293-linux-authority-reassessment.md`. No privileged Linux mutation or readiness composition is attached. Steps 3–8 remain active work.

Protected-mechanics checkpoint: DB-HO011 records the exact name/numeric-identity ownership model, atomic generation/unit contract, append-only ordinary membership, exact service membership, and provider-separation plan for the next isolated slice.

Protected-storage checkpoint: DB-HO012 is integrated at `bcfae330cb223ed7705feff888bd9c9690c137e4`; all four CI jobs passed and Ubuntu exercised the real-filesystem canary. DB-HO013 now scopes the next dependency to admitted lifecycle claim plus ownership/transaction persistence, with no account, service, provider, VM, or runtime mutation.

Protected-transfer checkpoint: DB-HO013 is integrated at `f1f0400f7087a9ea81f69bb68b0d16c1af9b2524`. DB-HO015 is integrated at `f9d410f5399bb2def945179ae93dfc8c47fdacfa`: descriptor-bound streamed transfer preserves the 1 MiB record boundary, uses a separate bounded streaming contract, and passed all four hosted jobs including its real Linux canary.

Protected-tree checkpoint: DB-HO015 is integrated at `f9d410f5399bb2def945179ae93dfc8c47fdacfa`, with all four CI jobs green and the Ubuntu real-filesystem canary passing. DB-HO016 isolates immutable tree installation behind neutral relative-path and file contracts; lifecycle generation projection composes it only after this lower transaction is independently qualified.

Protected-storage checkpoint: DB-HO012 isolates normalized no-follow directory/file policy, bounded atomic replacement, file and parent-directory durability, and exact pending-file recovery before lifecycle records or runtime staging consume that capability.

Exit evidence:

- Linux setup is one-command and resumable;
- ordinary identity lacks libvirt RW and backing-store mutation authority;
- protected authority can perform the exact lifecycle operation;
- unqualified Linux hosts fail closed without a local-provider fallback.

## Phase 4 — complete fresh-host install and image supply

Owners: image acquisition/cache, provider-specific image builders, setup authority, distribution, and activation adapters.

Order:

1. exact reconstructable image acquisition/cache (#178);
2. Ubuntu construction/publication completion (#197);
3. generalized Windows media construction with explicit operator media/licensing authority (#198);
4. protected Windows activation authority (#199);
5. private immutable image artifact distribution without developer-repository assumptions (#200);
6. blank-slate install, image-loss, rebuild, and re-entry qualification (#201).

Setup remains discover-first. Repository selection never provisions one VM per repository. Provider/image/profile decisions remain separate from repository/workspace approval.

Protected-profile checkpoint 2026-08-28: `docs/testing/DB-HO035-issue-360-protected-profile-configuration.md` records the bounded accepted-configuration and declaration-CAS slice. Setup projects stable subjects into neutral desired declarations; the elevated child verifies/adopts the exact protected image, reconciles and re-observes its foundation resources, and registers by revision; the ordinary read client proves the declaration digest and consumes only aggregate protected readiness. The full repository suite and candidate preflight pass; physical activation is pending. Issue #364 and `docs/testing/DB-HO038-issue-364-conflict-retirement.md` replace the former ad hoc NAT prerequisite with setup discovery, opaque exact-subject consent, elevated re-observation, and retirement through the existing one-shot protected transaction.

Protected-activation checkpoint 2026-08-28: `docs/testing/DB-HO036-issue-360-protected-environment-activation.md` adds the provider-free setup activation adapter and corrects public setup ordering. Only an exact absent environment or interrupted create crosses the protected lifecycle client; final healthy status is independently re-read, and the existing construction workspace stage verifies roots before publishing credential-free routes. Incomplete images return before protected configuration/environment mutation. Focused gates, candidate preflight, and the complete 1,467-pass repository suite are green. The #364 setup-owned conflict flow is now the physical entry to UAC activation; the Linux fixed-source C canary and operational configuration/execution opt-in remain after that live route proof.

Operational-configuration assessment 2026-08-28: `docs/testing/DB-HO039-issue-360-operational-configuration.md` records the remaining gap between verified protected routes and a usable normal runtime. The planned publisher is setup-owned, deterministic, digest-bound, restart-reconcilable, refuses unmanaged config drift, enables controller-plan execution only after route readiness, and keeps coding-model adapters/direct-host fallback disabled. This work may proceed without provider mutation while the physical UAC gate waits.

Multiple-queue checkpoint 2026-08-28: `docs/testing/DB-HO037-issue-363-multiple-queue-runtime.md` replaces the false singular operational queue with one ordered collection. Credential, conditional-request, mutation-pacing, and rate-budget authority are shared once; queue runtimes retain isolated run/lease/status/handoff state and execute serially under one daemon admission. Ordinary queue failures are isolated, shared rate-limit evidence stops globally, and no singular compatibility path remains. This enables setup to publish every accepted repository later without multiplying daemons or execution concurrency. It does not delay the physical VM frontier further: protected setup and the Linux/Windows C canaries remain next.

### Windows critical-path checkpoint

Ubuntu v10 is admitted, but DevBridge is not minimally functional until the real Windows image and dual-guest C route pass. The Windows work now proceeds without another broad infrastructure detour:

1. bind approved local Microsoft media to exact integrity and edition/build metadata;
2. implement unattended Setup -> Audit Mode -> pinned tool provisioning -> qualification -> sanitation -> Sysprep shutdown;
3. replace the current Hyper-V Secure-Boot-off assumption with a neutral protected-boot requirement mapped locally to supported Windows 11 Secure Boot/vTPM settings in both construction and persistent environments;
4. establish fresh per-environment Windows enrollment plus a distinct non-administrative routine execution identity;
5. admit the generalized VHDX and materialize the persistent Windows profile;
6. run the exact Windows+Ubuntu C transfer/compile/execute/result proof, then repeat it across normal restart/reuse.

Interactive Windows Setup pages, installer UI, guest UAC prompts, credential prompts, and GUI babysitting are failures, not supported continuations. Construction privilege exists only in documented unattended setup/audit passes. Routine repository work is noninteractive and non-administrative. Exact assessment, Microsoft source research, ownership seams, and the primitive plan are recorded in `docs/testing/DB-HO030-issue-198-windows-construction-assessment.md`.

Checkpoint 2026-08-28: bricks 1–4 and the restartable physical-canary composition are implemented and pass repository preflight/focused tests. The host read-only gate proves Hyper-V protected-boot operations, IMAPI, managed connectivity, memory, and the declared sparse-allocation storage budget without UAC. A supply-chain reassessment replaced the insufficient bootstrapper-only compiler claim: the authority now binds Microsoft's fixed Build Tools `17.14.39` artifact, requires installed build `17.14.37614.0`, explicitly selects compiler/CMake/Windows SDK components, disables installer self-update, and rejects version drift during both preparation and qualification. The exact remaining external input is approved official Windows ISO media; bounded discovery currently finds none. No image, persistent Windows profile, or dual-guest C acceptance is claimed yet. Continue at the media inventory/approval frontier rather than reopening lower-level topology work.

Persistent-access reassessment 2026-08-28: `docs/testing/DB-HO040-issue-198-windows-persistent-access.md` records one prerequisite exposed before physical activation. The generalized image contains a fixed LocalSystem access-seed service and non-admin account policy, but the protected environment runtime does not yet compose per-environment Windows credential creation, exact seed delivery, strict PowerShell Direct re-observation, or the resulting routine connection. Implement that Windows-owned adapter before multi-profile setup; construction Administrator material must never become repository execution authority.

Persistent-access implementation checkpoint 2026-08-28: the Windows-owned adapter is implemented with fixed `devbridge` material, exact transient seed cleanup, provider-owned PowerShell Direct proof, and default protected-runtime composition. The unused injected-access compatibility path is deleted. Focused tests and repository preflight pass. One unrelated liveness test failed only during the first concurrent complete-suite run and passed in isolation; the clean complete-suite repeat then passed 1,529 total / 1,514 passed / 15 platform skips / zero failures. Commit this checkpoint and resume physical activation.

Multi-profile route assessment 2026-08-28: `docs/testing/DB-HO041-issue-360-multi-profile-route-admission.md` records a dual-guest blocker found while UAC activation waits. Construction currently leaves every route non-preferred and verifies a subject's preferred route rather than the exact subject/profile route being admitted. Preserve first-admitted preference and bind pre-publication verification to the exact derived workspace target before attempting a second guest profile.

Multi-profile route implementation checkpoint 2026-08-28: first-admitted preference is now stable, sole non-preferred policy is normalized only when topology expands, ambiguous existing topology fails closed, and health/put/execute proof binds to the exact subject/profile workspace before policy publication. Focused 21/21, preflight, and the complete 1,531-total / 1,516-pass / 15-skip / zero-failure suite are green. Commit this primitive, then resume the UAC-gated physical Linux activation without changing its accepted image or profile.

Physical conflict reassessment 2026-08-28: the approved one-shot host elevation refreshed the protected runtime and retired the exact stale translation, then failed closed on a separate unmarked deterministic target switch left by an earlier partial attempt. Extend the existing neutral conflict stud with one provider-local, exact-subject retirement for an unattached internal switch with only link-local address state. Never stamp/adopt the unmarked object from planned state alone. Test and push this correction before spending the final available UAC transaction.

Partial-switch implementation checkpoint 2026-08-28: the unchanged neutral conflict stud now classifies an exact unmarked target switch only when it is internal, guest-unattached, and has no configured non-link-local IPv4 state. Subject-bound re-observation/removal remains inside the one-shot elevated child. A live read-only probe produced subject `00dbc6dd7feff1861a435eb45c715739819ce3d1ec75e044bc50919e9d8b67ef`; focused 18/18, preflight 99/2/95, and full 1,541-total / 1,526-pass / 15-skip / zero-failure evidence are green. Commit and push before requesting the last available UAC transaction.

Physical switch-consent checkpoint 2026-08-28: `dc60348` is pushed and the exact consent is persisted, but the final announced host elevation was cancelled/refused before the child transaction began. Re-observation proves no partial second effect: the refreshed protected service remains running, no NAT exists, the target switch remains unchanged/unmarked, and no persistent VM exists. Do not request another UAC during the operator's stated three-day unavailable window. Continue no-elevation acceptance/tooling work and later resume with the exact saved subject.

Deterministic C acceptance assessment 2026-08-28: `docs/testing/DB-HO042-issue-360-c-route-acceptance.md` replaces the unsuitable self-materializing diagnostic idea with one normalized, all-ephemeral controller plan. It uses only registered CMake/CTest operations, an exact stdout challenge, target-aware SHA-256 reporting, empty changed paths, and verified scratch cleanup. Implement the fixed verbose CTest evidence option and plan factory while host elevation is unavailable; physical route proof remains separate and unclaimed.

Deterministic C acceptance implementation checkpoint 2026-08-28: the normalized plan factory and closed optional verbose CTest result channel are implemented. Focused tests pass 9/9 and repository preflight passes 100 syntax files, 2 JSON files, and 96 targeted test files. The first complete-suite run had one unrelated transient Windows temporary-file `EPERM`; its owning daemon-governance suite immediately passed 6/6, and a fresh complete-suite run passed 1,529 with 15 platform skips and 0 failures. The exact physical setup re-entry was then approved: protected reconciliation crossed the saved partial-switch frontier and started one exact Linux installer VM. Subsequent ordinary bounded observations show healthy provider status and advancing VHDX allocation; no second UAC has been requested.

Canonical payload reassessment 2026-08-28: the Linux installer subsequently completed guest qualification and finalization, but immutable publication correctly refused different bytes under existing output generation `ubuntu-2604-production-v5`. `docs/testing/DB-HO043-issue-197-canonical-payload-text.md` proves the new authority was accidental CRLF/LF source-delivery drift: current `i/lf w/crlf` payload members normalize to exact accepted generation `guest-image-688e4295403761cf5ae78fd1`. Fix the payload owner to canonical LF with bare-CR refusal; do not bump the release, overwrite the accepted image, edit journals/catalogs, or delete the retained failed subject.

Canonical payload implementation checkpoint 2026-08-28: canonical LF derivation and bare-CR refusal are implemented at the self-contained payload boundary. The current Windows checkout now reproduces exact accepted payload generation `guest-image-688e4295403761cf5ae78fd1`. Focused tests pass 52/52, preflight passes 101 syntax files + 2 JSON files + 97 targeted test files, and the complete suite passes 1,531 with 15 platform skips and 0 failures. The qualified/finalized colliding VHDX remains exact retained evidence; the accepted image/catalog are unchanged and no construction VM remains. The operator has declared no UAC available for three days, so do not launch elevation or bypass protected-runtime freshness. Publish this fix and continue no-elevation work; resume exact one-command refresh/re-entry only when the operator returns.

Semantic payload generation reassessment 2026-09-01: exact physical subject `subject-729bbc937efefaca4c1e4743fde1a75f` qualified, finalized, powered off, and was retained, then immutable publication correctly refused different bytes under existing output generation `ubuntu-2604-production-v5`. DB-HO109 distinguishes this from DB-HO043: canonical LF is intact, but the guest payload expanded semantically from six to nine members and now derives `guest-image-6c102cff53ad6d9f10f03530`. Advance only the setup-owned Ubuntu output generation to `ubuntu-2604-production-v6` and bind that release label to the exact payload generation before construction. Preserve the accepted image, retained subjects, journals, and catalog; do not rerun construction until software and hosted qualification are green and the installed candidate receives fresh physical authorization.

Semantic payload generation implementation checkpoint 2026-09-01: the setup-owned authority now binds `ubuntu-2604-production-v6` to exact payload `guest-image-6c102cff53ad6d9f10f03530` and rejects future payload drift before physical construction. No payload, provider, library, journal, catalog, or retention owner changed. Focused tests pass 53/53; repeated preflight passes 2 standalone artifacts, 253 syntax files, 2 JSON files, and 203 targeted test files; the full suite passes 2,061 with 21 expected platform skips and 0 failures out of 2,082. Publish this narrow Stage 8 correction and require all four hosted Ubuntu/Windows smoke/full jobs before protected-runtime installation or another physical invocation.

Acceptance-isolation reassessment 2026-08-28: `docs/testing/DB-HO044-issue-360-ephemeral-parent-cleanup.md` records why root `CMakeLists.txt`/`main.c` is not portable to arbitrary configured repositories. Extend the generic controller-plan cleanup ledger to own only parent directories observed absent before ephemeral writes, then remove them exactly, deepest-first and non-recursively after files. Use that primitive to place the C fixture under a challenge-derived directory and validate the complete ordinary task envelope. Do not add a special compiler path or acceptance bypass.

Acceptance-isolation implementation checkpoint 2026-08-28: the generic executor now durably owns only absent-before-effect ephemeral parents, cleans files before deepest exact directories, preserves pre-existing parents, and fails closed without recursive deletion when unexpected content appears. The challenge-derived C proposal traverses the ordinary `devbridge/task-v1` parser with no coding-tool selection. Focused tests pass 21/21, preflight passes 102 syntax files + 2 JSON files + 98 targeted test files, and the complete suite passes 1,534 with 15 platform skips and 0 failures. Publish this no-elevation checkpoint; physical guest proof remains pending and no UAC may be requested for three days.

Windows-media setup reassessment 2026-08-28: `docs/testing/DB-HO045-issue-198-windows-media-setup-intake.md` records the missing public seam between the existing Windows media inspector/authority and setup. Implement bounded local discovery through opaque source references, a path-free inventory candidate, exact re-observation at explicit approval, immutable catalog registration, and fixed official acquisition handoffs when no source exists. Do not silently download or select Evaluation, do not persist source paths in generic profile/route policy, and do not let Windows media absence block the independently usable Linux profile. This checkpoint must remain entirely non-elevated.

Windows-media setup implementation checkpoint 2026-08-28: the public setup seam now discovers bounded real ISO sources, exposes path-free inventory choices, requires a separate exact candidate/image/source-class approval, re-observes before immutable catalog registration, and keeps accepted source resolution observational. Stale accepted media is reconciled back to selection without deleting catalog evidence. Remote setup status exposes counts and bounded accepted-image facts but no local path/source/candidate/authority/media identity. Preflight passes 107 syntax files + 2 JSON files + 101 targeted test files; the complete suite passes 1,568 total / 1,553 passed / 15 platform skips / zero failures. Publish this non-elevated checkpoint and update issue #198; physical media approval, Windows construction, and both-guest C proof remain pending, with no UAC request for three days.

Windows construction setup-observation reassessment 2026-08-28: `docs/testing/DB-HO046-issue-198-windows-construction-setup-observation.md` records the remaining manual-config seam between accepted media and the existing physical canary. Compose the exact setup-owned authority/resource config and expose only canary `status` beside the Windows media state. Keep blockers profile-local, do not widen Linux `--construct`, and add no Windows mutation/elevation surface during the no-UAC window.

Windows construction setup-observation implementation checkpoint 2026-08-28: accepted media now resolves observationally into one fixed Windows production authority/resource config, and public setup exposes the existing physical canary status without any `run` path. Windows preflight blockers remain profile-local; remote status removes source/candidate/authority/image/provider paths while retaining bounded readiness facts. Preflight passes 108 syntax files + 2 JSON files + 102 targeted test files; the complete suite passes 1,574 total / 1,559 passed / 15 platform skips / zero failures. Publish and update issue #198. The explicit multi-profile construction mutation policy, real media, UAC-gated provider action, and physical qualification remain pending.

## Phase 5 — complete permanent-entry and recovery composition

Owners: permanent entry, runner, accepted runtime, recovery control plane, and application manager.

1. Qualify a stable installed entry independent of a developer checkout.
2. Ensure stale runtime can reach the updater/recovery path without executing a candidate on the host.
3. Compose exact runner acquisition, signed/static recovery-control admission, candidate-validation environment reconstruction, DB-020 validation, accepted-runtime activation, LKG rollback, and service handoff.
4. Recreate declared execution profiles and workspaces only after accepted runtime authority is healthy.
5. Prove missing runtime + VM + image-cache recovery from retained durable local authority.

Exit evidence: one installed command recovers a configured installation without branch-specific/manual host surgery or candidate host execution.

## Phase 6 — Stage 7/8/9 qualification and product cleanup

1. Complete real Hyper-V and KVM/libvirt security, lineage, bridge, workspace, resource, cancellation, and recovery qualification (#115).
2. Complete guided setup/reconfiguration/repair/uninstall manifests for both host families (#116/#103).
3. Remove obsolete sandbox-era and repository-owned-VM migration scaffolding only after the new path is qualified (#117).
4. Resolve touched oversized parent LEGOs through the #244 nested-LEGO method where their reasoning seams are a demonstrated defect risk.
5. Close fixed issues; update partially satisfied issues with exact remaining evidence.

Cleanup rules:

- clean only exact DevBridge-owned provider objects after lifecycle re-observation;
- remove worktrees/branches only after their commits are merged, superseded, or durably preserved;
- never discard the operator's dirty fast-track config;
- remove stale remote development branches after confirming no open PR, unique unmerged commit, or current handoff depends on them;
- keep historical checksum-bound handoffs as evidence, not live authority.

## Phase 7 — working end-to-end DevBridge

After the lower gates are green:

1. configure approved task authors and repositories independently;
2. poll all configured repositories under the existing serialized admission/rate-budget contracts;
3. keep coding-model use opt-in only;
4. route UCI Arena work into an admitted compatible VM profile/workspace;
5. transfer source/candidate through the bounded bridge;
6. compile/test inside the guest;
7. import, verify, seal, and publish through host-authoritative Git;
8. prove daemon restart, pause, lease loss, baseline drift, and publication recovery.

Exit evidence: a real UCI Arena task completes through the supported installed entry without direct-host repository execution, manual VM surgery, or coding-model default fallback.

### Minimal non-CUDA functional acceptance

The first product-level vertical slice deliberately uses trivial application logic so the evidence measures DevBridge rather than the fixture:

1. Admit one fixed C source from a trusted remote task and bind it to the exact repository revision, route, profile, image, environment, workspace, bridge, and locally registered tool operations.
2. Transfer that source through the bounded bridge to one native Windows guest and one Linux guest.
3. Compile and execute inside each exact guest, never through a host compiler/process or coding-model fallback.
4. Return exit code zero, one exact bounded run-specific stdout challenge, compiler evidence, and executable digest through the result contract.
5. Keep GitHub credentials, provider-management authority, authoritative Git, and publication state host-only; a missing compiler, route, environment, bridge, or provider fails closed.
6. Repeat after normal reuse and daemon/service restart to prove persistent-environment and recovery behavior.

Passing both guest profiles on a Windows Hyper-V host establishes this minimal product slice on that host. It does not establish Linux-host provider equivalence: Stage 7 still requires the corresponding real KVM/libvirt route, security, storage-lineage, cancellation, and recovery evidence before cross-platform provider readiness is claimed.

### Baseline guest toolchain qualification

The C vertical slice proves the DevBridge pipeline; it is not the complete prepared-profile usability claim. Each supported Windows and Linux guest profile should also advertise and smoke-test a bounded baseline matrix:

- Node.js plus its standard package client, including the TypeScript path when present;
- Python plus its standard package/environment path;
- native C and C++ compiler/linker support;
- Rust plus Cargo;
- .NET SDK and C#;
- a JDK and the ordinary Java build path;
- Go;
- Git, CMake, Ninja and/or Make as appropriate to the guest; and
- the native noninteractive shell surface (PowerShell on Windows and POSIX shell tooling on Linux).

Qualification means exact in-guest inventory plus one tiny deterministic compile-or-run smoke for every capability the profile claims. It does not mean exhaustive language conformance testing, host-side compiler discovery, or a separate DevBridge adapter for each package ecosystem. Additional ecosystems such as Ruby or PHP remain demand-driven unless a declared profile or admitted repository requirement makes them part of the local contract. Tool additions still require pinned/acquired guest authority, bounded evidence, and no host-execution fallback.

## Deferred GPU program

Issues #162, #186, and #283 remain deferred until Phases 0–7 establish recoverable, installable CPU profiles. No generic compute abstraction is built speculatively. Later GPU work begins with a real provider/guest feasibility canary and plugs into the same profile, lifecycle, bridge, evidence, and setup studs.

The operator has supplied a candidate path to preserve for that later research gate:

- keep repository-controlled GPU execution VM-only and fail closed when the exact hardware profile is unavailable;
- model GPU/device/driver requirements as host-owned execution-profile policy, never repository-granted authority;
- evaluate first-class PCIe passthrough or mediated/SR-IOV assignment, with the physical device and DMA boundary proved per provider rather than assumed;
- keep compilation and artifact production in the admitted persistent workspace VM, while evaluating a separate disposable execution worker for tests that require resettable device state;
- compare a direct-passthrough VM path with a separately bounded NVIDIA `nvproxy`/gVisor path; do not combine them into one implicit topology or claim equivalent isolation;
- evaluate a narrow host-controlled guest transport such as `virtio-vsock`, read-only execution roots, and ephemeral writable state without exposing host credentials, paths, provider objects, or Git authority;
- treat binaries and PTX/SPIR-V transferred between environments as untrusted candidate artifacts bound to exact source, toolchain, driver, device, and verification identity;
- require teardown/reset, IOMMU grouping, peer-device, DMA, driver, firmware, bus-I/O, and hostile-pointer/memory-copy qualification before admitting low-level GPU tests.

This is a roadmap hypothesis, not an accepted security claim. Later assessment must use current primary NVIDIA, Linux VFIO/IOMMU, KVM/QEMU/libvirt, Hyper-V GPU-assignment/GPU-P, gVisor, and virtio-vsock sources and must explicitly separate Linux and Windows provider capabilities. No GPU implementation or host fallback is authorized by recording it here.

## Verification order for every slice

1. static/preflight and contract-shape checks;
2. focused owner tests including normal/failure/restart/boundary cases;
3. architecture/LEGO isolation gates;
4. relevant Windows and Ubuntu full suites;
5. doctor/readiness checks;
6. real provider qualification only when the slice claims OS/hypervisor behavior;
7. exact diff/status/remote-state reconciliation;
8. implementation and issue evidence update.

Passing mocks prove composition, not Hyper-V/libvirt enforcement. Expensive still-valid evidence is reused only when its exact candidate/provider/image/environment/policy identity remains valid.

## No-elevation hosted-CI correction plan

Checkpoint 2026-08-28: PR #368 run `33202061303` proved the Windows serialization policy was active but exposed two deterministic cross-platform contract defects. `docs/testing/DB-HO056-issues-369-370-hosted-cross-platform-contracts.md` records the assessment, Microsoft/Node primary-source research, reassessment, and plan. Issue #369 owns Windows short/long filesystem-alias handling without weakening link/substitution checks. Issue #370 owns bounded read-only doctor reporting when the protected lifecycle authority is absent. Both are non-elevated prerequisites to meaningful hosted qualification of #290.

Implement only these neutral owner corrections, then re-run the exact Windows/Ubuntu workflow. Keep #290 open until the serialized hosted Windows suite passes; close #369/#370 only after their exact hosted acceptance evidence passes. No UAC, protected service, provider, image, environment, VM, guest, or repository-execution effect is permitted during the operator's three-day no-UAC interval.

Accepted checkpoint: the neutral filesystem-identity stud, exact setup consumption, canonical media expectations, typed lifecycle transport absence, and bounded doctor projection are complete. Hosted CI run `33204210686` on exact implementation commit `60182c978f4d97c9d03b631258cc617d35fc5252` passed Windows preflight with the real `RUNNER~1` alias, the complete serialized Windows suite, Windows doctor/installer checks, and both Ubuntu jobs including the absent-authority doctor path. This accepts #369/#370 and the separate #290 serialized-Windows gate. #367 remains open. Continue dependency-ordered non-elevated Stage-8 work; no UAC, protected service, provider, image, environment, VM, guest, or repository-execution effect is permitted during the operator's three-day no-UAC interval.

## No-elevation checkpoint: remote status authority

Checkpoint 2026-08-28: the parameter-free remote `setup.status` operation had been wired to the mutating setup coordinator. Its historical “read-only” label covered only the final physical canary call and concealed earlier path, prerequisite, authority, lifecycle, environment, and operational-configuration effects. This is now corrected on `stage8/362-protected-activity-channel`: runtime status consumes only immutable validated configuration facts plus the attached execution contract's read-only inspection, exposes no execution identity, and never claims unobserved construction readiness. The obsolete setup-runner connection is removed. Full evidence is recorded in `docs/testing/DB-HO047-setup-status-observation-boundary.md`.

This checkpoint is a prerequisite for profile-selection work: a remote status request must not become an implicit setup, profile change, UAC request, or construction action. Richer status will be added only through dedicated observation studs. No elevation or VM/provider mutation is permitted during the current three-day no-UAC interval.

## No-elevation checkpoint: execution-profile selection

Checkpoint 2026-08-28: `docs/testing/DB-HO048-issue-116-profile-selection.md` attaches a bounded `--profiles <linux|windows|both|none|defer>` choice to the existing restartable setup-authority record. The reusable resolver knows only opaque local profile identifiers and neutral choice mappings; the setup composition alone maps current Linux/Windows identities. Default and re-entry behavior preserve accepted intent, `defer` performs no selection-authority write, and an interrupted transaction is resumed only when its operation identity belongs to this component.

Setup now suppresses Windows media/status work unless Windows is selected and suppresses Ubuntu prerequisite/authority/construction work unless Linux is selected. Empty and deferred selections preserve repository setup and stop before either platform path. Cross-profile construction/media options fail before their adapters run. Windows-only setup reports its exact local readiness blocker and cannot claim operational readiness through the Linux activation path. This slice performs no provider, VM, image, lifecycle, guest, or elevation effect; full guided provisioning and physical qualification remain active under #116/#198.

Software evidence: focused selection/setup/CLI/architecture tests pass 69/69, repository preflight passes 112 syntax files + 2 JSON files + 105 targeted test files, and the complete repository suite passes 1,590 total / 1,575 passed / 15 platform skips / zero failures. Exact commit `6e4abb67ad0a71faee2dac570d49279378770331` is pushed, and issue #116 records the checkpoint.

## No-elevation checkpoint: serialized multi-profile construction

Checkpoint 2026-08-28: `docs/testing/DB-HO049-issue-198-multi-profile-construction-policy.md` generalizes the single public `--construct` option to advance the next incomplete accepted construction profile without introducing another platform-named flag. An import-free neutral selector receives only opaque identifiers and complete/blocked observations; the application composition alone supplies fixed Linux-then-Windows order. One invocation advances at most one frontier, and a blocked earlier target cannot be skipped.

The Windows production setup edge now has a closed `observe|advance` action and remains read-only by default. Explicit Windows advancement reuses the existing restartable physical canary. Setup projects the active profile in bounded status and handoff text, while empty/deferred selections, unavailable media/provider state, and malformed observations fail closed. No new provider, VM, media, activation, host-execution, or elevation mechanism was added.

Software evidence: the expanded focused set passes 79/79; repository preflight passes 113 syntax files + 2 JSON files + 106 targeted test files; the complete suite passes 1,598 total / 1,583 passed / 15 expected platform skips / zero failures. No installed setup, UAC request, provider mutation, VM operation, media approval, or guest execution was performed. Publish this checkpoint and update #198/#116; physical Windows construction and both-guest C acceptance remain pending until elevation is again available.

## No-elevation checkpoint: selected-profile environment activation

Checkpoint 2026-08-28: `docs/testing/DB-HO050-issue-116-selected-profile-environment-activation.md` removes the remaining Linux-specific assumptions from accepted environment declaration publication and setup activation. A neutral profile-source primitive selects or exactly retains image/bootstrap authority; isolated Ubuntu and Windows owners supply their own local specifications; the common publisher knows neither platform nor provider identities. Windows construction and declaration composition share one production-output identity.

Setup now requires declarations for every accepted profile, then invokes the existing protected lifecycle client through an import-free serial reconciler. Windows-only setup can reach protected activation after exact image completion without touching Ubuntu authority. Combined activation advances at most one changed profile per invocation in fixed Linux-then-Windows order, re-observes on re-entry, and never skips an earlier blocker. Public readiness language reports profile/environment counts without naming a current topology.

Software evidence: focused owner/boundary tests pass 55/55; repository preflight passes 118 syntax files + 2 JSON files + 109 targeted test files; the complete suite passes 1,614 total / 1,599 passed / 15 expected platform skips / zero failures; `git diff --check` passes. No installed setup, UAC request, provider mutation, VM operation, media approval, or guest execution was performed. Commit/push this checkpoint and keep #116/#198 open; real protected environment activation and both-guest C acceptance remain pending until elevation is available.

## No-elevation checkpoint: explicit Windows activation-policy selection

Checkpoint 2026-08-28: `docs/testing/DB-HO051-issue-199-windows-activation-policy-selection.md` closes the silent licensing-policy gap before protected Windows environment activation. Setup accepts only the explicit non-secret `--windows-activation later` choice. Its self-contained policy value is immutable and digest-addressed; the existing generic setup-authority record carries only the opaque subject through a component-owned restartable transaction. Missing, substituted, foreign, or invalid authority/policy state fails closed.

The new gate is enforced after exact image completion and before declaration publication, protected lifecycle/environment activation, or operational enablement. It does not impede Windows media/image progress or independent Linux image work. Public status and handoff report `configure later` and `activation required` without exposing the subject or accepting keys. Retail, MAK, KMS, Active Directory-based, subscription, host-license reuse, secret storage, guest commands, and actual activation remain unimplemented rather than hidden behind a compatibility path.

Recovery review found one shared-journal routing defect before the checkpoint was considered settled: profile selection originally rejected every foreign working generation, which could strand a later component's interrupted transaction. Ordinary no-choice re-entry now projects accepted state without touching foreign work so the owning downstream component can resume it; any new explicit choice remains blocked. The activation selector follows the same non-consuming rule for future downstream owners.

Software evidence: the focused setup/policy/CLI/LEGO suite passes 74/74; repository preflight passes 121 syntax files + 2 JSON files + 113 targeted test files; the complete suite passes 1,626 total / 1,611 passed / 15 expected platform skips / zero failures; `git diff --check` passes. No installed setup, UAC request, provider mutation, VM operation, guest command, media approval, or activation attempt occurred. Publish and update #199/#116, keeping both open. Continue dependency-ordered non-elevated Stage-8 work while protected physical activation remains deferred for three days.

## No-elevation reliability checkpoint: daemon control-record reads

Checkpoint 2026-08-28: full-suite qualification of shared setup-journal recovery exposed two consecutive Windows `EPERM` failures while opening different atomically published immutable daemon records. `docs/testing/DB-HO052-issue-366-daemon-control-transient-read.md` records the evidence, Microsoft/Node research, reassessment, bounded implementation, and verification. One topology-neutral primitive now retries only `EPERM` on a fixed 5/10/20/40/80 millisecond schedule; callers cannot widen the code, schedule, or attempt count. Daemon record parsers retain all absence, schema, PID, token, and ownership decisions.

The focused recovery/daemon set passes 27/27, repository preflight passes 122 syntax files + 2 JSON files + 115 targeted test files, and the complete suite passes 1,634 total / 1,619 passed / 15 expected platform skips / zero failures. This closes #366 if remote evidence reconciles. It introduced no UAC, provider, service, VM, guest, media, activation, or host repository-execution effect. Continue the non-elevated Stage-8 dependency path; protected activation remains queued until the operator's three-day no-UAC interval ends.

## No-elevation plan: local image-reconstruction policy

Assessment 2026-08-28: `docs/testing/DB-HO053-issue-200-local-reconstruction-policy.md` records the missing primitive beneath GitHub image publication. Setup has independent distribution authority state but no implementation or gate. Current GitHub API research confirms authenticated identity and existing-repository permissions are observations, not proof that repository creation or later Release upload will succeed; every mutation needs a separate durable effect and explicit approval.

Implement the complete source-neutral `local-reconstruction` policy first. Attach it through a profile-neutral reconciler and expose the exact Windows setup choice without adding an upload path. Enforce it only after image completion and before environment declaration/protected activation. Preserve construction/media identity in its existing owner and preserve the exact-digest-versus-new-generation rule. Remote repository proposal, rights confirmation, creation, draft release/asset publication, reacquisition, and boot qualification remain later #200 adapter boundaries. No UAC or external mutation is permitted in this slice.

Implementation checkpoint 2026-08-28: the exact `--windows-distribution local-reconstruction` choice is now immutable, digest-addressed, restartable, and profile-neutral below the setup composition. A profile-scoped operation owner cannot absorb another profile's interrupted transaction. The completed-image gate stops before conflict mutation, declaration publication, lifecycle authority, protected activation, or operational enablement, while Windows media/image work and independent Linux construction remain progressable. Status and handoff expose no subject, path, repository, Release, asset, credential, or upload capability.

Software evidence: focused policy/recovery/setup/CLI/LEGO tests pass 88/88; repository preflight passes 124 syntax files + 2 JSON files + 119 targeted test files; the complete suite passes 1,648 total / 1,633 passed / 15 expected platform skips / zero failures; `git diff --check` passes. Publish and update #200/#116, keeping both open. Continue with remote distribution discovery/effect design or another lower non-elevated Stage-8 prerequisite; do not request UAC during the operator's three-day interval.

## No-elevation plan: exact-execution fence reliability

Assessment 2026-08-28: `docs/testing/DB-HO054-issue-241-bridge-claim-observation.md` confirms a real guest-local publication race beneath the minimal C path. The exact-execution winner creates its exclusive monitor-claim path before asynchronously writing and closing the claim JSON. A concurrent caller can therefore observe `EEXIST` and then read incomplete bytes or encounter a transient Windows sharing/open failure. Claim disappearance between those observations is also a legitimate topology change that must return to exclusive acquisition, not become an invalid-claim error.

Implement a fixed bounded observation rule inside the isolated guest helper. Preserve `open('wx')` as the only ownership acquisition, retry only evidenced incomplete-publication read failures, fail closed after exhaustion, and retry exclusive acquisition when the observed claim disappears. Add deterministic delayed-publication and disappearance/reacquisition tests plus concurrent stress while preserving exactly one guest-side effect. This work must perform no setup, elevation, provider, VM, service, or guest-transport effect.

Implementation checkpoint 2026-08-28: the existing exclusive fence remains the only ownership acquisition. Claim observation now retries only partial JSON and, on Windows, `EPERM`/`EACCES`/`EBUSY` on a fixed 5/10/20/40/80/160 millisecond schedule; permanent malformed state fails closed after at most 315 milliseconds of waiting. A claim that disappears during observation returns to the second existing exclusive-create attempt and cannot become implicit ownership.

Software evidence: the focused bridge suite passes 16/16; controlled delayed-publication, disappearance/reacquisition, and permanent-malformation cases pass; ten extra stress repetitions pass 240 concurrent execute calls across 60 exact operations with one side effect per operation; repository preflight passes 124 syntax files + 2 JSON files + 119 targeted test files; and the complete suite passes 1,651 total / 1,636 passed / 15 expected platform skips / zero failures. Publish and close #241; update but do not close the broader #290 Windows process-pressure issue. No UAC or protected operation occurred.

## No-elevation plan: deterministic Windows CI concurrency

Assessment 2026-08-28: `docs/testing/DB-HO055-issue-290-windows-ci-concurrency.md` records the repeated hosted-Windows full-suite pressure failures, exact Node/GitHub runner behavior, and current timing. Node 22.16 defaults the process-isolated CLI runner to `availableParallelism() - 1` concurrent test files; this public repository's standard Windows runner exposes four processors, while 26 DevBridge test files contain PowerShell-related behavior that can spawn additional children. The current full Windows suite passes with test-file concurrency one in 214.7 seconds, inside the existing six-minute step and ten-minute job bounds.

Make only the Windows full-suite CI composition serial. Preserve the unchanged default suite on non-Windows, every test assertion/file, the real PowerShell checks, all product timeouts, smoke gates, architecture gates, and existing CI deadlines. Add a static workflow contract and require a clean hosted Windows run before closing #290. This is a bounded CI policy, not a cross-platform production setting or a claim that raw concurrency is a general resource scheduler.

Reassessment during qualification: a default-concurrency run exposed that the bridge observation fixture released an exited child PID before launching its observer. Windows reused that PID, so the fixture's “dead” identity named the live observer and produced `running`. Replace the fixture with a deterministic non-observable numeric identity and narrow the test name. The underlying product limitation—durable monitor/child PIDs cannot distinguish a later process after OS reuse, including cancellation targeting—is separately recorded in #367 and is not solved or hidden by CI serialization.

Implementation checkpoint 2026-08-28: the workflow now runs the complete Windows suite with exact test-file concurrency one while non-Windows retains default concurrency; both keep the existing six-minute limit. A static workflow test fixes that composition contract. The released-PID bridge fixture now tests only a deterministic non-observable numeric identity, while #367 durably owns reusable process identity and cancellation correctness.

Local evidence: focused workflow/observation contracts pass 4/4; exact npm concurrency forwarding passes 3/3; the pre-change full serial measurement passes 1,651 total / 1,636 passed / 15 expected skips / zero failures in 214.7 seconds; repository preflight passes 124 syntax + 2 JSON + 119 targeted files; and the post-containment default full suite passes 1,652 total / 1,637 passed / 15 expected skips / zero failures in 57.4 seconds. Hosted CI run `33204210686` on exact implementation commit `60182c978f4d97c9d03b631258cc617d35fc5252` passed the complete serialized Windows suite in 2 minutes 14 seconds plus Windows smoke and both Ubuntu jobs. Close #290; keep #367 open. No UAC or protected operation occurred.

## No-elevation plan: reusable execution identity

Assessment 2026-08-28: `docs/testing/DB-HO057-issue-367-operation-activity.md` records that durable monitor/child process identifiers are reusable locators, not operation identity. They can make a lost operation appear live and can direct cancellation at an unrelated later process. Microsoft and Node primary sources confirm both PID reuse and Node's explicit signaling hazard. A platform-specific creation-time probe would leak OS process objects into a neutral guest contract and would not repair split cancellation ownership.

Replace the claim/PID design with a permanent request-owned exclusive attempt fence and a replaceable token-bound activity heartbeat. Fence presence blocks replay forever; only a planned operation with no fence may start another monitor. Observation requires the exact durable activity token and a fresh, strictly validated heartbeat. Cancellation writes only an exact request-bound message; the winning monitor terminates only its still-live in-memory child. Remove all persisted PIDs, claim reclamation, and compatibility parsing, and advance the internal operation record directly to version 2.

Add the isolated neutral helper to both guest payloads and test concurrent exact effects, partial fence publication, stale/mismatched activity, cancellation, timeout, restart, fast completion, version-1 rejection, image membership, and LEGO isolation. Qualify locally and in hosted Windows/Ubuntu CI before closing #367. This work performs no setup, UAC, service, provider, VM, image-construction, guest-transport, or repository-execution effect; all protected activation remains deferred during the operator's three-day no-UAC interval.

Implementation checkpoint 2026-08-28: the guest operation journal is now version 2 and contains no persisted process locators. The isolated activity store owns a permanent exclusive attempt fence and exact UUID-bound heartbeat; observation has one fixed 315-millisecond cross-file settle window but never deletes/reclaims the fence, probes a process, accepts a substituted token, or grants replay. Cancellation is only a strict request-bound message. The winning activity process alone may terminate its still-live in-memory child for cancellation or timeout. Version-1 records and injected PID fields fail closed with no compatibility path.

Local evidence: focused activity/bridge/payload/LEGO tests pass 35/35; ten consecutive 16-operation fast-child stress runs pass; repository preflight passes 126 syntax files + 2 JSON files + 124 targeted test files; the complete suite passes 1,667 total / 1,652 passed / 15 expected platform skips / zero failures; `git diff --check` passes. Commit and push the isolated branch, then require the exact hosted Windows/Ubuntu run before closing #367. No UAC or protected operation occurred.

Accepted checkpoint: hosted CI run `33206256810` passed on exact implementation commit `ef370babedae610ad59a09864131326f57fa9d7a`: Windows full + doctor in 2 minutes 59 seconds, Windows smoke/preflight/identity/installer in 1 minute 7 seconds, Ubuntu full + doctor in 40 seconds, and Ubuntu smoke in 19 seconds. Close #367. Continue the remaining dependency-ordered non-elevated Stage-8 work; protected activation and real-VM acceptance remain deferred during the operator's three-day no-UAC interval.

## No-elevation plan: nested guest bridge internals

Assessment 2026-08-28: issue #246 is the lowest remaining architecture dependency exposed by #367. `docs/testing/DB-HO058-issue-246-guest-bridge-nested-legos.md` records the owner audit and Node primary-source research. Keep one external bridge endpoint, the parent protocol/dispatch adapter, and authoritative logical-location containment. Extract only the two remaining independently changeable effect domains: local process lifecycle and chunked transfer state. Exact-attempt/activity evidence is already isolated.

The local-process owner receives only normalized local execution values plus `pulse`, `readStop`, and `writeStop` ports. The transfer owner receives only opaque identity/binding values plus neutral read/write location resolvers. Neither may name the other, a provider, repository, controller, transport, or external topology. Move code completely and delete it from the parent; add both helpers to both guest payloads, direct owner tests, parent regression tests, LEGO isolation, preflight, full-suite, and hosted cross-platform gates. No UAC or protected operation is permitted.

Implementation checkpoint 2026-08-28: `bridge-agent.mjs` remains the one external endpoint and now composes three isolated local effect owners. Existing `activity-store.mjs` retains exact-attempt/activity/cancellation state. New `local-process.mjs` owns only the live child lifecycle through neutral pulse/stop ports. New `transfer-channel.mjs` owns only exact byte-transfer state through neutral location ports. The moved code is deleted from the parent; both guest payloads carry the new exact members. The public bridge protocol and result shapes are unchanged, while differently shaped old internal transfer records fail closed without compatibility code.

Local evidence: focused nested-owner and parent tests pass 43/43; ten additional fast-child observation repetitions pass; repository preflight passes 128 syntax files + 2 JSON files + 126 targeted test files; the complete suite passes 1,678 total / 1,663 passed / 15 expected platform skips / zero failures; and `git diff --check` passes. Publish the exact checkpoint and require hosted Windows/Ubuntu qualification before closing #246. The separate Windows daemon-control-file `EPERM` recurrence from documentation-only run `33206528459` is recorded on still-open #261. No UAC or protected operation occurred.

Hosted reassessment: run `33207765083` passed both Ubuntu jobs but its Windows preflight and serialized suite exposed a deterministic short/long path-alias mismatch in the new transfer owner's final containment check. Canonicalize the neutral supplied boundary and actual file/parent before comparison while retaining the lexical escape check. Re-run focused, preflight, complete local, and exact hosted gates; do not accept or close #246 from the failing commit.

Accepted checkpoint: hosted CI run `33208194846` passed all four Windows/Ubuntu jobs on exact correction commit `9e0f26b01f7bfe75a17a3cac0904c151fd34947a`, including the Windows short/long path spelling exercised by both preflight and the serialized complete suite. Close #246. Continue with the primitive daemon control-record reliability defect #261 before higher-level Stage-8 work; no UAC or protected operation is permitted.

## No-elevation plan: daemon control-record observation horizon

Assessment 2026-08-28: `docs/testing/DB-HO059-issue-261-daemon-control-observation-horizon.md` records a post-#366 hosted Windows `EPERM` after the current 155-millisecond read horizon was exhausted. The exact immutable pause request was neither absent nor concurrently removed. Microsoft and Node primary sources retain the same access-sharing/transient classification; no evidence supports widening the retry code set or weakening token/schema ownership.

Extend only the import-free reader's fixed exponential schedule with 160, 320, and 640 milliseconds, for at most 1,275 milliseconds of waiting inside the existing 10/15-second governance deadlines. Keep success immediate, exact final-error identity, and all non-`EPERM` behavior. Qualify focused/repeated, preflight, complete local, and multiple hosted Windows/Ubuntu runs before closing #261. No UAC or protected operation is permitted.

Implementation checkpoint 2026-08-28: the topology-neutral reader now uses the exact fixed `[5, 10, 20, 40, 80, 160, 320, 640]` schedule. No caller can alter its code class or horizon. Focused owner/boundary tests pass 19/19; ten additional daemon-governance repetitions pass; repository preflight passes 128 syntax files + 2 JSON files + 126 targeted test files; the complete suite passes 1,678 total / 1,663 passed / 15 expected platform skips / zero failures; and `git diff --check` passes. Publish and require multiple exact hosted runs before closing #261. No UAC or protected operation occurred.

Accepted checkpoint: GitHub Actions run `33208808793` passed three consecutive attempts on exact implementation commit `d84ac2c35ab07e1eb5245b9ff723f1d25ca6f40e`; every attempt passed Windows full + doctor, Windows preflight/identity/installer, Ubuntu full + doctor, and Ubuntu smoke. Close #261. Continue the next primitive non-elevated dependency; protected activation remains deferred during the operator's three-day no-UAC interval.

## No-elevation plan: nested persistent-environment internals

Assessment 2026-08-28: issue #245 is the lowest remaining parent-LEGO dependency in the VM lifecycle path. `docs/testing/DB-HO060-issue-245-persistent-environment-nested-legos.md` records the DB-020/profile-ownership audit, exact implementation/test inventory, Node filesystem research, reassessment, and scoped plan. Keep `PersistentEnvironments` as the sole caller-facing lifecycle authority and preserve the exact v1 catalog, attachment/source/generation identities, history, recovery, and provider semantics.

Extract complete durable-ledger, normalized-effect, provisioning, ordinary-lifecycle, generation-change, and retirement responsibilities. The parent alone owns their topology; children import no sibling and communicate only through neutral local ports. Delete moved code from the parent, add direct child and isolation tests, retain all parent recovery tests, and require local plus hosted Windows/Ubuntu qualification before closing #245. No setup, UAC request, service, provider, image, environment, VM, guest, or repository-execution effect is permitted during the operator's three-day no-UAC interval.

Implementation checkpoint 2026-08-28: the public parent is now a 286-line validation/composition/reconciliation surface around six closed nested owners: durable ledger, normalized effect channel, provisioning, ordinary lifecycle, generation change, and retirement. Only the parent imports children; children import no sibling or external topology. Moved code was deleted, while the exact public exports, v1 catalog shape, attachment/source/generation identities, history, retention, and recovery rules remain unchanged.

Focused child/parent/LEGO tests pass 34/34; ten additional persistent recovery/concurrency repetitions pass; repository preflight passes 135 syntax files + 2 JSON files + 133 targeted test files; the complete suite passes 1,686 total / 1,671 passed / 15 expected platform skips / zero failures; topology scan and `git diff --check` pass. Commit and push the exact checkpoint, then require hosted Windows/Ubuntu qualification before closing #245. No UAC or protected operation occurred.

Accepted checkpoint: GitHub Actions run `33211055066` passed Windows serialized full-suite/doctor, Windows preflight/identity/installer, Ubuntu full-suite/doctor, and Ubuntu preflight/identity/installer on exact implementation commit `42cd7543db764250dd83cb8222e137159eec1f47`. Close #245. Continue the next primitive nested-LEGO dependency under #244; protected activation and physical VM acceptance remain deferred throughout the operator's three-day no-UAC interval.

## No-elevation plan: nested run-coordinator internals

Assessment 2026-08-28: issue #247 is the next parent-LEGO dependency under #244. `docs/testing/DB-HO061-issue-247-run-coordinator-nested-legos.md` records the durable-run ownership audit, relevant recovery and publication tests, Node timer research, reassessment, and scoped plan. Keep `RunCoordinator` as the sole caller-facing authority for stage transitions, save/report ordering, workspace and execution effects, exact publication, lease fencing, and terminal acceptance.

Extract only bounded projection, retry-window, feedback-interpretation, candidate-recovery, and finalization-policy mechanics. The parent alone composes children; children import no sibling, learn no current topology, perform no external effect, and cannot persist, publish, seal, push, or declare a run stage accepted. Delete moved code, add direct owner and source-isolation tests, retain whole-run recovery tests, and require local plus hosted Windows/Ubuntu qualification before closing #247. No setup, UAC request, service, provider, image, environment, VM, guest, or repository-execution effect is permitted during the operator's three-day no-UAC interval.

Implementation checkpoint 2026-08-28: `RunCoordinator` remains the sole stage, persistence/reporting-order, workspace/effect, publication, fence, and terminal authority. Five import-free nested owners now contain bounded projection, retry timing, feedback interpretation, candidate recovery calculations, and finalization policy. Only the parent composes them and supplies current provenance labels. Children cannot assign a run stage or reach persistence, status, workspace, execution, seal, publication-effect, controller, model, provider, platform, or remote topology. Moved code was deleted with no legacy wrapper or alternate state machine.

Focused child/parent/recovery/gate tests pass 46/46; ten repeated recovery/retry runs pass; repository preflight passes 140 syntax files + 2 JSON files + 134 targeted test files; the complete suite passes 1,694 total / 1,679 passed / 15 expected skips / zero failures; and `git diff --check` passes. Commit and push the exact checkpoint, then require hosted Windows/Ubuntu qualification before closing #247. No UAC or protected operation occurred.

Accepted checkpoint: GitHub Actions run `33212636105` qualified exact implementation commit `9cf4155df5887d8df1120dbf13b43a110ab7c420`: Windows serialized complete suite/doctor, Ubuntu complete suite/doctor, Ubuntu smoke, and unchanged attempt-2 Windows smoke all passed. Attempt 1 exposed a parallel Windows preflight CMake-probe pressure failure even though the serialized full suite passed the same test on the same SHA. Reopen #290 with that exact failure/rerun evidence; do not widen the product probe timeout. Close #247 and continue the dependency-ordered non-elevated path under #244.

## No-elevation plan: nested repository-execution internals

Assessment 2026-08-28: issue #248 is the next parent-LEGO dependency under #244. `docs/testing/DB-HO062-issue-248-repository-execution-nested-legos.md` records the Stage-6/DB-020 ownership audit, current implementation and test inventory, Node exclusive-create/file-handle/byte-copy research, reassessment, and scoped plan. Keep `createRepositoryExecution` as the sole production composition edge and preserve the public execution protocol, neutral sequencing, route policy, portable tree validation, VM-only fail-closed behavior, and current durable/evidence identities.

Extract complete route-access, session-guard, bounded-byte, operation-materialization, and source/candidate-session responsibilities. Only the parent composes them and supplies current policy/activity/helper/file-tree topology. Children import no local implementation, name no sibling or external identity, and cannot create activity/provider authority, execute a host fallback, or bypass candidate staging and cancellation checks. Delete moved code, add direct owner and source-isolation tests, retain every Stage-6 parent/integration proof, and require local plus hosted Windows/Ubuntu qualification before closing #248. No setup, UAC request, service, provider, image, environment, VM, guest, or repository-execution effect is permitted during the operator's three-day no-UAC interval.

Implementation checkpoint 2026-08-28: `createRepositoryExecution` remains the sole production topology/public-adapter edge around five closed nested owners for route access, exact session exclusion, bounded bytes, operation materialization, and source/candidate session sequencing. Only the parent supplies current policy/activity/helper/file-tree identities and context-specific diagnostics. Children import no local implementation or sibling and name no external topology; moved code was deleted without compatibility wrappers or alternate execution paths.

Focused child/parent/contract/Stage-6 tests pass 30/30; the broader Stage-6 set passes 60 total / 58 passed / 2 expected Windows skips / zero failures; ten nested-owner and five real exclusive-session repetitions pass; repository preflight passes 145 syntax files + 2 JSON files + 135 targeted test files; the complete suite passes 1,701 total / 1,686 passed / 15 expected platform skips / zero failures; topology scan and `git diff --check` pass. Commit and push the exact checkpoint, then require hosted Windows/Ubuntu qualification before closing #248. No UAC or protected operation occurred.

Accepted checkpoint: GitHub Actions run `33214619626` passed Windows serialized complete-suite/doctor, Windows preflight/identity/installer, Ubuntu complete-suite/doctor, and Ubuntu preflight/identity/installer on exact implementation commit `b68078dfcae35526e143c2e8ce0146c08568fd41`. Close #248 and continue the dependency-ordered non-elevated path under #244. Protected activation and physical dual-guest acceptance remain deferred during the operator's three-day no-UAC interval.

## No-elevation plan: nested Git workspace internals

Assessment 2026-08-28: issue #249 is the next parent-LEGO dependency under #244. `docs/testing/DB-HO063-issue-249-git-workspace-nested-legos.md` records the DB-003/008/009/017/020 authority audit, complete 614-line implementation inventory, focused-test families, official Git worktree/diff/rebase/push research, reassessment, and scoped plan. Keep `GitWorkspaceManager` as the sole caller-facing authoritative Git/workspace surface and preserve all exact start/publication/candidate/remote predecessor identities and recovery semantics.

Extract complete admission, baseline, worktree lifecycle, observation/validation, candidate sealing, baseline reconciliation, and publication transaction responsibilities. Only the parent composes them and supplies current repository/path/remote/token/channel/branch policy and context-specific diagnostics/errors. Children import no local implementation or sibling, expose no raw command authority, and cannot reset unowned state, weaken rebase restoration, select publication targets, or broaden explicit force-with-lease CAS. Delete moved code, add direct owner and source-isolation tests, retain all parent/coordinator proofs, and require local plus hosted Windows/Ubuntu qualification before closing #249. No setup, UAC request, service, provider, image, environment, VM, guest, repository-execution, or real publication effect is permitted during the operator's three-day no-UAC interval.

Implementation checkpoint 2026-08-28: `GitWorkspaceManager` remains the sole public and authoritative Git/workspace surface around seven closed nested owners for admission, baseline authority, worktree lifecycle, observation/validation, candidate sealing, baseline reconciliation, and publication transaction. Only the parent supplies repository/path/remote/token/channel/branch policy, product-specific diagnostics/errors, and current topology. Children import no local implementation or sibling and name no external topology; moved code was deleted without wrappers or alternate Git authority.

Direct child plus retained parent proofs pass 29/29; the wider baseline/candidate/gate/lease/coordinator/publication recovery set passes 70/70; three repeated real-Git seal/rebase/publication runs pass 15/15 each; repository preflight passes 153 syntax files + 2 JSON files + 136 targeted test files in 37.21 seconds; the complete suite passes 1,709 total / 1,694 passed / 15 expected platform skips / zero failures; topology scan and `git diff --check` pass. Commit and push the exact checkpoint, then require hosted Windows/Ubuntu qualification before closing #249. No real remote mutation, UAC, or protected operation occurred.

Hosted attempt 1 (`33216113376`, commit `1d452f50c204fb2bc0a51fa742cfea1f2614a848`) passed Windows serialized complete-suite/doctor and both Ubuntu jobs but timed out Windows smoke at the fixed one-minute cheap-preflight boundary. Cause: five expensive real-Git parent suites were newly registered in cheap preflight even though full CI already owns them. Keep the fast nested contract in preflight, retain all real-Git suites in the complete run, and do not widen the product/job timeout. Requalify the corrected exact commit.

Accepted checkpoint: GitHub Actions run `33216379948` passed Windows serialized complete-suite/doctor, Windows bounded preflight/identity/installer, Ubuntu complete-suite/doctor, and Ubuntu bounded preflight/identity/installer on exact corrected commit `45d75c4f81c24d0e582bb6d57747ac50181a0f0d`. Close #249 and continue the dependency-ordered non-elevated path under #244. Protected activation and physical dual-guest acceptance remain deferred throughout the operator's three-day no-UAC interval.

## No-elevation plan: provider-local nested internals

Assessment 2026-08-28: issue #250 is the next dependency under #244. `docs/testing/DB-HO064-issue-250-provider-local-nested-legos.md` records the DB-020 provider-ownership audit, complete Hyper-V persistent/libvirt persistent/Hyper-V construction implementation and test inventory, Microsoft Hyper-V/VHDX, QEMU qcow2, and libvirt lifecycle/domain research, reassessment, and scoped plan.

Keep three independent provider-owned parents. Extract provider-local contract, durable-state, management, storage, observation, liveness, and evidence mechanics according to each platform's actual seams. Do not create a shared Hyper-V/libvirt helper or force symmetrical shapes. Only each parent composes its children and owns public topology, durable phase/lifecycle sequencing, and local destructive authority. Delete moved code, add direct child and provider-isolation tests, retain parent recovery behavior, and require local plus hosted Windows/Ubuntu qualification before closing #250. No UAC request, elevation attempt/bypass, service, provider, image, environment, physical VM, guest, or repository-execution effect is permitted through at least 2026-08-31.

Implementation checkpoint 2026-08-28: the Hyper-V persistent, libvirt persistent, and Hyper-V image-construction parents now compose separate provider-local trees. Children own bounded contract/identity, ledger, native management, native storage lineage, media, observation, liveness, or console-evidence responsibilities as appropriate to their provider; no child imports a sibling or the other provider, and only the parent advances lifecycle or construction phases. Moved code was deleted without a compatibility path or shared cross-provider helper. The extracted Hyper-V provider command programs are byte-for-byte unchanged.

Direct nested/parent/boundary tests pass 30/30; dependent physical-canary/environment-foundation tests pass 36/36; five repeated provider recovery runs pass 26/26 each; repository preflight passes 168 syntax files + 2 JSON files + 137 targeted test files; the complete suite passes 1,713 total / 1,698 passed / 15 expected platform skips / zero failures; and exact command comparison plus `git diff --check` pass. Commit and push the exact checkpoint, then require hosted Windows/Ubuntu qualification before closing #250. No UAC, elevation attempt/bypass, protected operation, or physical provider/VM/guest effect occurred.

Accepted checkpoint: GitHub Actions run `33217939931` passed Windows serialized complete-suite/doctor, Windows bounded preflight/identity/installer, Ubuntu complete-suite/doctor, and Ubuntu bounded preflight/identity/installer on exact implementation commit `03aa340d3ae5e130bdaa0ff1e40c269ad6cb2193`. Close #250 and continue the next dependency-ordered non-elevated issue under #244. Protected activation and physical dual-guest acceptance remain deferred throughout the no-UAC interval.

## No-elevation plan: nested chat-handoff internals

Assessment 2026-08-28: issue #251 is the next dependency under #244. `docs/testing/DB-HO065-issue-251-chat-handoff-nested-legos.md` records the DB-005/009/014 ownership audit, complete 395-line parent and persistence-adapter inventory, focused 19/19 baseline, Node UTF-8/digest research, ECMAScript JSON serialization research, reassessment, and scoped plan.

Keep `src/context/chat-handoff.js` as the only caller-facing parent and preserve all public exports, three durable protocols, canonical byte/digest identity, resume results, planned/ready/pointer ordering, compare-and-swap, fallback, and history semantics. Extract complete immutable-value, durable-record, current/previous pointer, retention, and store-transaction owners. Only the parent composes them and adapts the concrete state store to neutral read/write/list/remove ports. Children import no local implementation or sibling and name no external topology. Delete moved code without a compatibility implementation, add direct contract/order/isolation tests, and require local plus hosted Windows/Ubuntu qualification before closing #251. No UAC request, elevation attempt/bypass, protected operation, physical provider/VM/guest action, repository execution, remote handoff projection, or real publication effect is permitted through at least 2026-08-31.

Implementation checkpoint 2026-08-28: the chat-handoff parent is now a 109-line public validation/composition facade around five closed nested owners for immutable value/digest/resume behavior, durable record verification, current/previous pointer values, neutral retention ranking, and durable transaction sequencing. Only the parent maps semantic fields and concrete persistence methods to neutral ports. Children import no local implementation or sibling, persistence children name no external topology, and moved code was deleted without compatibility records, alternate pointers, or a second store path.

Direct nested/parent/integration tests pass 24/24; 50 varied values match the pre-extraction normalization/canonicalization/digest/seed behavior exactly; the frozen representative digest remains `33e360374592f4a01c7ead2d1137319837456bba6c3582d7d3fa6662fa9134ab`; ten repeated recovery runs pass 15/15 each; corrected repository preflight passes 168 directly checked syntax files + 2 JSON files + 138 targeted test files while the targeted nested test imports/verifies every child; the complete suite passes 1,718 total / 1,703 passed / 15 expected platform skips / zero failures; and topology gates plus `git diff --check` pass. Commit and push the exact checkpoint, then require hosted Windows/Ubuntu qualification before closing #251. No UAC, protected operation, physical provider/VM/guest action, repository execution, remote handoff projection, or product publication occurred.

Hosted CI run `33219166402` attempts 1 and 2 passed Windows serialized full-suite/doctor and both Ubuntu jobs but reached the fixed one-minute Windows cheap-preflight boundary without a product/test failure. This reproduces open #290. Five new child files were redundantly registered as individual Windows `node --check` processes even though the registered parent and targeted nested contract already import every child. Remove only those five duplicate process launches, retain all tests and the fixed CI deadline, and requalify. Corrected local preflight completes in 38.28 seconds; do not widen product or CI timeouts.

Accepted checkpoint: GitHub Actions run `33219513699` qualified exact correction commit `fb4d2650921e7e17fb9821988c74e061e64a1224`. Windows bounded preflight/identity/installer passed within the unchanged one-minute cheap-preflight budget, both Ubuntu jobs passed, and a failed-job-only rerun passed the complete serialized Windows suite plus doctor. Attempt 1's sole failure was the exact existing #290 20-second hosted-runner recurrence in `Windows Hyper-V construction reconciles only the exact default-adapter New-VM partial effect`; it passed on rerun without widening any product/test/CI timeout. Close #251 and continue with #252 under the no-UAC boundary.

## No-elevation plan: nested setup-authority internals

Assessment 2026-08-28: issue #252 is the next dependency under #244. `docs/testing/DB-HO066-issue-252-setup-authority-nested-legos.md` records the DB-003/009/011/020 and setup ownership audit, current 445-line implementation plus caller/state-store inventory, focused 24/24 baseline, ECMAScript immutability/async research, Node operation-identity research, reassessment, and scoped plan.

Keep `src/runtime/setup-authority.js` as the sole public accepted-authority surface and preserve all exports, three v1 protocols, closed sets, strict immutable values, template sanitization/revalidation, accepted/working generation rules, operation/base-revision checks, and current caller diagnostics. Extract complete value, snapshot, blocker-evaluation, durable-record, sanitized-template, and transaction-manager owners. Only the parent composes them; children import no sibling or local implementation and name no external topology. Delete moved code without compatibility paths or a second manager.

The audit separately proved a pre-existing multi-manager durability defect: 49/50 concurrent trials returned two different owned working generations and one trial hit a colliding temporary-file rename. Issue #371 owns installation-wide setup-authority serialization/CAS and collision-proof publication. Do not conceal that semantic correction inside #252 or claim concurrent transaction safety from this structural extraction.

Add direct child/topology tests, retain parent/state/profile/distribution/activation recovery proofs, and require local plus hosted Windows/Ubuntu qualification before closing #252. No setup, UAC/elevation attempt/bypass, protected service/provider/image/environment/VM/guest operation, repository execution, remote setup projection, or product publication is permitted through at least 2026-08-31.

Implementation checkpoint 2026-08-28: the setup-authority parent is now a 112-line public constants/composition/re-export surface around six import-free nested owners for values, snapshots, blocker evaluation, durable records, sanitized templates, and the existing single-owner transaction sequence. Only the parent supplies current protocols/closed sets/default clock/operation identity and connects the children. Children import no sibling/local implementation and name no external topology; moved code was deleted without compatibility schemas, alternate records, or a second manager.

Public callable names/arities match the pre-extraction parent; 50 varied values match its JSON behavior exactly; focused child/parent/state/caller tests pass 30/30; ten repeated recovery runs pass; repository preflight passes 168 syntax files + 2 JSON files + 139 targeted test files; the complete suite passes 1,724 total / 1,709 passed / 15 expected skips / zero failures; and topology plus diff checks pass. #371 remains the explicit owner of the unchanged concurrent multi-manager storage defect. Commit and push the exact checkpoint, then require hosted Windows/Ubuntu qualification before closing #252. No UAC or protected operation occurred.

Accepted checkpoint: GitHub Actions run `33220876080` passed Windows serialized complete-suite/doctor, Windows bounded preflight/identity/installer, Ubuntu complete-suite/doctor, and Ubuntu bounded preflight/identity/installer on exact implementation commit `bd4754699a8d9855d771914cd3b9258c396b5671`. Close #252. Keep #371 open and schedule its setup-authority serialization/CAS correction before claiming concurrent setup reliability. Continue the dependency-ordered non-elevated path under #244; protected activation remains deferred through at least 2026-08-31.

## No-elevation plan: setup-authority mutation serialization

Assessment 2026-08-28: issue #371 is a primitive correctness dependency discovered during #252 and must precede further setup composition. `docs/testing/DB-HO067-issue-371-setup-authority-serialization.md` records the DB-009/011/020 ownership audit, exact 50-trial reproduction, state/manager/caller inventory, Node 22.16 IPC/filesystem/UUID research, reassessment, and scoped plan.

Replace the setup-authority persistence stud with one fresh, exclusive `load -> transform -> replace -> re-observe` operation. Compose two neutral import-isolated children under the state adapter: an OS-owned Windows-named-pipe/Linux-abstract-socket lease keyed only by a digest of canonical local target identity, and an exact JSON-record publication owner using exclusive UUID temporary files, file sync, replacement reconciliation, and exact cleanup. The transaction manager retains all authority semantics; the storage children learn no setup component, profile, provider, repository, VM, remote, or caller identity. Delete the separate load/save mutation path with no compatibility adapter.

Require same-process and real cross-process collision, interleaving, failure-release, crash-release, readback, restart, and caller-boundary proofs. Preserve all v1 protocols and envelope values. Qualify the exact implementation locally and on hosted Windows/Ubuntu before closing #371. No setup, UAC/elevation request or bypass, protected operation, physical VM/guest action, repository execution, or product publication is permitted through at least 2026-08-31.

Implementation checkpoint 2026-08-28: the setup-authority state adapter now exclusively composes a neutral OS-owned mutation lease and neutral exact JSON-record publisher. Every authority mutation is one fresh `load -> transform -> replace -> re-observe` operation. The old cached `JsonStateStore` load/save path was deleted without a compatibility port. Windows named pipes and Linux abstract sockets release with OS ownership; UUID temporary publication uses exclusive create, file sync, replacement reconciliation, exact byte readback, and exact-attempt cleanup. The three setup callers reject an observation-to-begin ownership race without leaking their identities into the transaction manager or storage children.

The exact serialization file passes 9/9 and ten repetitions; setup tests pass 137 total / 136 passed / 1 expected Windows symlink skip; repository preflight passes 168 syntax files + 2 JSON files + 141 targeted test files; the complete suite passes 1,735 total / 1,720 passed / 15 expected platform skips / zero failures; doctor passes and preserves VM-route fail-closed status; topology and diff checks pass. Commit and push the implementation, then require exact hosted Windows/Ubuntu qualification before closing #371. No UAC or protected operation occurred.

Accepted checkpoint: GitHub Actions run `33222140918` passed Windows serialized complete-suite/doctor, Windows bounded preflight/identity/installer, Ubuntu complete-suite/doctor, and Ubuntu bounded preflight/identity/installer on exact implementation commit `614c373f6c5fbe5950590126b61bf8dcf961e930`. The exact child-process race and killed-owner recovery therefore passed on both the Windows named-pipe and Linux abstract-socket implementations. Close #371 and continue the dependency-ordered non-elevated work with #253. Protected activation remains deferred through at least 2026-08-31.

## Live no-elevation operational frontier

Assessment 2026-08-28: `docs/testing/DB-HO068-issue-360-no-elevation-operational-frontier.md` records the exact ordinary-user audit of the installed DevBridge home and protected service. Hyper-V access, the running lifecycle service, accepted repository/profile state, and the completed Ubuntu image are present. The normal `config.json` is absent, while the protected read client reports zero declarations/environments and the activity endpoint correctly reports unavailable. The missing step is protected admission/reconciliation, not image construction or Hyper-V group access.

The currently installed generation cannot complete that protected intake without the one-shot elevated child, and its service protocol has no separate bounded setup/configuration endpoint. Do not manufacture apparent operation by publishing config alone, calling Hyper-V from the ordinary process, modifying the protected package/service, weakening ACLs, or reusing a broader endpoint. During the no-UAC interval, implement and qualify a distinct exact-record setup/configuration capability so an already-current service can perform routine reconfiguration under its protected identity. One administrator-authorized service refresh will still be required to install that generation; afterward ordinary setup, lifecycle, and execution should remain non-elevated. Physical Linux/Windows C acceptance remains unclaimed.

## Linux protected configuration continuation

Assessment 2026-08-29: issue #373 and `docs/testing/DB-HO069-issue-373-linux-configuration-channel.md` record the next primitive after the qualified cross-platform configuration protocol. Linux must not copy the Windows protected service's ability to reread ordinary state because the dedicated service identity must not gain the operator home/state tree. Keep the 16 KiB revision/digest request unchanged. Publish only the dedicated accepted profile state file through a descriptor-bound fixed handoff below the configuration authority's `/run` root, then let the protected service normalize and recheck that subject before and after locally derived effects.

Provision a distinct service-owned configuration socket directory and root-owned sticky/SGID handoff through the existing exact tmpfiles owner. Reuse the exact service/operator coordination principals without granting ordinary provider management; configuration remains a separate address, protocol, port, and source artifact. Add only the socket directory to `ReadWritePaths`. Compose the Linux protected configuration port from existing foundation/declaration/image-verification owners, attach it to the service host, generalize the ordinary profile client, and retain fail-closed absence. Qualify locally and on hosted Ubuntu/Windows without invoking setup, sudo/UAC, systemd, libvirt, a VM, a guest, or repository execution. Bounded Linux elevation, activity-policy handoff, protected activity attachment, and physical KVM/libvirt/qcow2/C evidence remain later gates.

Implementation checkpoint 2026-08-29: the descriptor-bound transfer owner, fixed Linux accepted-record handoff, separate configuration authority topology, service attachment, shared protected reconciler, and shared ordinary proxy are implemented. Only the endpoint directory is service-writable; the handoff is root-owned/sticky/SGID and read-only to the systemd namespace. Linux does not acquire an ordinary image-adoption path. Lifecycle health now requires both neutral endpoints. Moved Windows reconciliation/proxy logic was deleted rather than retained as a second implementation.

Focused channel/plan/topology/inspection/host/proxy/health and retained Windows suites pass. Repository preflight passes 178 syntax files + 2 JSON files + 149 targeted test files; the complete suite passes 1,772 total / 1,757 passed / 15 expected platform skips / zero failures; and `git diff --check` passes. Commit and push the exact checkpoint, then require hosted Windows/Ubuntu qualification. Keep #373 open until the remaining exact evidence is recorded. No setup, sudo/UAC, systemd, provider, image, VM, guest, or repository-execution effect occurred.

Hosted acceptance checkpoint: [GitHub Actions run 33226235422](https://github.com/iteathen/DevBridge/actions/runs/33226235422) passed Windows serialized complete-suite/doctor, Windows bounded preflight/identity/installer, Ubuntu complete-suite/doctor, and Ubuntu bounded preflight/identity/installer on exact implementation commit `abbdb31242d02ab2725094da7d44006bee6fa1ef`. Keep #373 open for real Linux tmpfiles/systemd endpoint policy and the later provider/elevation gates; hosted mechanics are not physical readiness.

## Linux protected activity continuation

Assessment 2026-08-29: issue #374 and `docs/testing/DB-HO070-issue-374-linux-protected-activity.md` record the next Stage-8 dependency after the Linux configuration channel. The dedicated Linux service must not traverse the ordinary state tree to load or publish `environment-activity/policy.json`. Keep the existing neutral activity wire contract and store the accepted route policy durably below service-owned protected state after exact workspace verification. Export only the normalized credential-free policy through a fixed descriptor-bound `/run` handoff for ordinary setup/runtime projection.

Provision a distinct service-owned activity endpoint and service-owned export through the existing tmpfiles/systemd owner. Add only the endpoint and export directories to `ReadWritePaths`. Attach activity as an independent optional port on the neutral authority host, inject one neutral route-state port into workspace construction and protected activity, and import the verified export before operational execution is enabled. Primary Node documentation confirms that clean close removes a pathname socket but a crash leaves it behind, so first add an exact-owned stale-socket preparation brick; it may remove only a real single-link socket with exact owner/group/mode below a service-owned non-group-writable directory.

Qualify the complete slice locally and on hosted Ubuntu/Windows without invoking setup, sudo/UAC, systemd, libvirt, Hyper-V, a VM, a guest, or repository execution. Real Linux endpoint/provider evidence and the installed Windows service refresh remain later physical gates.

Implementation checkpoint 2026-08-29: the neutral route-state seam, protected Linux durable policy/export, ordinary verified projection, exact activity topology, generic stale pathname-socket reconciliation, independent host activity attachment, protected service composition, activity health probe, and setup ordering are implemented. The service receives no ordinary-state traversal and the wire protocol receives no path, provider, command, credential, or policy-publication authority. Moved persistence calls were deleted from the workspace owner; no compatibility implementation or host-execution fallback was added.

Focused integration passes 117/117; repository preflight passes 183 syntax files + 2 JSON files + 154 targeted test files; the complete local suite passes 1,793 total / 1,777 passed / 16 expected platform skips / zero failures, with the real crash-retained pathname-socket proof intentionally deferred to hosted Ubuntu; and `git diff --check` passes. Commit and push the exact checkpoint, require hosted Windows/Ubuntu qualification, and update #374. Keep real Linux tmpfiles/systemd/libvirt/qcow2/guest evidence, the Windows protected-service refresh, and dual-guest C acceptance open as physical gates. No setup, sudo/UAC, protected service/provider/VM/guest effect, or repository execution occurred.

Hosted acceptance checkpoint: [GitHub Actions run 33228024953](https://github.com/iteathen/DevBridge/actions/runs/33228024953) passed Windows serialized complete-suite/doctor, Windows bounded preflight/identity/installer, Ubuntu complete-suite/doctor, and Ubuntu bounded preflight/identity/installer on exact implementation commit `28bbb55184310fc44c92a7f41610158c90b3c894`. The Ubuntu suite also passed the real crash-retained Unix-domain-socket restart proof. Keep #374 open for real Linux tmpfiles/systemd endpoint ownership, libvirt/qcow2/provider/guest qualification, and the later protected activation gates. This is hosted software acceptance, not physical VM readiness.

## No-elevation plan: installer and Stage-0 nested internals

Assessment 2026-08-29: issue #253 is the next non-elevated child under #244 while physical Stage-8 activation waits for the future protected-service refresh. `docs/testing/DB-HO071-issue-253-installer-stage0-nested-legos.md` records the DB-003/009/011/020 audit, complete 735-line installer and 389-line zero-state parent inventory, 37/37 direct baseline, Node/Git/GitHub primary research, reassessment, and scoped plan.

The one-file distribution contract changes the mechanical solution: Node `data:` modules cannot resolve relative imports. Keep modular source parents around isolated input, source, state, component, lease, publication, materialization, and continuation children, then deterministically embed those child modules as `data:` imports in the committed standalone bootstrap/installer artifacts. Preflight must prove exact regeneration and reject relative child imports or artifact drift. Only parents compose topology; children import no sibling and name no setup/provider/repository-task/model/VM context.

Preserve all public callable behavior, durable protocols, exact subject/component/wrapper/selection identities, Git isolation, installation commit, and recovery ordering. Delete moved code and the redundant non-durable pin handoff instead of keeping compatibility implementations. Qualify direct copied-installer and first-byte data-import behavior plus focused children, interruption/recovery, preflight, full suite, doctor, and hosted Windows/Ubuntu. No setup, UAC/sudo, service/provider/image/environment/VM/guest mutation, repository execution, or GPU/CUDA work is authorized by this slice.

Implementation checkpoint 2026-08-29: the installer and zero-state source parents are now 191-line and 153-line public composition surfaces around six and four closed nested children. Children import only Node built-ins, import/name no sibling, and receive concrete protocol/source/file/route/action topology only from their parent. A generic deterministic compiler embeds the children as `data:` modules into the committed standalone root artifacts; repository preflight rejects import-boundary violations and any source/artifact byte drift. Moved monolithic code and the redundant `pinSelectedRunner` value were deleted without compatibility readers or alternate implementations.

Focused standalone/nested/installer/selection/exact-source/handoff tests pass 24/24; three additional real-filesystem/real-Git recovery repetitions pass 19/19 each; repository preflight passes 2 generated artifacts + 199 syntax files + 2 JSON files + 160 targeted test files; the complete serialized Windows suite passes 1,798 total / 1,782 passed / 16 expected skips / zero failures; doctor passes while correctly reporting no configured repository-execution route; exact regeneration and `git diff --check` pass. Commit and push the exact checkpoint, then require hosted Windows/Ubuntu qualification before closing #253. No setup, UAC/sudo, protected service/provider/image/environment/VM/guest effect, repository execution, or GPU/CUDA work occurred.

Hosted attempt 1 (`33281103991`, implementation commit `6a8efc9cf0d494d034210b6b6dd7a2ce6dfac081`) passed both Ubuntu jobs but Windows checkout converted the two generated root artifacts to CRLF, so the exact regeneration gate immediately reported both stale. Keep byte-exact verification. Add path-scoped `text eol=lf` attributes only for `bootstrap-devbridge.mjs` and `install-devbridge.mjs`, as defined by Git's official attributes contract, and requalify the corrected exact commit on all four jobs.

Accepted checkpoint: [GitHub Actions run 33281315853](https://github.com/iteathen/DevBridge/actions/runs/33281315853) passed Windows serialized complete-suite/doctor, Windows bounded preflight/identity/standalone-installer, Ubuntu complete-suite/doctor, and Ubuntu bounded preflight/identity/standalone-installer on exact correction commit `0eb21769cf7f3898fb85ac698f6dd67abad195e2`. Close #253. The remaining non-GPU operational frontier is the future one-shot protected Windows service refresh followed by real end-to-end Windows and Linux guest C canaries, Stage-7/Stage-8 physical acceptance, and then the authorized integration/cleanup sequence. No physical readiness is inferred from hosted CI.

## No-elevation plan: physical-canary nested internals and exact-owner lease

Assessment 2026-08-29: issue #254 is the remaining open child under #244. `docs/testing/DB-HO072-issue-254-physical-canary-nested-legos.md` records the mandatory DB-020 VM planning gate, complete 619-line parent and focused-test inventory, focused 31/31 baseline, Node 22 filesystem research, reassessment, and scoped plan.

Keep `src/app/ubuntu-production-image-physical-canary.js` as the sole public parent and the only place that names/composes the concrete construction, provider, image, access, durable-state, and release topology. Extract five closed children for neutral configuration, preparation evidence, mutation admission, completion reconciliation, and bounded progress coordination. Children import no sibling or local implementation, accept no broad runtime/provider object, name no current external topology, and cannot broaden run authority. Delete moved mechanics without wrappers or legacy paths.

The audit separately proved a pre-existing cleanup defect: the current run-lock owner unconditionally removes the pathname and can therefore delete a replacement record it did not acquire. Issue #375 owns the semantic correction. Preserve exclusive-create fail-closed admission and the existing conflict diagnostic, but release only after exact regular-file identity plus versioned token/PID record re-observation. Never infer stale ownership or reclaim a pre-existing PID-only record. This is conservative re-observation, not an unsupported atomic compare-delete claim.

Add direct child/ownership/recovery/topology proofs, retain all parent behavior, and require local plus hosted Windows/Ubuntu qualification before closing #375 or #254. No setup, UAC/elevation attempt or bypass, protected service/provider/image/environment/VM/guest operation, repository execution, or physical canary is permitted during this slice.

Implementation checkpoint 2026-08-29: the physical-canary parent now retains only public validation/projection and explicit current topology around five closed children for configuration/path roles, preparation evidence, exact-owner mutation admission, completion reconciliation, and bounded progress. The parent maps neutral child values/actions to the established configuration/runtime shape and is the only module naming Ubuntu, Hyper-V, VHDX, VM, guest, provider, source, access, and release identities. Children import no sibling/local implementation. Moved helpers and the old phase loop were deleted without wrappers or a second implementation.

#375 is corrected without stale-lock inference or a compatibility reader. Exclusive create remains the admission authority. The v1 owner record is synced and binds token/PID plus opened-file identity; cleanup proceeds only after non-symlink regular-file identity and exact-byte re-observation. Pre-existing PID-only and replacement records remain untouched. This does not claim atomic compare-delete behavior or broaden the host-control-owned state-root assumption.

Direct nested/lease tests pass 8/8; focused physical-canary/setup/composition/release tests pass 39/39 in three consecutive runs; repository preflight passes 2 standalone artifacts + 200 syntax files + 2 JSON files + 162 targeted tests in 28.7 seconds; the complete suite passes 1,806 total / 1,790 passed / 16 expected Windows skips / zero failures; doctor passes and preserves VM-route fail-closed status; topology/deleted-helper/diff checks pass. Commit and push the exact checkpoint, then require hosted Windows/Ubuntu qualification before closing #375/#254. No UAC or protected/physical operation occurred.

Hosted attempt 1 (`33282648657`, commit `1749d59412c050ffacd76490a6e396367c80f9e3`) passed both Ubuntu jobs. Windows smoke and the complete suite failed only the two new normal-release assertions because Node 22.16.0 pathname `lstat()` reports device `0` for the same NTFS file whose opened-handle `stat()` reports a nonzero device. The conservative mismatch preserved the owner's own record. This reproduces locally under exact Node 22.16.0 and does not reproduce under Node 24.

Do not weaken token/bytes/replacement checks. Compare complete device/inode/birth/change identity between acquired and reopened handles. Compare inode/birth/change identity for non-following pathname observations in the same fixed parent directory, where the Node 22 cross-API device field is not stable. Exact Node 22.16.0 and Node 24 direct suites both pass 20/20 after correction; focused behavior passes 39/39; preflight retains its 200/2/162 passing inventory; and the complete corrected suite passes 1,806 total / 1,790 passed / 16 expected skips / zero failures. Re-run all four hosted jobs before acceptance.

Accepted checkpoint: GitHub Actions run `33282936714` passed Ubuntu smoke/full and Windows smoke/serialized-full jobs on exact correction commit `c546075d7e5525817cf4b46ba68a11607f4b3e82`. Close #375 and #254, update the #244 umbrella, and keep the protected service refresh plus physical Windows/Linux guest C canaries as the next operational frontier. No elevation, provider/VM mutation, guest access, or repository execution occurred.

## No-elevation correction: deterministic Windows smoke preflight

Assessment 2026-08-29: #290 remains open because full-suite serialization did not own the smoke preflight's 162-file Node runner. Hosted runs `33212636105` and `33219166402` proved a separate load-sensitive preflight path. DB-019, Node 22.16.0 test-runner behavior, current GitHub public-runner resources, exact source/test topology, and the measured 101-second fully serialized preflight are recorded in `docs/testing/DB-HO055-issue-290-windows-ci-concurrency.md` at baseline `fd1d6e6536331fa7229d5a7e5a4ff6f188166ec6`.

Add one closed neutral `serializeTargetedTests` preflight option, reject all unsupported arguments, and have only the Windows smoke workflow select it. Preserve every selected test and all process/product/tool/provider timeouts; leave non-Windows/default scheduling and the already serialized Windows full suite unchanged. Qualify focused/repeated/default/preflight/full behavior and require multiple exact hosted Windows passes before closing #290. No UAC or protected/physical operation is permitted.

Implementation checkpoint: repository preflight now owns the fixed neutral serialization option and rejects every unsupported CLI/programmatic shape before work. Only the Windows smoke workflow selects it; non-Windows smoke and both full-suite policies remain unchanged. Direct Node 22/24 contracts pass 12/12, two exact Node 22.16.0 serialized preflights pass the same 200 syntax / 2 JSON / 163 targeted inventory in 79.3s and 77.9s, default preflight passes in 27.1s, and the complete local suite passes 1,810 total / 1,794 passed / 16 expected skips / zero failures. Publish and require multiple complete exact-head hosted runs before closing #290. No protected or physical operation occurred.

Accepted checkpoint: GitHub Actions run `33283831485` attempts 1 and 2 both passed Windows serialized smoke preflight, Windows serialized full-suite/doctor, and both default Ubuntu jobs on exact implementation commit `146539b3b6decac3de680c255cd394c882485082`. The Windows smoke jobs completed in 1m19s and 1m28s inside unchanged deadlines. Close #290 and continue the next primitive no-elevation dependency; protected/physical gates remain pending.

## No-elevation correction: owned command discovery

Assessment 2026-08-29: #291 is the next primitive setup/install reliability dependency. `docs/testing/DB-HO073-issue-291-owned-command-discovery.md` records the DB-003/009/011/020 audit, exact owner and presentation inventory, focused 49/49 baseline, Microsoft process-environment research, reassessment, and scoped plan.

Persistent User PATH and the inherited current process environment are separate facts. Classify exact post-write persistence plus current visibility as `available`, `refresh-required`, `caller-omitted`, or fail-closed `not-persisted`. Do not call every miss a new-shell problem: descendants of a deliberately reduced caller normally inherit the same reduced environment. Replace the old v1 shell/implementation-entry fallback with one v2 result carrying the exact verified stable command, add one installation-owned resolver for callers that know the canonical home, and update every relevant handoff so bare `devbridge` is never recommended to a caller that cannot resolve it. Delete moved/obsolete result fields rather than retain aliases.

Qualify the classifier, persistence observation, exact ownership, healthy persisted-plus-omitted Windows case, presentation, preflight, full suite, doctor, generated artifacts, diff hygiene, and hosted Windows/Ubuntu before closing #291. No setup invocation, UAC/sudo, protected service/provider/image/VM/guest operation, repository execution, or GPU/CUDA work is authorized by this slice.

Implementation checkpoint: the old v1 shell-refresh/Node-entry fields are deleted. One closed dependency-free child classifies only verified persistence/change/current-visibility facts; the Windows adapter rereads User PATH and rejects widened output; the POSIX adapter verifies its exact managed record; and one installation-owned resolver proves exact stable launcher bytes from canonical home. Setup uses the same resolver after publication and every relevant handoff now distinguishes `refresh-required` from `caller-omitted`, always providing the exact stable invocation when bare discovery is unavailable. No integration-specific PATH guess or compatibility result remains.

Focused current-Node and exact Node 22.16.0 tests each pass 64/64. Default and exact-Node-22 serialized preflights pass the same 2 generated artifacts / 202 syntax / 2 JSON / 166 targeted-test inventory. The complete serialized Windows suite on final implementation bytes passes 1,820 total / 1,804 passed / 16 expected skips / zero failures. Read-only doctor reports `ok: true`, coding adapters disabled, and repository execution fail-closed because no persistent-environment routes are configured. Finish diff/generated-artifact hygiene, commit/push the exact implementation, and require hosted Windows/Ubuntu acceptance before closing #291. No setup/elevation/protected/provider/VM/guest/repository-execution effect occurred.

Accepted checkpoint: [GitHub Actions run 33285239052](https://github.com/iteathen/DevBridge/actions/runs/33285239052) passed Ubuntu smoke/full and Windows serialized smoke/full plus doctor on exact implementation commit `e87abc6681924ce0af46fd24b7f8cd130fa8d2dc`. Close #291. This accepts the software contract only; the one-shot protected Windows service refresh and real Windows/Linux guest C canaries remain the non-GPU operational frontier. No elevation, protected/provider/VM/guest mutation, repository execution, or model invocation occurred.

## No-elevation closeout audit: self-refreshing Windows authority

Assessment 2026-08-29: `docs/testing/DB-HO074-issue-292-closeout-audit.md` reconciles #292's exact architecture, hosted, and physical Windows acceptance evidence. The current neutral reconciler and Windows authority corpus passes 156 tests with one expected filesystem-capability skip and zero failures; current exact-head CI run `33285419333` is four-job green. Historical physical re-entry on integrated commit `4483474fc85e5f50a21accd7fef7c4a7a6067dfb` crossed one bounded elevation, returned to the ordinary caller, completed its required negative/positive acceptance gates, retained all 16 repositories, and reached the read-only construction gate. Close #292.

The newer protected-service generation still requires one future operator-approved refresh. That is normal use of the accepted #292 mechanism, not a reason to keep its architecture issue permanently open. Keep #177, #293, #360, #372–#374, and #115–#116 open: Linux protected authority, current profile/provider/guest activation, dual-guest C execution, and Stage 7/8 physical evidence remain unproved. No setup, UAC, service/provider/VM/guest effect, repository execution, or model invocation occurred during this audit.

## No-elevation installed-candidate staging

Assessment 2026-08-29: the owned stable command now resolves correctly, but the installed development runner still selects `0ace83bf25d131d0d6bcd4f00617b30e96f9bb93`, behind the exact qualified draft integration head. `docs/testing/DB-HO075-no-elevation-installed-candidate-staging.md` records the DB-003/009/011/020 ownership audit and bounded plan. After the plan commit passes all four hosted jobs, run the supported zero-state bootstrap against that exact commit with `--install-only`, verify wrapper-owned entry status and the installed command, then run read-only doctor through Permanent Entry. Do not invoke setup, elevation, the protected service mutation channel, a provider, VM, guest, repository execution, or a model adapter.

Implementation checkpoint: plan head `ef70f581d8eeb5b39fd378548091de9ba34a16bd` passed all four jobs in CI run `33285925567`, then the supported exact zero-state `--install-only` transaction updated the user-owned Permanent Entry and pinned both component/default runner to that commit. Wrapper-owned status and the installation-owned command resolver independently passed. Installed doctor rejected a missing config path rather than guessing, then passed with `ok: true` against the verified example config while reporting repository execution unavailable and lifecycle `setup-reentry-required`; the normal generated config remains absent. No setup, UAC, service/provider/VM/guest effect, repository execution, or model invocation occurred. The Windows host is staged at the safe frontier for its later one-shot protected refresh.

## Linux provider-authority topology primitive

Assessment 2026-08-29: issue #376 and `docs/testing/DB-HO076-issue-293-linux-provider-topology-plan.md` record the next fundamental no-elevation dependency under #293. The existing Linux authority stack has no current production classifier for modular versus monolithic libvirt management, compatibility proxy surfaces, socket-activated policy, and group-only versus polkit-backed admission. The historical first-existing-socket rule is rejected.

Implement one read-only Linux/provider-local owner with fixed system-manager and NSS mechanics. It must accept no caller-selected unit, endpoint, path, executable, argv, environment, provider object, service identity, or policy. Return only path-free neutral classification, route, capability identity, subject role/policy, and bounded reason evidence. Select a capability only when every active full-management subject is exact group-only. Observe but do not accept service-only/traditional, root-only, policy-backed, mixed, ambiguous, stale, widened, malformed, or unexplained topology. Do not attach setup/elevation or perform a provider connection in this brick.

Implementation checkpoint: the classifier now observes all fixed modular, monolithic, and compatibility unit pairs, requires current socket-activated one-stream definitions, inspects both fixed management subjects without following the final entry, resolves numeric group identity through NSS, and withholds selection unless the complete active surface is group-only. Its output contains no host path or raw error. Focused tests pass 10/10; preflight passes 2 artifacts / 203 syntax / 2 JSON / 167 targeted tests; the Linux selection passes 197 total / 191 passed / 6 expected skips; architecture gates pass 33 with 1 expected Windows skip; the full suite passes 1,830 total / 1,814 passed / 16 expected skips; and doctor remains truthfully VM-route fail-closed. Commit/push, require exact-head Windows/Ubuntu CI, then close only #376. Keep #293 open for policy composition, ordinary denial, bounded elevation, positive provider access, protected storage, and physical libvirt/qcow2/guest proof. No privileged or physical effect occurred.

Accepted checkpoint: [GitHub Actions run 33286712099](https://github.com/iteathen/DevBridge/actions/runs/33286712099) passed Windows smoke/full and Ubuntu smoke/full plus doctor on exact implementation commit `08f41f13037d5ceb4d2aa064a63325439f604bbd`. Close #376. This accepts only the read-only topology classifier; #293 remains open for capability-policy composition, configured/effective ordinary-principal denial, bounded protected setup, positive provider access, protected storage, and physical libvirt/qcow2/guest evidence. No privileged or physical effect occurred.

## No-elevation plan: Linux principal capability separation

Assessment 2026-08-29: issue #377 and `docs/testing/DB-HO077-issue-377-linux-principal-capability-separation.md` record the next primitive dependency under #293. Exact group-only provider topology and NSS-configured account groups are independently observable, but the current ordinary process can retain inherited group authority after NSS membership changes. Configured and active capability sets are separate facts and both must deny every active management capability before later protected setup can select one.

Implement three closed read-only bricks: a native current-principal credential observer, a provider-agnostic pure separation policy, and one Linux composition root. Only the root composes #376 topology, NSS records, and current credentials. Children import no local implementation or sibling and use neutral numeric contracts. A selected capability is returned only for exact group-only topology, exact NSS capability name/ID binding, an exact non-root current principal, and absence from both configured and active capability sets. Do not create a plan, attach setup/elevation, mutate identity/service/provider/storage, or claim readiness.

Implementation checkpoint: the native leaf observes fixed real/effective process credentials; the import-free pure policy proves complete restricted-capability separation; and the Linux root alone composes exact topology, NSS rebinding, current credentials, and the neutral decision. Configured-only and inherited-active-only management membership independently withhold selection across every primary/compatibility capability. Focused new tests pass 10/10; the topology/NSS/preflight set passes 25/25; preflight passes 2 artifacts / 204 syntax / 2 JSON / 168 targeted tests; the Linux/authority selection passes 238 total / 231 passed / 7 expected skips; architecture gates pass 33 with 1 expected Windows skip; the complete serialized suite passes 1,840 total / 1,824 passed / 16 expected skips; doctor is green and VM-route fail-closed; generated artifacts and diff hygiene pass. Commit/push, require exact-head Windows/Ubuntu CI, then close only #377. No privileged or physical effect occurred.

Hosted correction checkpoint 2026-08-29: exact-head run `33287528755` passed Ubuntu smoke/full and Windows serialized full-suite/doctor, but the fully serialized Windows smoke preflight was killed at its unchanged 120-second boundary without emitting a test failure. Reopen #290. The complete recurrence assessment, refreshed Node/GitHub research, exact Node 22.16.0 two-worker measurement, and replacement plan are durable in `docs/testing/DB-HO055-issue-290-windows-ci-concurrency.md`.

Delete the serialize-only preflight interface rather than retain legacy. Replace it with one closed neutral `boundTargetedTestConcurrency` selection that maps only to a fixed two-worker policy for the unchanged test inventory. Keep every test, assertion, process isolation rule, product/tool/provider timeout, and workflow deadline unchanged; expose no numeric or arbitrary scheduler input. Qualify repeatedly under exact Node 22.16.0, the default path, the full suite, doctor, hygiene, and all four hosted jobs. Re-close #290 and accept #377 only after repeated complete exact-head hosted acceptance. No UAC/elevation or protected/physical operation is permitted.

Replacement implementation checkpoint: the obsolete flag and programmatic field are deleted; one closed neutral bound maps only to two targeted test-file workers at the preflight composition edge. Current/exact-Node-22 focused contracts pass 8/8; exact Node 22.16.0 bounded preflight passes the unchanged 204/2/168 inventory twice in 49.3s and 48.5s; default preflight passes in 27.9s; #377's exact-Node-22 focused topology/principal set passes 20/20; architecture gates pass 33 with one expected Windows skip; the complete suite passes 1,840 total / 1,824 passed / 16 expected skips / zero failures in 193.9s; doctor is green and VM-route fail-closed; generated/diff hygiene passes. Commit/push and require repeated complete hosted acceptance before closing #290/#377.

Accepted checkpoint: GitHub Actions run `33288158789` attempts 1 and 2 passed the complete Windows/Ubuntu smoke/full matrix on exact implementation commit `50d3a733785194be2d4161c7aebbfc42934289bd`. Windows bounded smoke completed in 1m30s and 1m23s; Windows serialized full-suite/doctor completed in 2m15s and 2m32s. Re-close #290 and close #377. Continue #293 at policy-to-plan composition; do not infer protected Linux authority, provider access, storage readiness, VM readiness, or physical guest execution from this read-only software proof.

## No-elevation plan: Linux authority-plan selection

Assessment 2026-08-29: issue #378 and `docs/testing/DB-HO078-issue-378-linux-authority-plan-selection.md` record the next primitive dependency under #293. The accepted #377 preflight owns exact local capability selection and ordinary-principal denial, while the deterministic lifecycle-authority plan still accepts its management capability directly. There is no production setup-local composition owner between them.

Add one closed read-only root whose request accepts only state identity and ordinary principal. It observes platform locally, accepts a plan only from exact verified eligibility evidence, maps only the selected neutral capability name into the existing deterministic projector, and revalidates the complete plan identity and capability binding. It must expose no raw capability selection, provider/socket/unit/path override, executable, argv, environment, service identity, or mutation authority. Off-Linux or malformed/unavailable/forged child evidence returns no plan and invokes no unrelated port. Keep both existing owners unchanged; add no legacy route or fallback.

Extend the existing focused test process, add only the new source to syntax preflight, qualify current and exact Node 22 behavior, preflight, authority selection, architecture, full suite, doctor, generated/diff hygiene, then require exact-head Windows/Ubuntu CI before closing #378. Keep #293 open for protected identity/service setup, positive provider access, storage, and physical Linux VM/guest proof. No setup, elevation, provider connection, protected mutation, VM/guest action, repository execution, or model invocation is permitted in this slice.

Implementation checkpoint: one setup-local root now observes platform locally, accepts only exact #377 eligibility evidence, validates the complete unique capability set and selected membership, maps one neutral capability into the existing deterministic plan, and independently requires exact canonical plan equality before returning immutable plan data. The caller cannot supply capability, provider, protected topology, executable, argv, environment, service identity, or mutation authority. Off-Linux and unavailable/malformed/forged evidence return no plan with bounded local reasons; no compatibility or fallback route exists.

Focused current-Node and exact Node 22.16.0 tests pass 15/15. Exact Node 22.16.0 bounded and current default preflights pass the same 2 standalone artifacts / 205 syntax / 2 JSON / 168 targeted-test inventory. Linux-focused selection passes 212 total / 206 passed / 6 expected skips; architecture gates pass 33 with 1 expected Windows skip; the complete serialized suite passes 1,845 total / 1,829 passed / 16 expected skips / zero failures in 204.8 seconds; doctor is green and VM-route fail-closed; standalone generated-artifact and diff hygiene checks pass. Commit/push and require exact-head hosted Windows/Ubuntu acceptance before closing #378. No protected or physical effect occurred.

Accepted checkpoint: GitHub Actions run `33289229145` passed Ubuntu smoke/full and Windows bounded-smoke/serialized-full plus doctor on exact implementation commit `ddda1fc4248db505d8be2941fb83f13f8e4c8697`. Close #378. This accepts only read-only capability-to-plan selection; #293 remains open for protected identity/service reconciliation, bounded setup/elevation, positive provider access, protected storage, and physical libvirt/qcow2/guest evidence.

## Stage-6 reliability correction: exact live-attempt observation

Assessment 2026-08-29: docs-only head `07c8c14e1857be46ae227c3bdcb2521282727138` exposed one Ubuntu full-suite recurrence in run `33289350133`: a fast request remained journaled as `planned` after both its permanent attempt fence and exact current activity record existed, and the fixed 315 millisecond reread window expired. Issue #379 and `docs/testing/DB-HO079-issue-379-live-attempt-observation.md` record the exact evidence, DB-003/009/020 ownership audit, Node 22.16 filesystem research, reassessment, and scoped plan.

Repair the owning guest-local activity stud before continuing higher-level Linux setup composition. Replace the Boolean attempt query with a closed store-owned aggregate observation: `absent` only when no fence exists, `current` only when a strictly valid fence names its exact strictly current activity record, and `indeterminate` for every partial/malformed/substituted/missing/stale/invalid condition. Return no token/path/time/error evidence. The bridge may map exact current transition evidence to its existing public running state while journal publication catches up. Preserve terminal journal authority, permanent non-replayable fences, exact-token running observation, bounded retries, cancellation ownership, and version-1 rejection. Do not mask the race by widening only a timeout and do not add compatibility or process-identity paths.

Add boundary and stress proofs, qualify locally and on hosted Windows/Ubuntu, and close #379 only on exact-head acceptance. Then resume #293 at the next non-elevated protected identity/service planning dependency. No setup, elevation, protected service/provider/VM/guest effect, repository execution, or model invocation is permitted by this correction.

Implementation checkpoint: the activity store now strictly correlates a complete permanent fence with only its exact current activity and projects only three bounded states. Its old Boolean attempt surface is deleted. Planned bridge observation recognizes that current transition without acquiring the token/schema/file topology; partial, malformed, widened, missing, mismatched, stale, invalid, and symbolic evidence stays indeterminate and non-replayable. Existing token-bound nonterminal, terminal journal, cancellation, freshness, and version-1 fail-closed behavior is unchanged.

Focused current/exact-Node-22 bridge tests pass 29 total / 28 passed / 1 expected Windows skip; the payload/LEGO set passes 41 total / 40 passed / 1 expected skip; twenty additional sixteen-request fast-child repetitions pass. Current and exact Node 22.16.0 preflights pass the unchanged 2 artifacts / 205 syntax / 2 JSON / 168 targeted-test inventory. Architecture gates pass 34 total / 33 passed / 1 expected skip; the complete serialized suite passes 1,849 total / 1,832 passed / 17 expected skips / zero failures in 193.8 seconds; doctor remains green and VM-route fail-closed; standalone/identity and diff hygiene pass. Commit/push, require the exact four-job hosted matrix, and close #379 only after acceptance. No UAC or protected/physical effect occurred.

Hosted acceptance checkpoint: GitHub Actions run `33289991127` passed Ubuntu smoke/full and Windows bounded-smoke/serialized-full plus doctor on exact implementation commit `3c6a579ea0ae64c970bccb25f6cc1eb453e451d6`. Require the documentation-only acceptance head to pass the same four jobs because that exact kind of run exposed the original recurrence, then close #379 and resume #293. No protected or physical effect occurred.

## No-elevation correction: Linux required-group identity binding

Assessment 2026-08-29: issue #380 and `docs/testing/DB-HO080-issue-380-linux-required-group-binding.md` record a primitive identity defect exposed before bounded Linux setup. #376–#378 selected an exact management-group name/ID pair, but plan selection retained only the name. The local identity reconciler then treated that externally owned capability like the two DevBridge-owned groups and could create a missing same-name replacement with a different numeric identity.

Bind the complete selected name/ID pair into the canonical plan, persist it in the root-owned ownership record, project the numeric ID into systemd and shadow-utils effects, and carry the plan through refresh composition, lifecycle identity binding, and local identity reconciliation. Require the exact identity before every identity mutation; create only the installation-owned read and coordination groups; never create, rename, infer, or name-only adopt the required group. Preserve ordinary-principal denial and immutable installed numeric binding. Add no duplicate sidecar authority, setup/elevation, provider object/path/unit, generic helper, legacy reader, or fallback.

Qualify missing, rebound, aliased, widened, interrupted, exact/no-op, and module-isolation behavior locally and on hosted Windows/Ubuntu before closing only #380. Then resume #293 at bounded setup/elevation and positive protected provider proof. No privileged or physical effect is permitted in this prerequisite.

Implementation checkpoint: the exact locally selected management capability now remains a name/numeric-ID pair through plan protocol v2, ownership protocol v2, restart reconstruction, refresh, identity reconciliation, systemd, process inspection, and protected runtime admission. Only the two installation-owned groups can be created. Account and service effect boundaries receive numeric group selectors; missing, rebound, oversized, widened, aliased, or persisted-ID-mismatched evidence fails closed, and base/candidate plan drift is rejected before claim admission. There is no v1 reader, duplicate sidecar authority, or fallback.

Current and exact Node 22.16.0 Linux-focused tests each pass 214 total / 208 passed / 6 expected skips. Both preflights pass 2 artifacts / 205 syntax / 2 JSON / 168 targeted tests. Exact-Node architecture gates pass 34 total / 33 passed / 1 expected Windows skip; the complete serialized suite passes 1,851 total / 1,834 passed / 17 expected skips / zero failures; product identity/standalone passes 3/3; doctor is green and VM-route fail-closed; generated and diff hygiene pass. Commit/push the isolated implementation and require all four hosted Windows/Ubuntu jobs before closing #380. No setup, elevation, protected service/provider/VM/guest effect, repository execution, or model invocation occurred.

Accepted checkpoint: [GitHub Actions run 33291463555](https://github.com/iteathen/DevBridge/actions/runs/33291463555) passed the complete Ubuntu/Windows smoke/full matrix plus doctor on exact implementation commit `39392f00a15a97bee43270e9cb2e80025bbcf88f`. Close #380. Continue #293 at bounded protected setup/elevation and positive provider proof; do not infer protected Linux authority, storage, VM, or physical guest readiness from this software acceptance.

## No-elevation plan: closed Linux protected-refresh child

Assessment 2026-08-29: issue #381 and `docs/testing/DB-HO081-issue-381-linux-protected-refresh-child.md` record the next primitive dependency under #293. The Linux refresh composition and shared reconciler are accepted, but no closed production child binds an ordinary-selected principal/capability/candidate to one protected refresh. Setup and the hidden lifecycle child still route only through Windows.

Implement a broker-independent Linux child contract before selecting or attaching `sudo`/`pkexec`. Its exact request contains only local state identity, ordinary principal name/UID/GID, neutral required-group name/ID, and exact package/Node candidate digests. The child must independently prove elevated Linux invocation, re-observe the exact fixed group-only management topology, rebind principal and group through NSS, reject configured ordinary membership, remeasure fixed local sources, reconstruct the deterministic plan, and attach the existing Linux composition/shared refresh. Return only bounded sanitized result evidence. Accept no profile/provider/service/socket/image/VM/storage/source path, executable, argv, environment, credential, or generic effect.

Review refinement: complete topology means every active group-only management surface, including a distinct compatibility group when one exists. The child rechecks non-membership in every observed group while binding the plan only to the selected primary pair. It also re-observes the state identity as one canonical, non-group/world-writable directory owned by the exact ordinary principal before candidate measurement; an arbitrary request path is not elevated identity authority.

Keep the child unreachable from setup/CLI in #381 and invoke no elevation or physical effect. Qualify identity/topology/candidate drift, malformed/widened contracts, no-op/recovery projection, source isolation, preflight, architecture, full suite, doctor, hygiene, and hosted Windows/Ubuntu before closing only #381. A following issue will own replaceable broker discovery/invocation and ordinary-parent automatic re-observation; provider/storage/physical Linux evidence remains later.

Implementation checkpoint: the exact neutral child contract and Linux-local composition root are now implemented without a CLI/setup route. The root re-observes invocation identity, exact principal origin, all active group-only management capabilities, NSS identity and non-membership, an ordinary-owned canonical non-writable state identity, and fixed local package/Node digests before reconstructing the deterministic plan. A distinct compatibility group is verified but never becomes plan authority. Pre-effect denial is proven unchanged; post-admission uncertainty is explicit, bounded, and path-free. No legacy request, generic privileged helper, broker selection, or fallback exists.

Local qualification passes direct child 10 total / 9 passed / 1 expected Windows skip; current and exact Node focused boundary 140 total / 138 passed / 2 expected skips; current and exact preflight 2 artifacts / 207 syntax / 2 JSON / 169 targeted tests; exact-Node architecture 34 total / 33 passed / 1 expected skip; product/standalone 5/5; and the complete exact-Node serialized suite 1,861 total / 1,843 passed / 18 expected skips / zero failures in 196 seconds. Doctor is green with coding adapters disabled and repository execution unavailable/fail-closed. Generated and diff hygiene are clean. Push the isolated implementation and require exact-head hosted Windows/Ubuntu before closing #381. No protected or repository-code effect occurred.

Accepted checkpoint: [GitHub Actions run 33293225945](https://github.com/iteathen/DevBridge/actions/runs/33293225945) passed the exact implementation `06fbc494cca82e741adf9c3d9bddf43792339231` across Ubuntu/Windows smoke/full plus doctor. Ubuntu executed the real-filesystem state identity canary. Close #381. Continue #293 with a separate replaceable broker/dispatcher/ordinary-reobservation slice; do not infer elevation, protected Linux service/provider/storage, VM, guest, or repository-execution readiness from this software acceptance.

## No-elevation plan: Linux ordinary readiness and one-refresh re-observation

Assessment 2026-08-29: issue #382 and `docs/testing/DB-HO082-issue-382-linux-ordinary-readiness.md` record the next primitive dependency under #293. The accepted ordinary plan selector and protected refresh child are disconnected, while setup and hidden child dispatch remain Windows-only. Attaching an authentication broker now would make broker mechanics own candidate identity, installed readiness, ordinary storage denial, and success policy.

Add three closed bricks before broker selection: an import-free one-attempt/re-observation policy, a Linux-local actual descriptor-acquisition denial probe, and one Linux-local readiness root. The root selects the exact plan, re-observes identities, measures and binds the fixed candidate, evaluates existing complete inspection evidence, proves current non-root read denial, and probes protected read interfaces. It returns either exact readiness or the unchanged #381 child request as an opaque local subject. The policy may forward that subject once but must ignore child claims and accept success only after a complete fresh observation.

Accept no broker/executable/argv/environment/provider/service/socket/image/VM/storage override, credential, protected effect, or generic mutation. Do not edit setup or CLI, add compatibility, or invoke elevation. Qualify ready/missing/foreign/drift/direct-access/one-attempt/re-observation boundaries locally and on hosted Windows/Ubuntu before closing only #382. Keep #293 open for broker attachment, protected positive provider/storage evidence, physical Linux VM/guest proof, and setup integration.

Implementation checkpoint: the #381 state observer is now one reusable Linux-local leaf; ordinary readiness and the protected child share its exact canonical/stable owner contract without cross-module behavior duplication. A separate descriptor-bound leaf proves actual current-process read denial against the stable protected authority directory and closes any acquired handle. The Linux readiness root composes exact plan, candidate, NSS/current-principal/state, full installed inspection, negative access, and protected read-interface evidence, returning only generic readiness or the unchanged frozen #381 request. An import-free policy forwards at most one opaque subject, ignores attempt claims, and accepts success only from a complete second observation.

Current/exact-Node focused tests pass 45 total / 43 passed / 2 expected Windows skips; the wider Linux boundary passes 240 total / 232 passed / 8 expected skips on both runtimes; current/exact preflight passes 2 artifacts / 211 syntax / 2 JSON / 172 targeted tests; exact-Node repository-execution architecture passes 28/28; product/standalone integrity passes 5/5; and the complete exact-Node serialized suite passes 1,877 total / 1,858 passed / 19 expected skips / zero failures in 192 seconds. Doctor is green and truthfully VM-route fail-closed. Commit/push the isolated implementation and require exact-head Ubuntu/Windows acceptance; Ubuntu must execute the real descriptor canary before #382 closes. No elevation or protected/physical/repository-code effect occurred.

Hosted implementation checkpoint: [GitHub Actions run 33294423593](https://github.com/iteathen/DevBridge/actions/runs/33294423593) passed Ubuntu smoke/full and Windows bounded-smoke/serialized-full plus doctor on exact implementation commit `70635a51ad208f71dee25a7d3d852b1fcf5c7904`. Ubuntu executed both real-filesystem canaries: the production state observer and production Linux descriptor-denial probe. Require the documentation-only acceptance head to pass the same four jobs, then close only #382 and continue #293 at a separately owned fixed authentication-broker adapter. No installed protected service/provider, elevation, VM, guest, or repository-execution readiness is implied.

Accepted checkpoint: documentation head `ca8ee61875d5429066d48777be896468677f91ae` passed the complete Ubuntu/Windows matrix in [GitHub Actions run 33294600094](https://github.com/iteathen/DevBridge/actions/runs/33294600094). Close #382. Keep #293 open; this accepts ordinary readiness and one-attempt re-observation only.

## No-elevation plan: fixed Linux CLI authentication broker

Assessment/research 2026-08-29: issue #384 and `docs/testing/DB-HO084-issue-384-linux-authentication-broker.md` record the next primitive dependency under #293. Accepted #382 now owns all readiness and retry policy, while #381 owns every privileged validation/effect. The missing brick is only the authentication handoff between one opaque attempt and the fixed child.

Implement one `sudo` adapter first for the CLI-default workflow; do not add an automatic `sudo`/`pkexec` fallback. The adapter must re-observe one fixed root-owned setuid non-writable executable, rebuild a minimal non-secret environment, invoke one fixed Node/entry pair through `shell: false` and `--`, carry only the bounded opaque subject on stdin, and return path-free non-authoritative status. Never use `-S`, `-E`, `-n`, askpass, a shell, caller executable/argv/environment, or a password relay.

Keep bounded JSON framing in an import-free neutral dispatcher and submitter parsing in a tiny Linux-local leaf. A fixed composition entry may attach those studs to the unchanged #381 child; setup/CLI must not import it in #384. Ubuntu must run only the real read-only executable-identity canary. No authentication/elevation, protected mutation, provider/storage operation, VM/guest action, repository execution, model invocation, or UAC is allowed during this slice.

Implementation checkpoint: four isolated owners now implement the neutral one-operation dispatcher, exact `SUDO_*` submitter projection, fixed `sudo` adapter, and sole Linux-local composition entry. The adapter fixes `/usr/bin/sudo -- <current-node> <fixed-entry>`, uses no shell/password relay/fallback, strips caller environment, and re-observes fixed executable plus launch identity after its one attempt. The entry validates dispatcher and protected-child contracts and cannot promote child output or exit status into ordinary readiness. Setup/CLI remains deliberately unattached.

Current and exact Node focused/wider tests pass 69 total / 67 passed / two expected Windows skips; current and exact preflight passes two artifacts / 215 syntax / two JSON / 176 targeted tests; exact-Node repository-execution architecture passes 34 total / 33 passed / one expected Windows symlink skip; and the complete exact-Node serialized suite passes 1,899 total / 1,879 passed / 20 expected platform skips / zero failures in 189 seconds. Exact-Node doctor, standalone artifact identity, and diff hygiene pass. Commit/push the isolated implementation and require exact-head Ubuntu/Windows acceptance; Ubuntu must execute the real read-only `/usr/bin/sudo` canary. No authentication, elevation, protected/physical/repository-code effect, or UAC occurred.

Hosted implementation checkpoint: [GitHub Actions run 33295960616](https://github.com/iteathen/DevBridge/actions/runs/33295960616) passed Ubuntu smoke/full and Windows bounded-smoke/serialized-full plus doctor on exact implementation `712ad50a76459aa90e31bd51966642c9a7f02e6a`. Ubuntu explicitly ran the real `/usr/bin/sudo` identity canary as test 693 rather than skipping it; the hosted full suite reported 1,899 tests and zero failures. Require the documentation-only acceptance head to pass the same matrix, then close only #384. Keep #293 open for setup selection/attachment and physical protected/provider/storage/guest evidence.

Accepted checkpoint: documentation head `dc6232fa18f752596222fbd7934666ac52d0bc0c` passed the complete Ubuntu/Windows matrix in [GitHub Actions run 33296109697](https://github.com/iteathen/DevBridge/actions/runs/33296109697). Close #384. The fixed broker remains unreachable until a separate setup composition slice attaches it.

## No-elevation plan: Linux setup authentication attachment

Assessment/research 2026-08-29: issue #385 and `docs/testing/DB-HO085-issue-385-linux-setup-authentication.md` record the next primitive dependency under #293. The accepted readiness policy, fixed authentication adapter, authenticated child, and Linux protected configuration proxy remain disconnected because setup defaults every platform to the Windows lifecycle-authority reconciler. Setup's GitHub identity is remote provenance and must never stand in for the current local OS principal.

Add one self-contained neutral current-principal observer and one setup-owned platform composition root. The observer projects only exact non-root name/UID/GID data and requires real/effective credential agreement. The root delegates Windows unchanged; on Linux it attaches accepted ordinary readiness to the fixed authentication attempt through the accepted one-attempt/fresh-reobservation policy, ignores child claims, and continues through the existing protected configuration inspect/reconcile/re-inspect contract only after authority readiness. Unsupported platforms fail closed.

Do not add broker discovery execution, `pkexec`/shell/direct-process fallback, password relay, caller program/argv/environment/path, GitHub-to-local identity mapping, generic privileged helper, provider operation, host repository execution, or compatibility setup route. Qualify direct, adversarial, source-isolation, preflight, full-suite, doctor, and hosted Ubuntu/Windows behavior before closing only #385. Ubuntu may execute the read-only current-principal observation canary; no authentication/elevation, protected/physical/repository-code effect, or UAC is permitted during this slice.

Implementation checkpoint: an import-free principal-evidence owner now binds one exact non-root name/UID/GID record to matching real/effective credentials, while a tiny fixed Node/OS parent supplies only `os.userInfo()` and process credential observations. The Linux setup root composes accepted readiness, one fixed authentication attempt, complete fresh re-observation, and accepted configuration inspect/reconcile/re-inspect; it never interprets child/authentication output. The application selector forwards the existing Windows request unchanged, projects only state/configuration to Linux, and fails closed elsewhere. Setup now defaults to that selector instead of the Windows adapter. No legacy/fallback path remains.

Final exact Node 22.16.0 focused qualification passes 89 total / 87 passed / two expected Windows skips. Current and exact preflight pass two artifacts / 219 syntax / two JSON / 178 targeted tests; the repository-execution/setup/product integrity gate passes 31/31; the final exact complete serialized suite passes 1,912 total / 1,891 passed / 21 expected platform skips / zero failures in 189 seconds. Exact doctor is green with repository execution unavailable/fail-closed and model adapters disabled. Diff hygiene passes, and both exact-runtime temporary trees were removed. Commit/push the implementation and require exact-head Ubuntu/Windows acceptance; Ubuntu must execute the read-only current-principal canary without invoking authentication.

Hosted implementation checkpoint: [GitHub Actions run 33297374805](https://github.com/iteathen/DevBridge/actions/runs/33297374805) passed Ubuntu smoke/full and Windows bounded-smoke/serialized-full plus doctor on exact implementation `db5cc6a88a98efd34ff0b2bac7a0f0626ff45975`. Ubuntu explicitly executed the production current-principal observation canary as test 218 rather than skipping it; its full suite reported 1,912 tests and zero failures. Require the documentation-only acceptance head to pass the same matrix, then close only #385. Keep #293/#372/#373 open for protected service/provider/storage and physical Windows/Linux VM/guest evidence.

Accepted checkpoint: documentation head `6213c8365c50d4e61c5ccf82f3a520d49dfffca5` passed the complete Ubuntu/Windows matrix in [GitHub Actions run 33297519460](https://github.com/iteathen/DevBridge/actions/runs/33297519460). Close #385. Keep #293 open; this accepts setup authentication attachment only.

## No-elevation correction: Linux protected resource re-observation

Assessment/research 2026-08-29: issue #387 and `docs/testing/DB-HO086-issue-387-linux-resource-reobservation.md` record a re-entry defect after #385. Linux ordinary configuration inspection verifies exact declarations but omits the existing neutral protected resource observer. Exact declarations can therefore suppress the protected configuration reconciler when management, image, storage, or networking readiness has degraded; later activation fails without invoking the one bounded repair owner.

Attach the existing protected activity client only at the Linux configuration composition root and pass it through the generic proxy's existing neutral `createResourceObserver` stud. Do not add another probe, endpoint, protocol, repair algorithm, provider identity, host path, command, credential, or fallback. Empty configuration remains a no-op. Qualify ready/unready/malformed/absent evidence, exact client construction, source isolation, preflight, architecture, full suite, doctor, hygiene, and hosted Ubuntu/Windows before closing only #387. No authentication, elevation, protected/provider/VM/guest/repository-code effect is permitted in this slice.

Implementation checkpoint: the Linux configuration composition now attaches the existing configured protected activity client through the generic proxy's neutral resource-observer port. Exact declarations alone can no longer suppress repair when protected management, image, storage, or networking readiness is degraded. Unready, null, forged, or throwing evidence remains bounded and unready; empty configuration performs no resource attachment. No provider identity, second endpoint, repair algorithm, or fallback was added.

Current focused activity/configuration/setup tests pass 81 total / 80 passed / one expected Windows skip. Exact Node 22.16.0 focused configuration/setup passes 82 total / 81 passed / one expected skip; current/exact preflight passes two artifacts / 219 syntax / two JSON / 178 targeted tests; current/exact repository-execution architecture passes 34 total / 33 passed / one expected skip; exact product/setup/standalone integrity passes 14/14; and the exact complete serialized suite passes 1,915 total / 1,894 passed / 21 expected platform skips / zero failures in 192 seconds. Exact doctor is green and truthfully VM-route fail-closed. Commit/push and require exact-head hosted Ubuntu/Windows acceptance before closing #387. No authentication, elevation, protected/provider/VM/guest/repository-code effect occurred.

Hosted implementation checkpoint: [GitHub Actions run 33298113471](https://github.com/iteathen/DevBridge/actions/runs/33298113471) passed Ubuntu smoke/full and Windows bounded-smoke/serialized-full plus doctor on exact implementation `3c0ae99809fcbd8780f90d135a59c4779f4e7f78`. Ubuntu reported 1,915 tests, 1,879 passed, 36 expected platform skips, and zero failures; its architecture gate passed 34/34. Require the documentation-only acceptance head to pass the same matrix, then close only #387. Keep #293 and the protected/physical gates open; hosted CI proves no provider, storage, environment, guest, or C-canary readiness.

Accepted checkpoint: documentation head `9aeacaaff34819c7c49d9050ebb2cba7457d377d` passed the complete Ubuntu/Windows matrix in [GitHub Actions run 33298282461](https://github.com/iteathen/DevBridge/actions/runs/33298282461). Close #387. Keep #293 and the protected/physical gates open.

## No-elevation current-candidate staging

Assessment 2026-08-30: wrapper-owned status proves the stable user installation is healthy but pinned at `ef70f581d8eeb5b39fd378548091de9ba34a16bd`, behind the exact accepted branch head. `docs/testing/DB-HO087-no-elevation-current-candidate-staging.md` records the DB-003/008/009/011/020 and installer ownership audit.

After this plan head passes all four hosted jobs, invoke only the zero-state bootstrap's exact-subject `--install-only` path for the canonical installation home. Verify the manifest-owned component and stable command through wrapper-owned status, then run only installed read-only doctor with the verified example configuration. Do not invoke setup, elevation, protected service/provider/storage, VM/guest, repository execution, a physical canary, or a model adapter.

Installation checkpoint: exact plan head `8cf98654170a7265052481436ecd8e5607cf1c4b` passed all four jobs in [GitHub Actions run 33298603332](https://github.com/iteathen/DevBridge/actions/runs/33298603332). The zero-state bootstrap then installed only that exact manifest-owned component under the canonical user home. Wrapper-owned status independently binds component, selected runner, and pinned runner to the same exact head. Installed read-only doctor is green, coding adapters remain disabled, and repository execution remains truthfully unavailable/fail-closed because no protected route is configured. Require the documentation-only acceptance head to pass before treating this staging checkpoint as complete. No setup, UAC, protected/provider/VM/guest/repository-code effect occurred.

Accepted staging checkpoint: documentation head `9781ac5a7997834cfe7818e8b58bd972d50a193b` passed all four jobs in [GitHub Actions run 33298737554](https://github.com/iteathen/DevBridge/actions/runs/33298737554). DB-HO088 independently reconciles that clean branch/PR state, exact installed candidate, green read-only doctor, and the running protected service's old command line. The service exposes read, mutation, acceptance, and activity capabilities but no configuration capability, so no no-UAC code path can legitimately complete setup. Preserve the live state until an announced administrator window, then perform one supported service refresh/re-entry and immediately continue with the Linux fixed-C canary plus restart repeat. Windows remains separately gated by approved official ISO media. No UAC, provider/VM/guest mutation, repository execution, or model invocation occurred during this reconciliation.

## No-elevation Windows Hyper-V physical preflight

Assessment and accepted result 2026-08-30: `docs/testing/DB-HO089-windows-hyperv-read-only-preflight.md` records the exact provider-owner audit, Microsoft `Get-VMHost`/virtual-switch/IMAPI research, reassessment, and bounded live plan. The installed candidate and current branch contain identical preflight/network/resource code. Focused tests pass 14/14, and two consecutive real ordinary-token invocations report provider, system-managed automatic connectivity, exact keyring, 2 GiB memory admission, and 32 GiB disk/peak-storage admission ready. Host Hyper-V command availability, construction connectivity, keyring presence, and current capacity are therefore not the active blocker. This does not prove a protected configuration endpoint, environment, bridge, guest, route, C canary, or Stage 7 security boundary. Publish and require the hosted matrix; no setup, UAC, protected/provider/network/VM/disk/guest mutation, repository execution, or model invocation occurred.

## No-elevation construction-artifact retirement

Assessment/research 2026-08-30: issue #388 and `docs/testing/DB-HO090-issue-388-construction-artifact-retirement.md` record a newly proven primitive blocker. The Windows protected-image preflight passes provider, system-managed connectivity, and 4 GiB memory admission but fails storage twice: the exact 64 GiB Windows construction profile plus bounded sparse/media policy requires `94,489,280,512` writable bytes and `23,622,320,128` reserve while about `103,262,910,000` bytes are free.

Read-only inventory attributes about 106 GB to the user DevBridge installation, including nine old construction disks, multiple subject-local prepared-media copies, one shared release cache, and the separately protected admitted immutable image. Seven older provider records re-observe as exact owned powered-off VMs. The current provider `discard()` is insufficient: it joins provider/disk removal to best-effort recursive cleanup while outer authority/journal/preparation records remain separate and has no complete DB-009 effect journal.

Implement #388 primitive-first: one neutral path-free classification/plan/authorization/recovery owner; one concrete parent that maps fixed construction topology and protection references; separate exact provider, file, and empty-directory effects; then an explicit local read-only-plan/exact-subject-plus-digest CLI. Delete the old recursive cleanup behavior and add no compatibility path. Protect the admitted image source, current/retained/recoverable/ambiguous subjects, shared cache, and any changed/unexpected/link-indirected artifact. Qualify normal, drift, link/reparse/hard-link, partial-effect, restart, and LEGO behavior before using the product surface for real cleanup. Rerun the exact Windows preflight after cleanup; never lower its resource policy or infer capacity from arithmetic alone. No UAC, protected-service bypass, VM start, guest action, repository execution, model invocation, or GPU/CUDA work is permitted in this slice.

Implementation checkpoint 2026-08-30: the neutral `devbridge/construction-retention-v1` owner, durable journal, exact non-recursive artifact-set adapter, Windows reparse observer, split Hyper-V provider/disk/record effects, concrete production-image topology parent, locally derived current setup authority, and explicit read-only-default CLI are complete. The old combined provider/disk/recursive cleanup route is removed with no legacy alias.

The first real plan caught and corrected one policy defect before mutation: unfinished phase is progress evidence, not supported-recovery selection. The final mapping protects a non-off exact provider, finalization uncertainty, retained state, admitted image references, the derived current authority, an active lease, malformed topology, and artifact drift. Superseded `planned`/`running`/`active` records can be obsolete only after their complete exact plan succeeds.

Initial preflight passed 2 artifacts / 219 syntax / 2 JSON / 178 targeted tests and the complete suite passed 1,944 total / 1,923 passed / 21 expected platform skips / zero failures. A real ordinary-token read-only CLI invocation reported plan digest `73ffa049ac5c4eee9036d4993352b56792757de14c0a171e38832a14e91aba9e`, no active lease, protected current `subject-72966b2f91318dd756cf932aba63b87a`, accepted `subject-8a7a9afe109534b2c128f272ab586bcf`, retained `subject-f7fc5e9be52e957f1b08dff05431a0b3`, and ten obsolete subjects with `67,500,791,071` estimated reclaimable bytes. No mutation occurred, and that digest is now historical evidence rather than current mutation authority.

Hosted run [33303340630](https://github.com/iteathen/DevBridge/actions/runs/33303340630) caught a Linux test fixture forcing Windows path semantics. After that correction, run [33303554286](https://github.com/iteathen/DevBridge/actions/runs/33303554286) passed Ubuntu smoke/full and Windows smoke but caught a real Windows portability defect: path `lstat` and open-handle `stat` can expose timestamp fields at different precision even for the same device/inode object. Node documents filesystem timestamp precision as platform-specific. The reassessed boundary compares before/after path identity through the same observation API, binds the held handle through device/inode, size, regular-file shape, and single-link evidence, and hashes through the held handle. Focused tests accept only cross-API precision variance and still reject before/after path drift.

Corrected local preflight passes 2 artifacts / 219 syntax / 2 JSON / 178 targeted tests. The corrected complete suite passes 1,946 total / 1,925 passed / 21 expected platform skips / zero failures. Corrected implementation `4c994a80a06159fcdb6f146a8ad146cf4a76197f` passed all four hosted jobs plus doctor in [run 33304058711](https://github.com/iteathen/DevBridge/actions/runs/33304058711), including the previously failing Windows serialized suite.

The supported local CLI then retired all ten exact eligible obsolete subjects one current plan digest at a time. Seven six-effect transactions reconciled `67,500,791,071` bytes; three one-effect transactions retired terminal records only. Every data-bearing completion reached durable revision 25, every record-only completion reached revision 5, and a fresh read-only plan followed every subject. Final digest `ccf36efc59e4011d9c965e84a80408596aa0737477c57fe0fb6a4d67814ef15b` contains only the protected current, accepted, and retained subjects, with no active lease and no eligible obsolete subject.

Two repeated ordinary-token Windows protected-image preflights now pass provider, system-managed automatic connectivity, 4 GiB memory, and exact storage admission. The fixed profile requires `118,111,600,640` bytes including reserve; the observations reported `169,682,202,624` and `169,682,096,128` bytes available. Storage is no longer the blocker. No UAC, protected-service mutation, provider bypass, VM start, guest action, repository execution, model invocation, or GPU/CUDA action occurred.

The live data-bearing CLI transactions expose no bounded phase progress before their terminal JSON result. Issue #389 separately tracks this DB-019 path-free liveness gap; do not weaken exact hashing, one-subject authorization, or effect reconciliation. After the documentation head passes all four hosted jobs, close #388 and continue from the primitive protected-service/physical VM frontier.

## No-elevation current candidate staging after artifact retirement

Assessment/research/reassessment 2026-08-30: issue #388 is closed after exact cleanup and capacity proof. The active isolated branch is clean and remote-equal at `490f6d217161df3b1be371c1be2a57f2c04da64e`, which passed all four hosted jobs plus doctor in [run 33306583185](https://github.com/iteathen/DevBridge/actions/runs/33306583185). Wrapper-owned observation still binds the manifest-owned user installation's component, selected runner, and pinned runner to `8cf98654170a7265052481436ecd8e5607cf1c4b`. Installed doctor is green but truthfully reports no route, zero declarations/environments, model adapters disabled, and repository execution unavailable/fail-closed.

`docs/testing/DB-HO091-no-elevation-current-candidate-staging.md` records the DB-003/008/009/011/020 and installer ownership audit. Commit/push the plan and require its exact hosted matrix. Then use only the zero-state bootstrap's exact-head `--install-only` transaction, independently re-observe wrapper-owned identities and installed read-only doctor, document the result, and require the documentation head matrix. Do not invoke setup, UAC, protected-service/provider/VM/guest mutation, repository execution, a physical canary, a model adapter, or GPU/CUDA work.

Implementation checkpoint: plan head `d7d35e6d0a7b7b6e73326ee9155049447a7a9276` passed all four hosted jobs plus doctor in [run 33306793905](https://github.com/iteathen/DevBridge/actions/runs/33306793905). The supported zero-state `--install-only` transaction then staged that exact commit under the canonical user home. Wrapper-owned status independently binds component, selected runner, and pinned runner to the same head. Installed read-only doctor remains green and truthfully reports execution disabled, repository execution unavailable/fail-closed, lifecycle `setup-reentry-required`, zero declarations/environments, and model adapters disabled. No setup, UAC, protected-service/provider/VM/guest effect, repository execution, physical canary, model invocation, or GPU/CUDA work occurred. Require the documentation-head hosted matrix before accepting this checkpoint.

## Bounded local construction-retirement liveness

Assessment/research/reassessment 2026-08-30: issue #389 and `docs/testing/DB-HO092-issue-389-retirement-liveness.md` record the DB-019 gap observed during exact cleanup. Healthy data-bearing retirements were silent for minutes because exact plan/hash work occurs while the neutral retention owner awaits its injected source, before an effect adapter can report a phase.

Add one optional topology-free progress stud to the neutral owner and one separate import-free local liveness projector. The owner may expose only phase, completed/total effect counts, and bounded attempt count before planning and after durable saves. The projector must rebuild a fixed path-free record, use one cleared/unref'ed interval, emit only to stderr, suppress output after backpressure/failure, and stop in `finally`; stdout remains one terminal JSON result. Observation has no authority and cannot affect plan confirmation, hashing, retry, ambiguity, ordering, result, or error.

Implement primitive-first and qualify normal, slow-I/O, no-progress, failure, restart, bounded-output, and LEGO isolation behavior, followed by preflight, architecture/product gates, full serialized suite, doctor, hygiene, and exact-head hosted Ubuntu/Windows. Do not invoke setup, elevation/UAC, protected/provider/VM/guest effects, repository execution, models, or GPU/CUDA work. Close only #389 after complete acceptance.

Implementation checkpoint: plan head `5cd8372960f3667d4c0919c4e7cd1cf937c3c403` passed all four jobs in [run 33307245004](https://github.com/iteathen/DevBridge/actions/runs/33307245004). Exact implementation `a061110bc37f926b802f09e7e384dcd1860c9f26` adds one optional four-field progress stud to the neutral retention owner, one import-free timer/backpressure liveness owner, and one thin CLI composition that keeps status on stderr and one terminal JSON result on stdout. Observation exceptions, rejections, malformed updates, and output failure cannot alter the transaction; backpressure stops further status writes; the single interval is unref'ed and cleared in `finally`.

Focused liveness/retention tests pass 27/27; preflight passes two standalone artifacts / 219 syntax / two JSON / 178 targeted tests; repository-execution architecture plus product/standalone gates pass 37 total / 36 passed / one expected Windows symlink skip; and the full serialized suite passes 1,957 total / 1,936 passed / 21 expected skips / zero failures in 190 seconds. Doctor remains green and VM-route fail-closed. A real ordinary-token read-only retention inspection emitted bounded liveness and returned the unchanged final protected-only digest with no eligible obsolete subject. Hosted [run 33307730400](https://github.com/iteathen/DevBridge/actions/runs/33307730400) passed Ubuntu/Windows smoke/full plus doctor on the exact implementation. Commit/push this durable evidence, require the documentation-head matrix, then close only #389 and return to the protected-service/physical VM frontier. No UAC, protected/provider/VM/guest mutation, repository execution, model invocation, or GPU/CUDA action occurred.

## No-elevation CI action-runtime supply-chain maintenance

Assessment/research/reassessment 2026-08-30: issue #390 and `docs/testing/DB-HO093-issue-390-node24-actions.md` record a low-level evidence-pipeline defect visible in every otherwise-green hosted job. The exact pinned checkout/setup-node releases declare Node 20, while GitHub's hosted runner now forces them onto Node 24 and warns that users must update to Node-24 action releases.

Use only the official immutable subjects: checkout v7.0.1 at `3d3c42e5aac5ba805825da76410c181273ba90b1` and setup-node v7.0.0 at `820762786026740c76f36085b0efc47a31fe5020`. Preserve exact SHA pins and all existing job/test semantics. Explicitly disable checkout credential persistence and setup-node automatic package caching because no later step owns either capability. Add a workflow-contract regression test, run focused/preflight/architecture/product/full gates, and require all four hosted jobs to pass without the Node-20 warning before closing only #390. This independent CI primitive does not replace or weaken the still-pending administrator-authorized protected-service refresh.

Implementation checkpoint: plan head `4e539578e161619252fcecd0fdf789b28b0fe4eb` passed all four old-action jobs in [run 33308240607](https://github.com/iteathen/DevBridge/actions/runs/33308240607) and reproduced the warning. Exact implementation `1254170582f849429a45c3a1ea0b415f16cb7d06` pins the two official Node-24 action subjects, disables checkout credential persistence and setup-node automatic caches in both jobs, and adds one exact workflow contract without changing tested Node, permissions, triggers, matrices, timeouts, or commands. Focused tests pass 5/5; preflight passes 2 artifacts / 219 syntax / 2 JSON / 178 targeted files; architecture/product/standalone gates pass 36 with one expected Windows symlink skip; and the complete serialized suite passes 1,958 total / 1,937 passed / 21 expected skips / zero failures. Doctor remains green and VM-route fail-closed. [Hosted run 33308595425](https://github.com/iteathen/DevBridge/actions/runs/33308595425) passed all four jobs, and every check reports zero annotations. Commit/push the durable evidence, require its documentation-head matrix, then close only #390 and return to the protected-service/physical VM frontier.

## Exact candidate restaging after accepted no-elevation maintenance

Assessment/research/reassessment 2026-08-30: `docs/testing/DB-HO094-no-elevation-current-candidate-restaging.md` records the exact installation ownership and unchanged-mechanism audit. The isolated branch is clean and remote-equal at accepted head `ffbdcafefbb891e25a517eaaacfb1effbc301129`; its four hosted checks passed with zero annotations in [run 33308721004](https://github.com/iteathen/DevBridge/actions/runs/33308721004). The canonical manifest-owned installation remains at the older accepted DB-HO091 subject. Since then, installer-owned runtime changes add only the neutral construction-retirement liveness path; the zero-state bootstrap, Permanent Entry publication, and setup separation are unchanged.

Commit/push this plan and require its exact hosted matrix. Then invoke only the exact plan head through the supported zero-state `--install-only` transaction, independently verify the bounded install result, wrapper-owned identities, command resolution, and installed read-only doctor, and publish the evidence. Do not invoke setup/authentication, UAC, the protected service, a provider/image/environment/VM/guest, repository execution, a model adapter, or GPU/CUDA work. The next physical dependency remains one later administrator-authorized protected-service refresh, followed by route/guest/Linux fixed-C proof and its restart repeat; Windows remains independently gated by accepted official media/profile readiness.

Installation checkpoint: exact plan head `b535a5d2ce04a420ac3e0f559be712009747c8e2` passed all four hosted jobs plus doctor in [run 33308965436](https://github.com/iteathen/DevBridge/actions/runs/33308965436). The zero-state `--install-only` transaction selected only that exact subject. Its bounded result and independent wrapper status bind component, selected runner, and pinned runner to the same head; both manifest-owned commands resolve below the canonical user installation. The installed command reverified and launched a clean exact checkout. Doctor against that checkout's verified example config exits zero with `ok: true`, GitHub authentication available, execution disabled, repository execution unavailable/fail-closed, lifecycle `setup-reentry-required`, zero declarations/environments, and coding adapters disabled.

An initial doctor attempt against the pre-existing Stage-0 runtime subtree's stale example config was rejected for its removed singular repository field. It did not change runner identity or produce an execution fallback. Preserve that unselected pre-existing data until an exact manifest-owned retirement plan can prove authority; do not erase it speculatively. Publish this evidence and require the documentation-head matrix. No setup/authentication, UAC, protected/provider/VM/guest effect, repository execution, model invocation, or GPU/CUDA action occurred.

## Stage-8 application inventory and exact removal primitive

Assessment/research/reassessment 2026-08-30: issue #391 and `docs/testing/DB-HO095-issue-391-application-removal-primitive.md` record the missing foundational ownership/uninstall contract. The repository already has exact installed-component manifests, an exact non-recursive artifact-set remover, serialized/flushed state replacement, and provider/environment lifecycle adapters. It does not have one versioned application inventory, created/adopted/foreign provenance, separate application-removal/full-purge plans, cross-owner DB-009 journal/report, or supported uninstall command.

The accepted runtime owns mode, confirmation, planning, orchestration, and reporting. Other layers publish neutral receipts/effects through replaceable adapters. The neutral owner receives no provider/repository/permanent-entry implementation/VM/domain/disk/path identity. Both modes require literal `REMOVE` plus the exact current plan digest. Foreign, ambiguous, referenced, current/LKG-required, unregistered, or changed state is preserved. Every effect is rebound and re-observed before mutation; filesystem cleanup reuses exact artifact-set identity/reparse protections and never recursively sweeps a broad root.

Implement the neutral protocol, read-only deterministic planner, confirmation binding, and durable planned/attempted/observed/reconciled coordinator first, with fake-port recovery/boundary tests and no production mutation route. Then attach exact filesystem and ownership producers one at a time, and expose a mode in the CLI only after a completeness gate proves every required producer is registered. Preserve the legacy Stage-0 tree until #159 supplies exact retirement authority. Qualify application-only removal/recovery before provider-backed full purge; full Hyper-V and KVM/libvirt purge evidence remains a real-host gate. No live removal, setup/elevation, protected/provider/VM/guest effect, repository execution, model invocation, or GPU/CUDA work is authorized by this plan.

First implementation checkpoint: exact commit `2bbc9d71acb4cdd1a2299e4a2781d4fb30421879` implements the isolated neutral protocol, deterministic effect-opaque planner, literal-confirmation/digest binding, and port-driven DB-009 coordinator. It bounds total effects before normalization; treats only created or locally adopted unprotected state as eligible; preserves foreign, referenced, incomplete, active, out-of-mode, and dependency-blocked state; rejects corrupt/noncontiguous journals; rebuilds the plan before every effect; and reconciles interrupted exact absence without replay. Module-isolation tests prohibit topology and foreign owner names.

Final-byte local evidence passes 15/15 focused tests on current and exact Node 22.16.0, preflight at 2 artifacts / 223 syntax / 2 JSON / 180 targeted tests, architecture/product/standalone at 37 total / 36 passed / 1 expected Windows symlink skip, and the complete exact-Node serialized suite at 1,973 total / 1,952 passed / 21 expected skips / zero failures. Standalone artifacts and diff hygiene are clean; doctor remains green and VM-route fail-closed. [Hosted run 33310910845](https://github.com/iteathen/DevBridge/actions/runs/33310910845) passed all four Ubuntu/Windows jobs plus doctor on that exact implementation.

This checkpoint creates no production journal store, artifact adapter, producer inventory, CLI route, or deletion authority. Implement the durable journal adapter next, then one exact artifact-effect adapter and producer at a time. Keep #391 open until producer coverage and supported application-only removal are complete; full purge remains separately gated by real Hyper-V and KVM/libvirt evidence. No live removal, setup/elevation, protected/provider/VM/guest effect, repository execution, model invocation, or GPU/CUDA action occurred.

Durable-journal assessment/research/reassessment 2026-08-30: `docs/testing/DB-HO098-issue-391-removal-journal-persistence.md` records two gaps below the accepted coordinator. Revisioned records need compare-and-replace inside OS-local serialization, and admission must cover the complete observe/attempt/reconcile loop so concurrent local callers cannot both perform one effect after accepting the same idempotent phase record. The reusable exclusive-mutation and flushed exact-record bricks are neutral internally but incorrectly live beneath a setup-specific source path; move them to neutral state ownership and delete the old copies.

Implement a generic subject-bound `run(subject, operation)` record store, not an application-aware persistence module. Its session owns fresh load, absent-to-revision-1 and exact +1 replacement, identical accepted-write reconciliation, corrupt/stale/conflicting rejection, flush/rename/exact-reread, and local contention. Change the coordinator's injected journal stud to use that exclusive session; keep record schema/effect recovery in the coordinator and filesystem/locking details in state. Prove fresh-process restart, concurrent admission, stale revision, corrupt root, idempotent save, and lock-release behavior before the exact artifact adapter. Do not add a compatibility journal path, production CLI, inventory producer, deletion effect, or live mutation.

Durable-journal implementation checkpoint: plan head `12f3a37e14b979fc3eda47fa3f4f55078f952ffd` passed all four hosted jobs plus doctor in [run 33312776472](https://github.com/iteathen/DevBridge/actions/runs/33312776472). The neutral exclusive-mutation and exact JSON-record bricks now live directly under state ownership, the setup authority adapter consumes them there, and the old setup-nested files are deleted with no forwarding copies. Valid JSON non-object roots now fail closed rather than becoming empty state.

The new generic revisioned record store owns one subject-bound exclusive `run` session, exact JSON/revision validation, absent-to-1 and exact +1 acceptance, identical accepted-write reconciliation, stale/conflicting/skipped rejection, and flushed replace/reread. The application-removal coordinator consumes only that session stud and keeps its complete observe/attempt/reconcile loop within local admission. Tests prove exact fresh-process persistence, cross-instance contention, lock release, corrupt-state rejection, interrupted-effect recovery without replay, and one effect attempt across two competing coordinators.

Final local evidence passes 33/33 focused tests on current and exact Node 22.16.0, preflight at 2 artifacts / 226 syntax / 2 JSON / 183 targeted tests, architecture/product/standalone at 37 total / 36 passed / one expected Windows symlink skip, and the complete exact-Node serialized suite at 1,983 total / 1,962 passed / 21 expected skips / zero failures in 189.3 seconds. Standalone artifacts and diff hygiene are clean; doctor remains green and repository execution unavailable/fail-closed. Commit/push and require the exact implementation matrix. Keep #391 open and proceed next to the exact artifact-effect adapter plus first neutral producer only after hosted acceptance. No production removal composition, CLI, live effect, setup/elevation, protected/provider/VM/guest action, repository execution, model invocation, or GPU/CUDA work occurred.

Hosted acceptance: exact implementation `5ed56c5752e10a48cf5cd4a61f6dab5fb0805140` passed all four Ubuntu/Windows smoke/full jobs plus doctor in [run 33314309896](https://github.com/iteathen/DevBridge/actions/runs/33314309896). The durable journal is accepted. Keep #391 open and assess the exact artifact-effect adapter plus first neutral producer next; do not infer CLI or live removal readiness from this lower-brick acceptance.

Artifact-binding assessment/research/reassessment 2026-08-30: `docs/testing/DB-HO099-issue-391-artifact-inventory-binding.md` records the next primitive boundary. Exact discovery currently omits content SHA-256 even though the held-handle observer can measure it; a concrete effect cannot expose its path-bearing manifest through the neutral coordinator; and a live-tree producer cannot reconstruct the same plan after an interrupted effect has already removed an entry. Persist the complete private descriptor before the first attempt and project its frozen neutral receipt across restart/partial absence.

Strengthen exact discovery first, then add a neutral required-contributor aggregator, an import-free catalog-to-action bridge, and one import-isolated exact-artifact inventory contributor. The contributor owns only local provenance/activity/record/action ports; the bridge preserves exact neutral binding fields and hides private descriptors. Compose them only against a disposable installed-component fixture and the existing verifier in tests. Keep application/purge coverage incomplete in production, add no CLI or live removal route, and defer installer-lock/wrapper/retained-tree composition plus receipt retirement/operation rotation to the next assessment. No setup/elevation, protected/provider/VM/guest mutation, repository execution, model invocation, or GPU/CUDA work is authorized.

Artifact-binding implementation candidate: exact-artifact discovery now records held-handle SHA-256 and validates supplied digests; a neutral application-owned source derives deterministic aggregate generations and completeness from locally required contributor identities; an import-free bridge keeps private descriptors behind catalog/action ports; and an import-isolated inventory contributor freezes the complete descriptor in a revisioned record before effects. Restart projects the same neutral receipt after exact absence instead of reconstructing authority from a partial tree. The only integration effect removes an independently verified component installed into a disposable temporary home.

Current and exact Node 22.16.0 focused evidence passes 44/44 on each runtime. A pre-commit hygiene review found and corrected the explicit preflight inventory before commit; its final-byte run passes 2 artifacts / 230 syntax / 2 JSON / 188 targeted files, now including every changed source and regression. Architecture/product/standalone passes 37 total / 36 passed / one expected Windows symlink skip, and the exact complete serialized suite passes 1,995 total / 1,974 passed / 21 expected skips / zero failures in 193.5 seconds. Doctor is green, coding adapters remain disabled, repository execution remains unavailable/fail-closed, and standalone/diff hygiene is clean. Commit/push the isolated candidate and require the exact-head four-job hosted matrix. Keep #391 open. Production contributor registration, mutation locking, wrapper/staging/quarantine/retained-tree ownership, receipt retirement/operation rotation, complete modes, CLI, and live removal remain absent. No UAC or protected/physical/GPU effect occurred.

Hosted acceptance: exact implementation `34cdada887a8f327e77e08c5ea380c06ecb01a42` passed all four Ubuntu/Windows smoke/full jobs plus doctor in [run 33316264686](https://github.com/iteathen/DevBridge/actions/runs/33316264686). Accept this exact-artifact inventory/binding primitive, keep #391 open, and assess Permanent Entry production composition next. The documentation head must pass the same matrix before further implementation. Production removal, complete mode coverage, receipt rotation/retirement, setup/elevation, protected/provider/VM/guest effects, repository execution, model invocation, and GPU/CUDA work remain absent.

Permanent Entry receipt assessment/research/reassessment 2026-08-30: `docs/testing/DB-HO100-issue-391-permanent-entry-ownership-receipts.md` records the next primitive prerequisite. Per-component manifests bind current component bytes, but there is no installation-wide ownership history; entry wrappers have no sidecar receipt; staging/quarantine may hold interruption or displaced-object evidence; and retained generations cannot be inferred obsolete from their names, age, or current selection. Reusing a static item/journal identity would also make a later reinstall of identical bytes collide with an immutable completed removal receipt.

Implement one import-isolated neutral append-only receipt journal before attaching the live installer. Each immutable record carries a random installation epoch, contiguous revision, previous-record SHA-256, deterministic generation, and bounded sorted created/adopted items with private exact-JSON values. Read must reject extra/gapped/noncanonical/link-indirected/corrupt history. Acceptance must use flushed same-volume scratch, create-if-absent hard-link publication, exact cleanup, exact reread, bounded contention reconciliation, idempotence, and a fresh epoch only for a truly absent recreated journal. Do not claim filesystem-independent power-loss atomicity.

This slice does not decide adoption, inspect or mutate the live installation, wire a production producer, retire a receipt, rotate the application-removal journal, expose CLI, or remove anything. After hosted acceptance, separately assess exact static adoption, wrapper/component/retained-tree grouping, installer mutation activity, receipt self-inventory, and operation rotation. Preserve legacy Stage-0, staging, quarantine, configuration, provider/environment state, and every unregistered or ambiguous artifact.

Permanent Entry receipt implementation candidate: the import-isolated exact-artifact receipt journal now accepts and rereads immutable canonical revisions through neutral `read`/`accept` ports. One journal epoch, contiguous revision, previous-record digest, deterministic generation, bounded sorted created/adopted items, and frozen exact-JSON private values form the local contract. Publication uses an exclusive flushed scratch file and create-if-absent hard link; identical and competing proposals reconcile within a fixed bound, while extra/gapped/noncanonical/aliased/changed history fails closed. A freshly recreated journal rotates its epoch even for identical artifacts.

Final local evidence passes 15/15 focused tests on current and exact Node 22.16.0, preflight at 2 artifacts / 231 syntax / 2 JSON / 190 targeted files, architecture/product/standalone at 37 total / 36 passed / one expected Windows symlink skip, and the complete exact-Node serialized suite at 2,010 total / 1,989 passed / 21 expected skips / zero failures in 192.6 seconds. Exact doctor is green, model adapters remain disabled, and repository execution remains unavailable/fail-closed. Standalone regeneration and diff hygiene are clean. Commit/push the isolated candidate and require exact-head hosted Ubuntu/Windows acceptance. Keep #391 open; producer/adoption policy, live installer composition, receipt retirement/operation rotation, complete removal modes, CLI, and all protected/physical/GPU effects remain absent.

Hosted correction gate: run 33318238507 rejected candidate `0e95df9af390063940a6b76040f6032efdd7082e`. Ubuntu passed; the new receipt tests alone failed in both Windows jobs because the runner supplied the valid short path spelling `RUNNER~1` and `realpath()` returned its long spelling. DB-HO100 records the primary-source reassessment. Preserve strict POSIX equality; on Windows, accept alternate spelling only after both complete component chains are link-free and both endpoints prove the same nonzero filesystem identity. Requalify locally and on a fresh exact hosted head; do not count the rejected run as acceptance.

Corrected local candidate evidence passes 15/15 focused tests on current/exact Node 22.16.0, preflight at 2/231/2/190, architecture/product/standalone at 36 plus one expected skip, and the complete exact serialized suite at 2,010 total / 1,989 passed / 21 expected skips / zero failures in 194.9 seconds. Doctor remains green and route fail-closed; regeneration/diff hygiene pass. Push the isolated correction and require all four jobs on the new exact head, with Windows runner short-path use as the decisive regression observation.

Hosted acceptance: exact corrected implementation `a4fa9b1b9a9aadebeb524af35b630dc3701574bf` passed Ubuntu smoke/full and Windows bounded-smoke/serialized-full plus doctor in [run 33318685645](https://github.com/iteathen/DevBridge/actions/runs/33318685645). The same Windows `RUNNER~1` environment that rejected the predecessor now passes both jobs. Accept the receipt primitive, require the documentation-head matrix, keep #391 open, and assess the production Permanent Entry producer/installer-lock composition next. Do not infer complete uninstall or any live removal authority.

## Windows contract-child hard-timeout correction

Assessment/research/reassessment 2026-08-30: issue #392 and `docs/testing/DB-HO096-issue-392-windows-contract-timeout.md` record the documentation-head Windows failure from run 33311082103. One fixed PowerShell contract child hit its exact 20-second timeout and therefore returned a null signal-termination exit code; the other 1,972 tests completed. Three exact local repetitions finish in about 1.04 seconds, and the preceding implementation head passed all four hosted jobs, but the supported hosted observation proves that 20 seconds is not a reliable hard bound.

Change only this test child to a named 60-second hard timeout and assert non-timeout explicitly before existing exit/output claims. Keep the workflow's six-minute suite bound and every production timeout/behavior unchanged. Qualify focused repetitions, preflight, architecture/product/standalone, complete serialized suite, doctor, hygiene, and both exact-head hosted matrices before closing #392. Then resume #391 at its durable journal adapter. No setup/elevation, provider/VM/guest effect, repository execution, model invocation, or GPU/CUDA work is permitted.

Implementation checkpoint: plan head `f3220c1ea59064267681de5d5c9163321fd17664` passed all four jobs in run 33311355643. Exact correction `342784b573e830baa088adbf0fec7336ee926286` changes only the fixed PowerShell contract test's child hard bound from 20 to 60 seconds and adds explicit non-timeout assertions; production timeouts/behavior and workflow deadlines are unchanged. Five focused repetitions pass, exact preflight remains 2/223/2/180, architecture/product/standalone passes 36 plus one expected skip, and the complete serialized suite passes 1,973 with zero failures. An independent smoke failure exposed #393 rather than invalidating the corrected full-suite behavior; combined exact head `39e676b7491eb9c5f6bd5ae6ec6461624b67554a` passed the full four-job matrix twice in run 33312155273. Require documentation-head acceptance, then close #392.

## Guest capability-probe admission correction

Assessment/research/reassessment 2026-08-30: issues #290/#393 and `docs/testing/DB-HO097-issue-393-capability-probe-admission.md` record a separate failure exposed while accepting #392. Exact head `342784b573e830baa088adbf0fec7336ee926286` passed Ubuntu smoke/full and Windows serialized full/doctor in run 33311577191. Windows bounded smoke completed all 968 preflight tests but failed the real CMake health check; the same file passed serially on the same SHA.

The guest helper owns up to 64 requested capability identities but launches every known tool probe at once through `Promise.all`. CI test-file concurrency cannot bound that internal child-process burst. Keep the fixed tool identities/arguments, eight-second per-probe timeout, response, Windows two-file preflight policy, and host/guest security boundary unchanged. Add one import-free neutral ordered-observation stud inside the standalone helper and admit exactly one capability observation at a time while independent state/network observations remain unchanged.

Qualify direct ordering/max-active/rejection behavior, the real baseline, repeated bounded and ordinary preflights, architecture/product/standalone, complete serialized suite, doctor, hygiene, and repeated complete hosted matrices. Close #393 and re-close #290 only after repeated exact-head acceptance; the same matrix may accept #392. Then resume #391 at the durable removal journal. No setup/elevation, provider/VM/guest mutation, repository execution through DevBridge, model invocation, timeout widening, fallback, or GPU/CUDA work is permitted.

Implementation checkpoint: exact commit `39e676b7491eb9c5f6bd5ae6ec6461624b67554a` replaces only capability `Promise.all` with one bounded ordered-observation stud inside the standalone guest helper. Direct tests prove max-active one, stable order, stop-on-rejection, and strict local inputs; existing real baseline, restart, secret, and unknown-capability contracts remain green. No registry, executable, argv, path, provider, host, CI identity, timeout, response, workflow bound, or fallback crosses the stud.

Focused current/exact tests pass 6/6; two bounded and one ordinary exact preflight pass 2 artifacts / 223 syntax / 2 JSON / 180 targeted files; architecture/product/standalone passes 37 total / 36 passed / one expected skip; and the complete serialized suite passes 1,975 total / 1,954 passed / 21 expected skips / zero failures. Doctor is green and VM-route fail-closed; artifact/diff hygiene passes. Plan run 33311849489 and both complete attempts of implementation run 33312155273 pass all four jobs. Require the documentation-head matrix, then close #393, re-close #290, close #392, and resume #391 at its durable journal adapter. No protected/physical or GPU/CUDA effect occurred.

## Receipt generation CAS before production application-inventory producers

Assessment/research 2026-08-30: `docs/testing/DB-HO101-issue-391-receipt-generation-cas.md` records a primitive gap exposed before Permanent Entry production composition. The accepted append-only receipt journal's `accept(items)` is a complete-list convergence operation. Two independent producers that merge from the same observed generation could both succeed sequentially and the later list could erase the earlier producer's receipt. The Permanent Entry installation lock cannot become a global cross-owner lock without leaking topology.

Add one neutral `compareAndAccept({ generation, items })` operation. It requires the exact current generation, treats `null` as an empty journal, performs at most one numbered create-if-absent publication attempt, and reports the exact observed winner on contention without overwriting it. It performs no caller callback or merge. Corrupt/ambiguous history continues to fail closed. Keep unconditional `accept(items)` explicitly scoped to a sole owner supplying the complete list.

Qualify empty/current/stale/contention/restart/corruption behavior on current and exact Node 22.16, then run preflight, import isolation, architecture/product/standalone gates, the complete serialized suite, doctor, hygiene, and hosted Ubuntu/Windows. No production receipt, installer contract change, live adoption/removal, CLI, setup/UAC, protected/provider/VM/guest, repository execution, model, or GPU/CUDA action belongs to this slice.

After acceptance, compose the Permanent Entry producer with pre-created path reservations, neutral created/adopted/retained observations, exact component trees, sparse wrapper files, installer activity, intentional generation retention, and conditional receipt merge. Preserve unverified quarantine and receipt self-state until their separately journaled retirement/rotation contracts exist; do not expose uninstall while producer coverage is incomplete.

Receipt-generation-CAS acceptance checkpoint 2026-08-30: exact implementation `2df65cd999b817f32e9025c673af9cc3c3d1f62f` adds a strict expected-generation `compareAndAccept` operation with explicit empty-state admission, exact-current idempotence, one bounded immutable publication attempt, winner preservation on contention, and unchanged fail-closed corrupt-history behavior. The journal remains import-isolated and topology-neutral; unconditional `accept` remains the sole-owner complete-list convergence operation.

Focused current/exact Node 22.16.0 tests pass 18/18, both preflights pass 2 artifacts / 231 syntax / 2 JSON / 190 targeted files, architecture/product/standalone passes 39 plus one expected Windows symlink skip, and the exact serialized suite passes 1,992 with 21 expected skips and zero failures. Doctor is green and repository execution remains unavailable/fail-closed. Plan [run 33319303628](https://github.com/iteathen/DevBridge/actions/runs/33319303628) and implementation [run 33319623210](https://github.com/iteathen/DevBridge/actions/runs/33319623210) each pass all four Ubuntu/Windows jobs plus doctor.

Accept this primitive and keep #391 open. The next ownership slice is production Permanent Entry receipt composition: exact pre-effect reservations, neutral mutation observations, exact component trees, sparse wrapper files, installer activity, retained-generation policy, and bounded conditional merge. It must still add no uninstall CLI or removal authority, infer no ownership for unverified quarantine, and preserve receipt/control self-state until terminal retirement and operation rotation are separately designed and accepted.

Transitive-standalone assessment/research/reassessment 2026-08-30: `docs/testing/DB-HO102-issue-391-transitive-standalone-closure.md` records a packaging prerequisite below production receipt composition. The standalone compiler currently rewrites only the entry module's direct imports and requires every embedded child to import Node built-ins only. The accepted exact-artifact brick has its own neutral local dependency. Flattening it, copying identity logic, or inventing an installer-specific descriptor would violate the LEGO boundary to accommodate a packaging limitation.

Replace the flat child list with one closed logical module-graph contract. The compiler receives an entry identity/bytes and a narrow local-edge loader, recursively compiles every relative dependency to an exact data URL, caches exact logical identities, and rejects missing, cyclic, conflicting, duplicate, package/bare, or unsupported-scheme graphs. It receives no filesystem or topology identity. The builder alone resolves edges beneath the repository source root and rejects escape/non-file targets. Remove the old flat contract rather than preserve a compatibility path.

Qualify nested/shared determinism and failure boundaries, regenerate both standalone entry stages, prove copied help execution and reproducibility, then run focused current/exact Node 22.16, preflight, architecture/product/standalone, the complete exact serialized suite, doctor, hygiene, and hosted Ubuntu/Windows. This slice changes no installer API and creates no receipt, adoption, removal authority, setup/UAC, protected/provider/VM/guest effect, repository execution, model invocation, or GPU/CUDA work. After acceptance, make installation asynchronous and compose receipts through the intact accepted modules.

Transitive-standalone implementation candidate: the compiler now owns only a bounded logical module graph and recursively encodes every relative edge bottom-up through one injected loader. It caches exact logical identities and rejects missing, cyclic, conflicting, duplicate, package/bare, unsupported-scheme, and extensionless graphs. A separate filesystem adapter contains logical reads beneath one canonical root, rejects escape, link indirection, and non-file targets, and returns no host topology to the compiler. The old flat one-level contract is deleted with no compatibility path. The existing generated artifacts remain byte-identical because their current graphs are shallow, while nested/shared execution and all failure boundaries are now pinned.

Final local evidence passes focused current/exact tests 4/4, both preflights at 2 artifacts / 232 syntax / 2 JSON / 190 targeted tests, architecture/product/standalone at 36 plus one expected Windows symlink skip, and the exact complete serialized suite at 2,015 total / 1,994 passed / 21 expected skips / zero failures in 197.2 seconds. Exact doctor is green, execution is disabled, repository execution remains unavailable/fail-closed, and coding-model adapters remain disabled. Standalone regeneration and diff hygiene pass. Commit/push the isolated candidate and require its exact-head four-job hosted matrix before accepting the prerequisite or starting live receipt composition. No installer, receipt, removal, setup/UAC, protected/physical, repository-execution, model, or GPU/CUDA effect occurred.

Hosted acceptance: exact transitive-packaging implementation `f26982c0477276c1ee555bda9abea6647a8d7f79` passed all four Ubuntu/Windows jobs, architecture gates, standalone regression, and doctor in [run 33321350511](https://github.com/iteathen/DevBridge/actions/runs/33321350511). Accept the prerequisite, keep #391 open, and proceed to asynchronous Permanent Entry installation plus production receipt composition. Do not infer uninstall or live-removal readiness from this lower-level acceptance.

## Permanent Entry production ownership receipts

Assessment/research/reassessment 2026-08-30: `docs/testing/DB-HO103-issue-391-permanent-entry-production-receipts.md` records the exact production gap after accepted transitive packaging. Post-hoc receipts are not truthful: staging/wrapper temporary names are chosen inside mutation leaves, wrapper rename overwrites an existing target, leaves return no created/adopted/retained observations, the lease exposes no read-only activity, and journal generation CAS deliberately does no multi-owner merge.

Implement a topology-neutral declarative receipt-collection CAS first. Then add installer-local protected anchor/reservation/completion state, reserve every exact temporary/quarantine name before creation, make component and wrapper leaves return neutral observations, statically adopt only exact publications, preserve unrelated/older receipts, record exact component trees and sparse wrapper files, and make installation async through every caller with no compatibility path. Arbitrary/ambiguous wrapper state fails closed; quarantine and unverified historical state remain preserved and unregistered.

Qualify normal, adoption, no-op, advancement/retention, interruption, contention, foreign/indirect state, activity, sparse-root isolation, and LEGO boundaries, followed by regeneration, preflight, architecture/product/standalone gates, the exact complete suite, doctor, hygiene, and hosted Ubuntu/Windows. Use only disposable installation homes. Do not expose an inventory contributor or uninstall CLI, perform deletion/live canonical mutation, run setup/UAC or protected/provider/VM/guest actions, execute repository code, invoke a model, or implement GPU/CUDA work. After acceptance, attach the read-only contributor and finish application-mode coverage before separately designing receipt retirement and operation rotation.

Implementation candidate: the installer now owns one protected immutable receipt journal and edits it through a new topology-neutral item-level conditional collection. Separate import-isolated tree/file ownership bricks classify exact created/adopted/retained publications, preserve unrelated and older component receipts, reserve every exact work/stage/preservation name before effects, and complete only after exact artifact rediscovery. Component, wrapper, and lease-observation leaves read accepted bytes through held descriptors with before/held/after identity checks; component/wrapper leaves receive all temporary names from their caller and refuse indirect or multiply linked state. Permanent Entry supplies only the local topology edge and Windows reparse observation. Installation and every production caller are async with no unreceipted compatibility route; the installer lease adds read-only activity observation only.

Final-byte local evidence passes the focused ownership/installer boundaries, current Node 24.15.0 and exact Node 22.16.0 preflight at 2 standalone artifacts / 236 syntax files / 2 JSON files / 194 targeted tests, architecture/product/standalone/LEGO gates at 46 total / 45 passed / one expected Windows symlink skip, and the complete exact serialized suite at 2,036 total / 2,015 passed / 21 expected platform skips / zero failures in 232.8 seconds. Exact doctor is green, coding adapters are disabled, and repository execution remains unavailable/fail-closed without a configured persistent-environment route. Standalone regeneration and diff hygiene pass. Commit/push the isolated candidate and require the exact-head hosted Ubuntu/Windows matrix. Keep #391 open; no removal contributor/CLI, canonical-install mutation, setup/elevation, protected/provider/VM/guest effect, repository execution, model invocation, or GPU/CUDA work occurred.

Hosted acceptance: exact production ownership implementation `7da445fb5adafdf1ee55396623f7e9ad386394ad` passed all four Ubuntu/Windows smoke/full jobs plus doctor in [run 33325147542](https://github.com/iteathen/DevBridge/actions/runs/33325147542). Accept the receipt writer, keep #391 open, and proceed to the receipt-backed read-only application-removal contributor and complete application-mode coverage. Do not expose deletion or an uninstall CLI until terminal receipt retirement and removal-operation rotation are separately designed and accepted.

Application-payload inventory assessment/research/reassessment 2026-08-30: `docs/testing/DB-HO104-issue-391-application-payload-inventory.md` records the exact next dependency. The immutable production receipts cover all installed entry component generations and sparse launch files, but the accepted single-item inventory cannot project that dynamic set directly. More importantly, first normal use creates separately owned replaceable runner/runtime cache materialization that is not yet receipted; stable runner state is authority and must remain outside application payload.

Implement a topology-neutral dynamic exact-value inventory first, then one Permanent Entry-local read-only receipt adapter. Keep application coverage incomplete through that checkpoint. Next add ownership receipts at the runner/cache providers' existing exact publication boundaries and a second local adapter. The broader audit also requires managed-runtime/supervisor, legacy Stage-0/cutover, setup-owned PATH integration, and protected-service payload producers or exact-absence gates; service removal must preserve environment authority and VM/guest state. Only the accepted runtime composition may require the complete set and report `application` coverage; `purge` remains incomplete. Pending/corrupt receipts, active publication, ambiguous artifacts, dirty/wrong-subject cache state, unregistered topology, stable authority, quarantine/staging residue, receipt/control journals, legacy Stage-0, configuration, service authority, and provider/environment state remain preserved. Expose read-only inspection only after the complete set qualifies. Add no deletion or CLI until the separate operation-interlock, terminal receipt retirement, and completed-removal rotation design is accepted. No UAC/elevation, protected/provider/VM/guest effect, repository execution, model invocation, or GPU/CUDA work belongs to this slice.

First implementation candidate: one import-isolated neutral dynamic inventory now validates bounded acyclic source generations, hides private exact descriptors behind opaque effects and durable per-item bindings, and double-observes source/activity before authority can be accepted. A separate Permanent Entry-local adapter reads only its accepted ownership receipts, maps completed component/entry payload, treats pending publication as incomplete, and assigns local wrapper-before-component relationships. Disposable production-installation coverage proves read-only projection and deliberately requires a missing `runtime-payload` contributor, so application coverage stays empty.

Focused exact-Node tests pass 31/31; current and exact bounded preflights pass 2 artifacts / 239 syntax / 2 JSON / 196 targeted files; exact architecture/product/standalone passes 45 total / 44 passed / one expected Windows symlink skip; and the complete exact serialized suite passes 2,046 total / 2,025 passed / 21 expected skips / zero failures in 234.8 seconds. Exact doctor is green and VM-route fail-closed; regeneration and diff hygiene pass. Commit/push the isolated candidate and require exact-head hosted Ubuntu/Windows acceptance. Keep #391 open and move next to runner/cache receipts only after acceptance. No removal/CLI, setup/elevation, protected/physical, repository-execution, model, or GPU/CUDA effect occurred.

Hosted acceptance: exact implementation `838212d7f1d05b464d59b2ca6657bd55897f7e7c` passed Ubuntu smoke/full and Windows bounded-smoke/serialized-full plus doctor in [run 33326909315](https://github.com/iteathen/DevBridge/actions/runs/33326909315). Accept this read-only boundary and continue #391 with runner/cache ownership receipts. Application/purge coverage and every removal surface remain unavailable.

## Runner-cache ownership receipts

Assessment/research/reassessment 2026-08-30: `docs/testing/DB-HO105-issue-391-runner-cache-ownership.md` records the shared runner-cache boundary after accepted dynamic Permanent Entry inventory. Content-addressed objects, exact checkouts, their structural directories, and the credentials-free control file were verified before launch but had no durable pre-effect ownership evidence. Corrupt digest-shaped content was replaced by pathname, checkout temporary trees were recursively removed without a durable reservation, and the reusable state/projection/lease mechanisms were trapped inside the installer namespace.

Implement neutral exact value-state, receipt collection/projection, process-activity lease, exact empty-directory, and exact-action-router bricks first. Keep producer protocols, identities, relationships, paths, and topology in separate installer and runner-cache compositions. Make both providers share one cache-local preparation lease; reserve before every materialization; adopt only exact verified pre-receipt state; publish from operation-bound temporary paths; complete exact receipts before launch becomes available; revalidate at launch; and preserve corrupt, unowned, conflicting, or ambiguous material. Add one bounded read-only cache inventory with application-only coverage, but keep aggregate application and purge coverage incomplete. Expose no removal or uninstall surface until launch/removal mutual exclusion, terminal receipt retirement, and operation rotation are separately designed and accepted.

Local implementation checkpoint: the providers now use the neutral ports with no unreceipted compatibility path, exact file/tree/directory receipts cover all created or adopted cache payload, and cache authority/stable selection remain outside payload. The topology source withholds completeness for pending receipts, unknown residue, unsafe indirection, or unsupported identities. Candidate qualification and installation share one permanent-entry component manifest after the full suite caught and eliminated a stale duplicate staging list.

Exact Node.js 22.16.0 evidence passes bounded preflight at 2 standalone artifacts / 253 syntax files / 2 JSON files / 203 targeted test files, the authoritative serialized suite at 2,057 total / 2,036 passed / 21 expected platform skips / zero failures in 272.5 seconds, focused post-review cache/provider tests 11/11, and focused installer/candidate closure tests 19/19. Doctor is green with coding adapters disabled, execution disabled, and repository execution unavailable/fail-closed. Standalone reproduction and diff hygiene pass. Commit/push the isolated candidate and require exact-head Ubuntu/Windows acceptance before accepting this checkpoint or starting activity/removal interlock work. No setup/elevation, protected/provider/VM/guest effect, repository execution, model invocation, removal/publication action, or GPU/CUDA work occurred.

Hosted implementation run [33330536875](https://github.com/iteathen/DevBridge/actions/runs/33330536875) passed Ubuntu smoke/full and Windows serialized-full plus doctor, but Windows smoke killed the still-running bounded preflight at its exact two-minute step timeout. No test failed. GitHub's documented step/job timeout semantics and the observed 2m50s successful Windows full job show that the old smoke budget no longer covers the preserved 203-file gate. Keep every test and concurrency two; raise only that step to three minutes and its enclosing smoke job to four, then require a fresh exact-head four-job pass before acceptance.

Hosted acceptance: exact implementation and CI-contract head `5497c46e4f55a9ab734538eb79bac138ddc6baf8` passed Ubuntu smoke/full and Windows bounded-smoke/serialized-full, architecture gates, standalone regression, and doctor in [run 33331176770](https://github.com/iteathen/DevBridge/actions/runs/33331176770). Accept the runner-cache receipt/inventory checkpoint and keep #391 open. Continue next with launch/removal mutual exclusion, terminal receipt retirement, and operation rotation; application/purge coverage and all removal/uninstall surfaces remain unavailable.

## Removal interlock, receipt retirement, and operation rotation

Assessment/research/reassessment 2026-08-30: `docs/testing/DB-HO106-issue-391-operation-interlock-retirement.md` records the exact race and lifecycle gaps after accepted runner-cache receipts. Preparation is leased but launch is not; read-only activity observation cannot stop launch/removal admission between observation and effect; per-effect locks leave gaps across a multi-effect operation; completed ownership receipts prevent exact absent topology; immutable binding records collide with a later reinstall; and a completed removal record cannot admit a different exact plan.

Extend the neutral activity brick with one awaited same-operation callback transaction, hold module-local transactions around launch and the complete removal journal session, and acquire required contributors in deterministic neutral-identity order. Remove/reconcile every payload effect before a separately journaled terminal-retirement sweep changes producer generations. Receipt sources conditionally retire only the exact completed item, binding records advance from bound to retired and may be replaced only by a later source generation, and a completed removal record rotates only for a newly confirmed different exact plan. Preserve private descriptors, immutable receipt history/control anchors, exact artifact re-observation, fail-closed changed/reinstalled state, and all currently incomplete producer gates. Add no CLI, live canonical removal, setup/elevation, protected/provider/VM/guest effect, repository execution, model invocation, or GPU/CUDA work in this slice.

Local implementation checkpoint: the neutral lease now provides awaited same-operation transactions while raw observation continues to report physical activity. Each exact inventory owns only the suppression of its own live transaction; peer and leaked contexts still observe/block. Permanent Entry installation and runner-cache preparation use the callback primitive, runner launch keeps it through exact revalidation and awaited completion, and application removal nests every required contributor in deterministic neutral-identity order for its complete journal session.

Exact receipt CAS retirement, bound/retired inventory revisions, opaque effect retirement, a separately journaled post-effect retirement sweep, mode-bound journal recovery, and newly confirmed completed-operation rotation are implemented without a compatibility path. Every effect is bound after durable confirmation and before deletion. Producer completeness is separate from current materialization consistency, so only an all-bound plan may retain its generation across its own intended effects; pending/new/missing/changed producer state remains fail-closed. Terminal receipts retire in reverse dependency order, and the runner-cache topology owner translates only an exact current projection to its receipt CAS generation. Retired evidence remains recovery-readable but cannot mint fresh action authority; changed generations and reinstalled artifacts are preserved. The standalone installer is regenerated.

Focused evidence passes 78/78, a 40/40 broader post-preflight regression set, a 49/49 review-driven multi-item/installer set, and a final 19/19 inventory/cache subset. A disposable real runner-cache transaction removed dependency-ordered payload, retired every exact receipt, and projected complete empty application coverage. Current Node 24.15.0 and exact Node 22.16.0 preflights both pass 2 standalone artifacts / 253 syntax files / 2 JSON files / 203 dependency-selected tests. The complete serialized repository suite passes with exit 0 on exact Node 22.16.0 after those fixes. Exact doctor is green but truthfully VM-route fail-closed and `setup-reentry-required`; artifact freshness and diff hygiene pass. Planning head `8383dbce9433df62d1033af63045b913f8b21aa1` passed all four hosted jobs in [run 33332054305](https://github.com/iteathen/DevBridge/actions/runs/33332054305). Commit/push the exact implementation and require its four-job hosted matrix before acceptance. Keep #391 open; no uninstall CLI, live canonical removal, setup/elevation, protected/provider/VM/guest mutation, repository execution, model invocation, or GPU/CUDA work occurred.

Hosted acceptance: exact implementation commit `c2c95fb059b235cddf18a5817070a77b1981e949` passed Ubuntu smoke/full and Windows bounded-smoke/serialized-full, architecture gates, standalone installer regression, and doctor in [run 33334772070](https://github.com/iteathen/DevBridge/actions/runs/33334772070). Accept the launch/removal interlock, terminal receipt retirement, and completed-operation rotation boundary. Keep #391 open and continue primitive-first with complete application/purge producer coverage and the user-facing uninstall/reconfiguration surface; no live canonical removal, setup/elevation, protected/physical, repository-execution, model, or GPU/CUDA action occurred.

## Command exposure ownership

Assessment/research/reassessment 2026-08-30: `docs/testing/DB-HO107-issue-391-command-exposure-ownership.md` records the next missing application-payload owner. Permanent Entry and runner-cache receipts do not own the separate stable command written by setup or the Windows current-user PATH/POSIX profile publication that exposes it. The current setup mutates both without a pre-effect reservation, completed receipt, restart evidence, or activity transaction. Those shared operator values cannot be inferred removable later from a marker or pathname.

Implement an owner-local closed command-exposure action first, then a stable activity transaction, immutable conditional receipts, exact restart reconciliation, and one read-only dynamic inventory. Bind exact complete before/after shared values; automatically remove only while the complete value remains the accepted post-effect value; preserve duplicate, foreign, linked, changed, or ambiguous state. Use the accepted exact-artifact action for the command file, remove the exposure before the command, and keep all descriptors private. Refactor setup onto the new owner with no compatibility mutation path. Qualify only in disposable homes and keep production application/purge coverage incomplete and every uninstall CLI unavailable. No canonical mutation, setup/elevation, protected/provider/VM/guest action, repository execution, model invocation, or GPU/CUDA work is permitted.

## Windows protected network capability

Physical checkpoint 2026-09-01: `docs/testing/DB-HO114-issue-372-windows-network-capability.md` records the first exact failure after the current five-endpoint protected service reached a healthy running generation. The configuration endpoint is available, but provider-owned network inspection under the virtual service identity fails at `Get-NetNat` with Windows `System error.` Hyper-V authority and protected filesystem ACLs are already exact; no manual host correction occurred.

Keep #372 open and add only fixed Network Configuration Operators SID `S-1-5-32-556` beside the existing fixed Hyper-V Administrators SID. The immutable plan owns both capabilities, one elevated idempotent adapter admits and re-observes exact membership, and the read-only protection proof requires both. Preserve the virtual account, pipe/DACL boundaries, immediate single-UAC path, ordinary configuration channel, and exact consent flow. Add no Administrators/LocalSystem authority, caller-selected group, networking implementation in the service installer, helper service, or direct elevated fallback.

Require complete local and hosted qualification plus fresh post-integration Stage 8 acceptance before one new ordinary setup re-entry. Physical acceptance must prove exact group memberships and successful ordinary configuration reconciliation. If the standard least-authority group remains insufficient for the required operation, stop and reassess from new evidence rather than widening authority. Environment activation, route publication, GitHub Hello World compilation/testing, and restart repetition remain downstream gates.

Local implementation checkpoint: the final-byte candidate uses two explicit plan-owned fixed capability SIDs, validates both before the first service mutation, admits missing membership idempotently, immediately re-observes exact postconditions, and requires both in the separate read-only structural proof. Focused service/protection tests pass 44/44; architecture/product/standalone/LEGO tests pass 19/19; exact Node 22.16 preflight passes two artifacts / 255 syntax / two JSON / 205 targeted files; doctor is green and route fail-closed; and the complete exact serialized suite passes 2,110 total / 2,089 passed / 21 expected skips / zero failures in 85.4 seconds. Commit and push the candidate, require all four pull-request jobs, merge only after exact acceptance, and require the fresh Stage 8 post-integration matrix before install-only or setup.

## Windows provider logon identity

Physical rejection and reassessment 2026-09-01: `docs/testing/DB-HO115-issue-372-windows-provider-logon.md` records that exact accepted Stage 8 head `737890fdd73e5594fce90fc73bbe906a9fd8aef1` installed both DB-HO114 fixed group memberships, yet the exact virtual service still received `System error.` from `Get-NetNat`. The narrow standard-group hypothesis is rejected. Do not preserve it as a fallback or retry it manually.

Keep one measured lifecycle authority. Separate its unique service-SID ACL identity from its fixed SCM provider logon identity: protected files and five named pipes continue to use `NT SERVICE\<exact-name>`, while SCM uses explicit `LocalSystem`. During the same quiesced elevated refresh, retire and post-verify absence from the two obsolete groups. Exact plan validation must precede every effect; exact service proof and independent structural protection proof must close the result. Add no helper service, caller-selected account/group, WMI ACL edit, direct elevated network route, or compatibility path.

This is an explicit security trade: Microsoft documents LocalSystem as extensively privileged, so require complete local and hosted qualification and one exact physical acceptance. If ordinary configuration still cannot reconcile the exact accepted network plan, stop. After route health, continue the original goal through GitHub task receipt and Hello World compile/test; do not infer those downstream results from provider readiness.

Local implementation checkpoint: the plan now separates unique service-SID ACL identity from fixed LocalSystem SCM logon, refresh and legacy migration recognize the retired virtual logon only for one exact owned transition, and every steady-state proof remains strict. The quiesced service transaction removes only the two formerly admitted fixed group memberships and independently proves their absence. Focused tests pass 90/90; exact Node 22.16 preflight passes two artifacts / 255 syntax / two JSON / 205 targeted files; architecture/product/standalone/setup gates pass 40 plus one expected Windows symlink skip; doctor is green and route fail-closed; and the complete serialized suite passes 2,111 total / 2,090 passed / 21 expected skips / zero failures in 335.3 seconds. Artifact/diff hygiene passes and the checked disposable runtime is deleted. Commit and publish only this isolated candidate, then require all four pull-request jobs, merge, and require the fresh Stage 8 matrix before install-only or setup.

## Windows protected-pipe re-arm

Physical checkpoint 2026-09-01: exact accepted Stage 8 head `7c4edd4a044cefccb455fe5141083f6e387077bc` installed successfully. One ordinary setup requested UAC at elapsed zero and completed its protected transaction at 190 seconds, but candidate health reported the configuration authority unavailable and restored exact previous generation `83e931ffb86024ef868caff3fac67cd28d0cf144d5b9b78e2a82d434ece9c4cf`. The provider-logon transition persisted safely: SCM now reports Running/Automatic `LocalSystem`. No setup retry or manual host correction occurred.

`docs/testing/DB-HO116-issue-372-windows-pipe-rearm.md` records the independent IPC diagnosis. All five listeners exist, yet adjacent read-only calls intermittently fail. A disposable compiled-host test requiring 30 immediate public-client inspections reproduces native zero-response `EPIPE` after a prior one-instance request. Microsoft requires named-pipe clients to wait/retry unavailable instances and warns that a successful wait does not guarantee the subsequent open.

Keep the one first-instance server and distinct capability protocols. Add one neutral bounded local transaction adapter: Windows open `ENOENT`/`EBUSY` may retry within the original connect deadline; only exact `inspect` may retry a zero-response `EPIPE`; complete bounded frames survive the normal Windows disconnect; mutations, partial responses, denials, Linux absence, and every other error remain single-attempt/fail-closed. Focused final-byte evidence passes 34/34, including 30 immediate disposable compiled-host inspections and explicit no-mutation/no-partial-replay boundaries. The first complete run caught an unreferenced awaited retry delay through twelve cancellations; keeping that bounded timer live corrected the shared LEGO without widening its deadline. Final exact Node 22.16 qualification passes preflight at 2 artifacts / 256 syntax / 2 JSON / 206 targeted files, doctor `ok: true` and truthful `setup-reentry-required`, and the complete serialized suite at 2,116 total / 2,095 passed / 21 expected skips / zero failed / zero cancelled in 350.4 seconds. The checked temporary runtime and eight exact cancelled-run test directories were deleted with absence verified.

Run the complete local qualification and hosted PR/post-integration matrices before one fresh setup authorization. Environment route health, GitHub task receipt, and Hello World compile/test remain downstream. Add no setup delay, timeout widening, extra pipe/server/service, mutation replay, provider fallback, or manual host repair.

## Windows read-capability pipe re-arm

Physical checkpoint 2026-09-01: exact accepted Stage 8 head `e5f5ffbfb1e96ce24447824cf88dd27b837babd9` passed all four jobs in pull-request run `33564367977` and fresh post-integration run `33565085111`, then installed exactly. One ordinary setup requested UAC at elapsed zero and completed its protected transaction at 139 seconds and protected verification at 323 seconds. Environment activation began at 324 seconds and failed at 346 seconds because lifecycle authority was unavailable. The service nevertheless remains Running/Automatic `LocalSystem` at exact new generation `19fa5bc9cadb87774c94388d536beea9ef91d156a887803b9aaa3b9a36c11cf2` with all five pipe endpoints. No retry or manual host correction occurred.

`docs/testing/DB-HO117-issue-372-windows-read-capability-rearm.md` records the next precise IPC boundary. Setup activation begins with lifecycle `list`, but DB-HO116 allowed zero-response re-arm replay only for literal `inspect`. The lifecycle protocol already owns five read operations separately from `run`/`resume`; activity likewise owns `inspect`/`list`/`observe` separately from `prepare`/`exchange`. The strengthened compiled-host regression reproduced lifecycle failure at request 12 and native zero-response activity `EPIPE` on the same one-instance disconnect seam.

Keep one neutral bounded transaction LEGO and let each protocol owner classify only its own replay-safe reads. Rename the shared input to `replaySafe`; preserve the original deadline and retry only Windows named-pipe zero-response `EPIPE`. Mutations, partial responses, denials, Linux absence, and other errors remain single-attempt. Add no setup sleep, timeout widening, extra host/service, mutation replay, provider fallback, or manual repair.

Focused evidence passes 49/49 and the broader setup/authority/architecture set passes 307/307, including 90 consecutive read requests against a disposable compiled protected host. Three live read-only lifecycle lists against the installed service pass in 21.9–22.0 seconds each through the corrected transport with no caller retry or host mutation. Current and exact Node 22.16 preflights pass 2 artifacts / 256 syntax / 2 JSON / 206 targeted files; the complete exact serialized suite passes 2,118 total / 2,097 passed / 21 expected platform skips / zero failed / zero cancelled in 340.9 seconds. Exact doctor is green with GitHub and native C/CMake/CTest available and repository execution truthfully unavailable pending setup. Artifact/diff hygiene passes; all 117 qualification-created temporary roots, including the checked Node runtime, are deleted with absence verified. Require all four pull-request jobs and a fresh four-job Stage 8 integration run. Bind only that exact accepted head, and require fresh authorization before one new ordinary setup attempt. Route health, GitHub task receipt, and Hello World compile/test remain downstream.

## Windows read re-arm deadline

Physical checkpoint 2026-09-01: exact accepted Stage 8 head `d7f9b45ba0fef0e2677eaf39afb5d60d213b1a5d` passed all four pull-request jobs in run `33568922051` and all four fresh post-integration jobs in run `33569257424`, then installed exactly. The first ordinary setup invocation prepared protected apply in 42 seconds and required explicit re-entry. After fresh authorization, the single ordinary re-entry requested UAC at elapsed zero, completed its protected transaction at 236 seconds, completed verification and checkpointed the exact applied subject at 469 seconds, began activation at 470 seconds, and failed at 492 seconds because lifecycle authority was unavailable. The protected service remains Running/Automatic `LocalSystem` at exact applied generation `4586b2932e283ea48a4cbc2158bd96faf9e8137162b798c2b3eab6b0333a9810`. No retry or manual host correction occurred.

`docs/testing/DB-HO118-issue-372-windows-read-rearm-deadline.md` records the exact connection-boundary defect. Setup lifecycle `list` legitimately spends approximately 22 seconds observing providers, but the DB-HO117 adapter calculated the Windows zero-response replay deadline before the first connection. The later stale-instance `EPIPE` therefore arrived after the three-second connection deadline and could never re-arm.

Keep the neutral adapter and protocol-owned read classification. Give the first connection its caller-selected bounded window. Only the first replay-safe Windows zero-response `EPIPE` opens one new bounded connection re-arm budget. Reconnect delay and pipe-open work consume that budget; time inside an established replay-safe transaction does not, and later failures cannot replenish consumed connection budget. This preserves the compiled one-instance host contract without converting connection timeout into operation timeout. Mutations, partial responses, denials, Linux absence, and all other errors remain single-attempt/fail-closed. Add no setup sleep, timeout widening, extra server/service, provider change, caller retry, fallback, or manual repair.

An exact serialized run rejected the intermediate wall-clock-window form on activity read 21/30; temporary diagnostics proved a native zero-response `EPIPE` after connected activity cleanup consumed the nominal connection window. Final focused evidence passes 43/43, including 9/9 deterministic adapter tests, and five exact-runtime disposable compiled-host cycles pass 450 total five-endpoint reads. Exact Node 22.16 preflight passes 2 artifacts / 256 syntax / 2 JSON / 206 targeted files; the complete exact serialized suite passes 2,122 total / 2,101 passed / 21 expected Windows skips / zero failed / zero cancelled in 333.2 seconds. Exact doctor is green, lifecycle-ready, and truthfully repository-execution unavailable until the absent environment is created. Diff/artifact hygiene passes. Cleanup removed the checked runtime and all 1,770 same-day qualification Temp roots (334.3 MiB after runtime deletion), with final matching-root count zero. Require hosted PR and fresh Stage 8 qualification before acceptance. After those matrices, bind only the accepted head and obtain fresh authorization for one new ordinary setup attempt. Route health, GitHub task receipt, and Hello World compile/test remain downstream.

## Visible, identified Windows elevation

Physical checkpoint 2026-09-01: exact accepted Stage 8 head `ee87c7fb7f6ff9f3472c32b4676f234886214744` left the durable protected-apply frontier prepared. Two separately authorized ordinary re-entries reported that Windows was asked for consent immediately, waited through the bounded transaction, and returned cancellation/refusal without starting construction. During the latest attempt, direct ordinary-desktop observation found no consent surface, and the operator independently confirmed no UAC screen appeared. Host policy keeps UAC and secure desktop enabled and UAC works in other contexts. No retry or manual host correction occurred.

`docs/testing/DB-HO119-issue-430-visible-identified-elevation.md` records the source defect and narrow correction. The generic process adapter had forced every Windows child hidden, while the elevated target was generic `powershell.exe`; that combination neither reliably surfaced consent nor identified DevBridge or its purpose. Keep generic commands hidden by default, but give the elevation adapter one explicit visible outer handoff. Replace the elevated PowerShell broker with one content-addressed DevBridge-owned launcher whose displayed description says `DevBridge Protected Setup - reconcile lifecycle service and protected environment` and whose bounded fallback filename is `DevBridge-Protected-Setup-Lifecycle-Environment.exe`. Immediately before handoff, state the full reason in the ordinary CLI. Bind the launcher to exact source, manifest, home, Node bytes, detached runner bytes/head, fixed child argv, and the existing bounded result channel. Preserve one prompt, one helper, one protected transaction, cancellation durability, and ordinary post-child verification.

The helper carries an explicit `asInvoker`, `uiAccess=false` manifest; `RunAs` remains the sole elevation request. DevBridge has no established Authenticode authority, so Windows must truthfully report an unknown/unverified publisher. Do not create a local certificate, change UAC policy, automate secure desktop, add a second helper, admit caller-selected executable/argv, or widen service/provider authority. Qualify compilation/identity, reuse, tamper/stale rejection, exact input/child command, visible-only elevation composition, setup checkpoint/re-entry, repository preflight, architecture/product/standalone gates, the complete exact serialized suite, doctor, cleanup, and both hosted matrices before installing. Then obtain fresh authorization for one physical re-entry; only the operator's direct observation can prove the prompt appeared and carried the expected requester/purpose identity. Route health, real GitHub task receipt, and Hello World compile/test remain downstream.

Local implementation checkpoint: focused command/setup/elevation evidence passes 85/85 on Node.js 24.15.0; preflight passes two standalone artifacts / 256 syntax files / two JSON files / 206 dependency-selected tests; architecture/product/standalone LEGO gates pass 23/23; and the complete serialized suite passes 2,126 total / 2,105 passed / 21 expected Windows skips / zero failures / zero cancellations in 329.4 seconds. Doctor is green with GitHub and native C/CMake/CTest available and repository execution truthfully unavailable pending the absent environment. Standalone regeneration and diff hygiene pass. Cleanup removed 120 qualification-created temporary roots with zero remaining; ambiguous canonical elevation evidence remains preserved. Commit and publish only this isolated candidate, require all four pull-request jobs, merge only the accepted exact head, and require a fresh four-job Stage 8 matrix before install-only or another physical authorization. No UAC or protected/physical/repository-task effect occurred.

First hosted classification: PR run `33578473128` passed both Ubuntu jobs and Windows smoke, but Windows full failed the new helper `--identity` process with exit 2. The candidate was not merged. The failure belongs to DB-HO119 and exposed an unnecessary JSON-runtime dependency in a fixed identity response. Replace only that response with exact static UTF-8 bytes and add a direct ordinary-process test proving the full bound input parses before the helper rejects absent administrator authority without writing a result or requesting UAC. Post-correction focused 85/85 and preflight 2 / 256 / 2 / 206 are green. Require a fresh complete matrix; do not rerun only the failed job and do not advance the physical gate.

Second hosted classification: fresh complete run `33578975609` passed Ubuntu smoke/full and Windows smoke; Windows full accepted fixed identity and then reported `TypeInitializationException` while directly validating the bound input before the administrator check. Keep the candidate unmerged. Remove only the launcher's unnecessary process-wide regular-expression and field-set initialization, retain equivalent local pure validation, and report a bounded exception-type chain for any remaining hosted mismatch. Focused 85/85, exact standalone artifacts, and preflight 2 / 256 / 2 / 206 pass after the correction; all 29 newly created temporary roots were deleted with zero remaining. Require another complete four-job run. No setup, UAC, protected host, construction, or repository-task effect occurred.

Third hosted classification: fresh complete run `33579543488` passed Ubuntu smoke/full and Windows smoke; Windows full exposed the bounded root `TypeInitializationException > ConfigurationErrorsException > PathTooLongException` from `System.Web` JSON initialization beside the deliberately descriptive content-addressed executable. Preserve the UAC identity and exact install binding. Remove only the ambient configuration-dependent serializer, use explicit `System.Runtime.Serialization`/`System.Xml` framework references, retain the exact eight-field input and bounded result contract, and forbid regression to `System.Web` in source tests. Focused evidence passes 86/86, standalone artifacts are exact, preflight passes 2 / 256 / 2 / 206, and all 29 new temporary roots are deleted. Require another complete four-job run; do not merge or advance the physical gate from partial success.

Fourth hosted classification: fresh complete run `33580106665` passed Ubuntu smoke/full and Windows smoke, but Windows full returned the same configuration-system `PathTooLongException` without `System.Web`. The legacy .NET runtime probes an adjacent `.config` path for other framework facilities, and the hosted content-addressed fixture crossed its 260-character boundary. Keep the full UAC file description and immutable bundle. Shorten only the fallback filename to the still-specific `DevBridge-Protected-Setup-Lifecycle-Environment.exe` and reject any launcher location whose adjacent configuration probe would reach 260 characters. Focused evidence passes 87/87, exact standalone verification and preflight 2 / 256 / 2 / 206 pass, and cleanup leaves zero new temporary roots. Require another complete matrix; do not merge or advance the physical gate.

Fifth hosted classification: fresh complete run `33580625567` passed Ubuntu smoke/full and Windows smoke. Windows full cleared `PathTooLongException` and then returned bounded `InvalidDataException` during strict helper input validation. The direct helper fixture owned home, state, channel, runner, and input but bound ambient setup-node `process.execPath`; the production helper correctly rejects filesystem indirection anywhere in an executable path chain. Keep that protection. Bind the test to one fixture-owned `node.exe` byte artifact and retain separate fixed-command/argv coverage. Focused 87/87 and preflight 2 / 256 / 2 / 206 pass, and all 29 new temporary roots are deleted. Require another complete matrix; do not weaken the broker for hosted tool-cache topology or advance the physical gate.

Sixth hosted classification: fresh complete run `33581065389` passed Ubuntu smoke/full and Windows smoke, while Windows full still returned the same bounded `InvalidDataException` after every bound fixture file became test-owned. Reject the earlier tool-cache inference and keep every production validation boundary intact. The next correction is diagnostic only: wrap input validation with one fixed stage token, retain the original exception as a bounded inner type, and never disclose input values, paths, or messages. Prove the diagnostic contract with a malformed-input process test that also proves the fixture root is absent, then require another complete hosted matrix to identify the exact owned boundary before changing behavior.

Diagnostic qualification: all 87 focused tests pass, including a direct malformed-input process proof of exact `stage=protocol` output with no fixture-root disclosure. Preflight passes 2 artifacts / 256 syntax / 2 JSON / 206 selected tests; the hosted-equivalent architecture/product/standalone selection passes 36 with one expected Windows symlink-capability skip; the complete serialized suite passes 2,128 total / 2,107 passed / 21 expected skips / zero failures / zero cancellations in 326.2 seconds; doctor, standalone regeneration, and diff hygiene are green; and zero new disposable elevation roots remain. Publish only this diagnostic candidate and require all four hosted jobs. Change behavior only after the fixed stage token identifies the owned rejection boundary.

Seventh hosted classification: run `33582093924` passed Ubuntu smoke/full and Windows smoke. Windows full named only `stage=binding-digest`, proving every earlier protocol, identity, channel, physical-path/reparse, fixed-name, runner-head, and file-byte validation passed. The cross-runtime defect is the final formula: JavaScript hashes the exact normalized path strings it publishes, while the helper hashes a second .NET-normalized representation even though the child later consumes the published fields. Preserve every validation and hash those exact published fields in the helper. Add a source contract forbidding regression to the second representation, qualify locally, and require a fresh complete matrix before merge.

Exact-string correction qualification passes 88/88 focused tests plus preflight 2 / 256 / 2 / 206; standalone artifacts and diff hygiene are exact; zero disposable launcher roots remain. The immediately preceding diagnostic head already passed all 2,128 serialized tests locally, so require the fresh hosted matrix for the cross-runtime proof rather than repeating that unchanged local surface.

Eighth hosted classification: run `33582561706` passed Ubuntu smoke/full and Windows smoke. Windows full cleared binding validation and reached the current-principal boundary. The hosted Windows runner is already administrative, so the `asInvoker` helper inherited that token without UAC and returned the bounded broker failure from the deliberately invalid fixture executable; the test had assumed every runner was non-administrative. Keep production unchanged. Make only the process proof principal-aware: require either non-admin denial plus no result, or an admin-only bounded `started=false` broker record with no child output. Require another full matrix before merge.
