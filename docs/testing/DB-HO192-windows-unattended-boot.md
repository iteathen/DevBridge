# Unattended Windows installation media

The first Windows construction attempt reached a Hyper-V UEFI boot-failure screen and retained its initial 4 MiB VHDX allocation through its two-hour deadline. The admitted Microsoft ISO contains `efi/microsoft/boot/efisys_noprompt.bin`. Its ordinary DVD boot path is unsuitable for an unattended first boot. The console proves boot failure, not which transient prompt preceded it.

The scoped change belongs to the existing media writer and unattended preparation adapter. Prepare a UDF installer with the admitted ISO's complete file tree and Microsoft's own no-prompt EFI boot image. Keep the original ISO's admission identity, image selection and approval as source evidence; return the prepared ISO's actual digest and size for the construction attachment. Advance the recipe and output generations so a new attempt cannot inherit the expired attempt's receipt or deadline. Preserve historical v1 authority records for observation.

Microsoft documents [IMAPI boot image assignment](https://learn.microsoft.com/en-us/windows/win32/imapi/adding-a-boot-image) through `IBootOptions`, and [the EFI platform value](https://learn.microsoft.com/en-us/windows/win32/api/imapi2fs/ne-imapi2fs-platformid) as `0xef`. The provider retains its Microsoft UEFI secure-boot and TPM requirements. This change does not alter guest execution boundaries, credentials, licensing, or accepted source authority.

`AssignBootImage` [automatically selects floppy emulation for a 1.44 MiB image](https://learn.microsoft.com/en-us/windows/win32/api/imapi2fs/nf-imapi2fs-ibootoptions-assignbootimage). The writer must set no emulation after assigning the image. A native catalog read caught this ordering requirement; the native regression uses a 1.44 MiB fixture and checks the actual EFI catalog and boot bytes.

The media writer verifies and holds the exact source ISO while it mounts the source read-only, assigns the EFI boot image, and writes a bounded output. IMAPI's tree import requires a drive-letter path on this host; its volume-GUID path returned an invalid-parameter error. Mounts created by the writer are detached in cleanup; pre-existing mounts remain owned by their caller. Output publication is create-only and source identity is checked again afterwards. Preparation budgets both source and generated media; after successful preparation, the composition removes only its copied staging ISO before disk construction begins.

Validation covers input identity, output collisions, native failure cleanup, invalid returned media identity, distinct source/output evidence, generation selection and peak storage. Native qualification must establish the generated EFI boot catalog, unchanged installer contents, detached source, and an actual installation boot before claiming Windows readiness. Neither a written ISO nor a console image establishes Hello World completion.

On 2026-09-06 native preparation produced 8,471,969,792 bytes with SHA-256 `2410a8be219afd6aa9eeb52c27f4a7fc9b84588daa880f902ecfdfc54206f32f`. Independent reads verified a checksummed EFI El Torito catalog, bootable/no-emulation entry, and boot-image SHA-256 `bc1df11a9148b4e3b60b32095ca3dc5d400ca0d16e2b43d3a8a0282a9e66c4d5`, matching the admitted ISO's no-prompt asset. The normal WIMGAPI inspector still observed Windows 11 Pro index 6, amd64, version 10.0.26200.8037. All three native IMAPI tests passed, including failure detachment and the emulation regression. Repository preflight passed; the focused suites passed after correcting a test that incorrectly expected storage preflight to throw instead of returning a blocked result. Actual installation remains the next qualification step.
