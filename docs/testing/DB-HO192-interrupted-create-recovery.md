# Superseded interrupted creation

The protected Linux environment has an active create at `fenced-attempt` from September 2, while accepted setup advanced its declaration to revision 2 and the qualified Ubuntu image. Setup repeatedly recommends resume, but creation rejects the changed revision. The observed materialization is present with invalid storage/attachment for the new declaration. This is a lifecycle recovery defect, not a missing Linux image.

The scoped repair is confined to lifecycle failure recording and its persistence adapter. On resume, a create whose declaration was superseded acquires the existing fence, rechecks both authorities, and appends terminal failure. It performs no construction, checkpoint removal, provider mutation, or declaration rewrite. Retain the complete terminal journal by operation identity before the next lifecycle operation begins. Current-generation status and an explicit impact plan continue to own any subsequent rebuild/recreate decision.

Verify preserved evidence across restart, rejected success-stage skipping, fence rejection, declaration/journal drift, and unchanged normal create/resume behavior. Native recovery must go through the updated installed lifecycle authority and its ordinary client, followed by the normal replacement owner if its exact impact is acceptable. Never edit protected state files to advance this attempt.
